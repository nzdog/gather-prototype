# GTC-153 — Fix stale file references in GATHER-BUILD-CONSTANTS.md — CLOSED

**Status:** Complete | **Branch:** feat/moment-one-redesign | **Commit:** (backfilled after commit)

**Severity:** Low (doc drift in the doctrine file — misleads executors, no runtime impact)
**Surfaced by:** Skill-library authoring run (2026-07-09). Three independent authoring/review
agents flagged the same drifts while ground-truthing skills against the repo.

### Signal

GATHER-BUILD-CONSTANTS.md ("Keep this file accurate", last updated 2026-03-05) contained three
references that no longer match the repo:

1. **Executor Preamble step 2** listed `FEATURE-TICKET-TEMPLATE.md`, `CHORE-TICKET-TEMPLATE.md`,
   `SPIKE-TICKET-TEMPLATE.md` — none exist at root (verified: `ls *TEMPLATE*.md` returns only
   BUG-TICKET-TEMPLATE.md, BUG-TICKET-TEMPLATE-FULL.md, UX-TICKET-TEMPLATE.md,
   oldBUG-TICKET-TEMPLATE.md). The list also omitted the two templates that DO exist
   (BUG-FULL, UX).
2. **Do-Not-Touch Zone 1** cited `src/middleware*` — the middleware lives at repo root
   (`middleware.ts`, 116 lines); `src/middleware.ts` does not exist. A grep or edit targeting
   the stated path silently misses the protected file.
3. **Do-Not-Touch Zone 6** cited `scripts/classify-routes.ts` — removed; the living tool is
   `scripts/triage-unknown-routes.ts`.

### Scope

**Fix here:** GATHER-BUILD-CONSTANTS.md only. The four `.claude/skills/` files that carried
"constants file has drifted" flags were updated in the working tree to record the correction;
they land with the pending skill-library commit (the library predates its first commit as of
this ticket). No templates invented:
the preamble now routes feature/chore/spike tickets to the BUG template structure and
`docs/tickets/` precedents (e.g. GTC-152), matching actual practice.
**Do not touch:** the zones themselves, templates, any code.

### Evidence (Executor-Completed)

- Each drift re-verified against the repo before editing (ls / grep, 2026-07-09) — not taken
  from the review report on trust.
- Diff reviewed and approved by Nigel in chat before writing.
- Skills updated to reflect the correction: gather-change-control (preamble template list,
  Zone 1 row, Zone 6 note, known-drift provenance), gather-experiment-methodology (spike
  template note), gather-validation-and-evidence (classify-routes note),
  gather-docs-and-writing (template trap note).
- `npm run typecheck` and `npm run test:security` pass (doc-only change; run as part of the
  paired GTC-154 verification in the same session).
- "Last updated" stamp bumped to 2026-07-09.

### Known limitations

- `tests/security-inventory-gate.ts` failure text still mentions `classify-routes.ts`, and
  `scripts/triage-unknown-routes.ts` expects SECURITY_ROUTE_INVENTORY.md at repo root while it
  lives in `docs/05_ops/security/`. Out of scope here; listed as adjacent problems.
