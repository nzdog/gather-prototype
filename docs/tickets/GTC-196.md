---
ticket: GTC-196
title: "A3b — Ledger wiring: fold frozen-edit into the ordinary routes, record every change"
status: open
branch: experiment/send-lock-ledger
commit:
moment: 4
type: architecture-build
depends_on:
  - "[[GTC-169]]"
blocks:
  - "[[GTC-197]]"
tags: [moment-4, hinge, epic-a, state-machine, ledger, migration, experimental]
---

# GTC-196 — A3b: Ledger wiring

**Status:** Open | **Branch:** `experiment/send-lock-ledger` | **Commit:** [pending] | **Effort:** xhigh

**Plan of record:** [`docs/04_roadmap/send-lock-reconciliation-plan.md`](../04_roadmap/send-lock-reconciliation-plan.md)
§4.2 Layer 2 (GTC-167, commit `76943e4`). Filed 2026-08-03 as part of the approved A3 four-way split
— see [[GTC-169]] (A3a), [[GTC-197]] (A3c), [[GTC-198]] (A3d).

---

### Context

A3a removed the gates. This ticket makes every mutation carry its story: `recordChange()` (built in
[[GTC-168]]) is wired into every route that changes plan state, and `frozen-edit` — the 499-line
route that did allow+reason for three actions, FROZEN-only, unconditionally — is deleted and
redistributed.

Large diff, low risk: this changes what the system *records*, not what it *permits*.

**A3a and A3b must merge together.** Between them, post-send mutations are allowed but not recorded.
That window exists only between two commits on an undeployed branch (plan §4.3).

### Rulings folded in (Nigel, 2026-08-03)

- **Coordinators — (a):** the ledger is actor-agnostic. Coordinator routes get `recordChange()` on
  the same terms as host routes, with `actorKind: 'COORDINATOR'`. There is no host-only path.
- **Post-send regeneration — (a) now, (c) once F1 exists,** *"with the consequence stated plainly in
  the UI before the act."* This ticket implements (a): regenerate emits one bulk changeSet (one
  `changeSetId`, one reason, N entries) plus one `PlanRevision` checkpoint. The UI warning is
  [[GTC-197]] (A3c); **the copy is specified there and must land in the same merge** — allowing the
  act without the warning would ship half of a ruling.
- **§13.1 required-reason-never-blocks — endorsed.** No route added here may 4xx for a missing
  reason. A touching-someone change without one lands, recorded `reasonRequired: true, reason: null`.

---

## Scope

### 1. Delete `frozen-edit` and redistribute it

`src/app/api/events/[id]/frozen-edit/route.ts` (499 lines) is removed. Its three actions fold into
the ordinary routes, which gain an optional `reason` field:

| `frozen-edit` action | Destination | Reason? |
|---|---|---|
| `reassign` | `POST`/`DELETE /api/events/[id]/items/[itemId]/assign` | **Yes** — T1 |
| `edit_item` | `PATCH /api/events/[id]/items/[itemId]` | **Only if T4** (`ASK_FIELDS` changed AND `response != PENDING`) |
| `toggle_critical` | `PATCH /api/events/[id]/items/[itemId]` | **No** — Moment 4 §8.3: criticality *"touches nothing else… entirely a host-facing signal."* Today's unconditional reason demand is the contradiction the discovery report flagged. |

Preserve, do not reinvent: `handleReassign`'s two correct halves — capture the why, and notify the
released person. The why becomes `recordChange()`'s `reason`; the release notification becomes a hook
`onAssignmentReleased(personId, itemId, changeSetId)` that [[GTC-176]] (D3) implements. Its
`logInviteEvent(..., metadata.auditType: 'FROZEN_EDIT')` writes are replaced by real ledger entries;
`InviteEvent` keeps `MANUAL_OVERRIDE_MARKED` for genuine channel overrides only.

### 2. Wire `recordChange()` into the 25 mutation routes

```
api/events/[id]/route.ts (PATCH — date/venue: the T5 site; today ungated AND unlogged)
api/events/[id]/items/[itemId]/route.ts, .../assign/route.ts
api/events/[id]/teams/[teamId]/items/route.ts
api/events/[id]/people/route.ts, .../[personId]/route.ts,
  .../[personId]/manual-override/route.ts, .../auto-assign/route.ts, .../batch-import/route.ts
api/events/[id]/households/route.ts, .../[householdId]/route.ts
api/events/[id]/review-items/route.ts, .../items/mark-for-review/route.ts
api/events/[id]/conflicts/[conflictId]/execute-resolution/route.ts
api/events/[id]/finalize-plan/route.ts, .../generate/route.ts, .../regenerate/route.ts
api/c/[token]/items/route.ts, .../[itemId]/route.ts, .../[itemId]/assign/route.ts,
  .../ack/[assignmentId]/route.ts
api/h/[token]/people/[personId]/manual-override/route.ts
api/p/[token]/ack/[assignmentId]/route.ts
api/join/[token]/claim/route.ts
api/templates/[id]/clone/route.ts
```

**Guest-side routes write to `InviteEvent`, not the plan ledger.** Moment 4 §7: responses *"are not
the plan changing; they are the plan being answered."* That covers `p/ack`, `c/ack`, and
`join/claim`'s response half — but `join/claim` adding a person post-send **does** hit the ledger
via T1 if it creates an assignment.

Every `recordChange()` call goes inside the route's existing `prisma.$transaction`, per
`gather-architecture-contract` §7. Routes with no transaction today and multi-table writes get one —
notably the household routes (`reconcileHouseholdMembers` still runs non-transactionally, flagged in
the discovery report §5).

### 3. The material-change hook (the F1 handoff)

`PATCH /api/events/[id]` (`route.ts:97,98,111`) is the only route writing `startDate`, `endDate`, or
`venue*`. It currently has **no status gating and no audit logging at all** — this is new server
behaviour, not migration.

Add: T5 detection → `recordChange()` with `MATERIAL_CHANGE`, plus a **no-op hook**
`onMaterialChange(eventId, changeSetId, changedFields)` for [[GTC-183]] (F1) to implement.

**The gap window is real and must be documented, not hidden** (plan §7.3): between this ticket and
F1, a post-send date change is recorded but no one is re-asked. F1 is therefore a **correctness**
dependency of Epic A, not a feature dependency. A3c ships the one plain sentence that states this to
the host — *"Your guests won't be re-asked automatically yet."* A fact, not a challenge, not a block.

### 4. `PlanRevision` write sites

Demoted to coarse checkpoints. Written at: DRAFT→CONFIRMING (exists), **the press** (new — the
as-sent plan, load-bearing for *"what did I send?"*), and every AI regenerate (exists). Not written
per change — measured at 156.6 KB for the demo event vs 549 B for a ledger entry, a 285× ratio
([[GTC-168]]).

### 5. Move the five anchor-copy sites

`Person.inviteAnchorAt` → `PersonEvent.sentAt` at: `people/route.ts:130,133–137`,
`people/batch-import/route.ts:122,125–129`, `households/route.ts:144,147–150`,
`households/[householdId]/route.ts:160`, `lib/households/reconcileMembers.ts:139–142,170,173–176`.
This fixes the per-person-for-a-per-event-fact leak (plan §8.2) for the anchor. `nudge24hSentAt` /
`nudge48hSentAt` keep the same bug and stay put — [[GTC-178]] (E1), zone-7-adjacent.

### ⚠ Doctrine: the no-undo refusal is INTERIM (Nigel, 2026-08-03)

[[GTC-169]] (A3a) made `restoreFromRevision()` throw on a sent event. **That refusal is the interim
contract, not the end state, and it must not silently harden into doctrine.**

> "Post-send restore is the same species as post-send regeneration (ruled: allow with checkpoint +
> ledger) — a bulk change that must carry a why, not a thing to forbid. When this ticket's wiring
> makes restore recordable, it converts from refused to allowed-as-recorded-changeset, and the suite
> assertion flips with it."

Restore was refused in A3a for one reason only: A3a removed the gate before A3b built the recording,
and an *unrecorded* bulk rewrite of a sent plan is worse than either state. The refusal is scaffolding
around the designed A3a→A3b window, not a product decision.

The conversion is therefore this ticket's, and it uses the same bulk-changeSet mechanism as
regenerate: one `PlanRevision` checkpoint, one `changeSetId`, one reason, one entry per resulting
change. The UI consequence line is [[GTC-197]] (A3c)'s, alongside the regenerate warning, and must
merge with it.

A matching comment sits at the guard itself in `src/lib/workflow.ts`, ending: *"If you are reading
this comment after A3b has landed, the conversion was missed."*

### Do not touch

- All Do-Not-Touch Zones. Zone 6 was approved for A3a only; this ticket adds ledger assertions to the
  suite but must not weaken A3a's.
- All UI — [[GTC-197]] / [[GTC-198]], except that A3c's regenerate warning must land in the same merge.
- Building F1's re-ask, D3's release notification, or E1's cadence — hooks only, no implementations.
- Epic B–K territory.

### Acceptance

- [ ] `frozen-edit/route.ts` deleted; all three actions reachable through ordinary routes.
- [ ] `toggle_critical` demands **no** reason (verify — this reverses current behaviour).
- [ ] Every route in the list writes ledger entries inside its transaction.
- [ ] Guest response routes write `InviteEvent`, **not** the plan ledger.
- [ ] Fixture run of 20 mutations spanning all T1–T5 triggers and every enumerated non-trigger:
      reason prompted on exactly the T-subset; one ledger entry per changed field; `changeSetId`
      count equals request count.
- [ ] A touching-someone change sent without a reason returns 2xx and records
      `reasonRequired: true, reason: null`. **No route 4xx's for a missing reason.**
- [ ] Regenerate post-send emits one bulk changeSet + one `PlanRevision`, and does not 4xx.
- [ ] `onMaterialChange` fires on date/venue change post-send; is a no-op; is documented as F1's.
- [ ] Ledger size measured on the fixture run: **< 700 B p95** for T1/T4, **< 2 KB** for whole-row
      deletes ([[GTC-168]]'s revised targets).
- [ ] `npx tsc --noEmit` clean, `npx next build` succeeds, `npm run format:check` clean,
      `npm run test:security` exits 0.

### Stop Conditions

Standard eight, plus:

9. [[GTC-169]] (A3a) is not closed.
10. A route cannot be made transactional without touching a Do-Not-Touch zone.
11. Wiring a route requires a behaviour change beyond recording — that is scope creep; stop.
12. The household routes' non-transactional reconcile path resists a transaction — flag rather than
    forcing it (adjacent to a known open weak point, not this ticket's to fix).

### Closed ticket format (for the executor)

```
# GTC-196 — A3b: Ledger wiring — CLOSED
Status: Complete | Branch: experiment/send-lock-ledger | Commit: [hash] | Effort: xhigh
## Routes wired (with the triggers each can fire)
## frozen-edit redistribution (action → destination → reason rule)
## Fixture run: why-scope precision + ledger completeness + measured sizes
## Hooks left for F1 / D3 (and what they no-op to today)
## Assertions met
## Evidence
```
