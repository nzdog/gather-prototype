---
ticket: GTC-200
title: "A-merge — Land Epic A on feat/moment-one-redesign: cherry-pick, tag-strip, dead-code removal"
status: closed
branch: feat/moment-one-redesign
commit:
moment: 4
type: architecture-build
depends_on:
  - "[[GTC-198]]"
  - "[[GTC-201]]"
  - "[[GTC-202]]"
blocks:
  - "[[GTC-199]]"
tags: [moment-4, hinge, epic-a, state-machine, merge, experimental]
---

# GTC-200 — A-merge: land Epic A — CLOSED

**Status:** Complete | **Branch:** `feat/moment-one-redesign` (target) ← `experiment/send-lock-ledger` (source) | **Commit:** [pending] | **Effort:** medium

**Plan of record:** [`docs/04_roadmap/send-lock-reconciliation-plan.md`](../04_roadmap/send-lock-reconciliation-plan.md)
§10.1 (GTC-167, commit `76943e4`). Filed 2026-08-03.

---

### Context

Epic A runs on an experiment branch and **merges as one unit** — [[GTC-168]] (A2) through
[[GTC-198]] (A3d). This ticket is that merge, in the GTC-145/146 shape
(`gather-experiment-methodology`).

Merging as one unit is not ceremony. A3a removes the lifecycle gates and A3b adds the ledger writes;
between those two commits, post-send mutations are allowed but unrecorded. That window must never
exist on `feat/moment-one-redesign`, which is why nothing lands piecemeal (plan §4.3).

### Gate — A3d's residue check must pass first

**This ticket does not start until [[GTC-198]] closes with a zero-match residue grep.** That grep is
the epic's completeness proof:

```bash
grep -rn "FROZEN" src --include="*.ts" --include="*.tsx" | grep -v lifecycle.ts
# expect: no output
```

A non-zero result means a consumer was missed and the epic is not finished. **Do not merge around
it** — send it back to whichever of A3a–d owns the site.

**[[GTC-201]] (A3b-2) is a hard gate too.** [[GTC-196]] wired every why-scope trigger and deferred
the bulk and non-trigger paths, so between A3b and A3b-2 the ledger is complete for changes that
touch someone and incomplete for some that touch nobody. Merging over that gap would ship a history
with holes in it. A3b-2's completeness sweep — *every mutation route produces at least one entry* —
is the check this merge inherits.

**[[GTC-202]] (A3c-2) is a hard gate too, added 2026-08-04 by this ticket's own pre-merge review.**
That review found two acceptance boxes ticked ahead of their evidence: [[GTC-197]]'s reason-prompt
component was never built (so no screen could write the ledger's `reason`, and every
touching-someone change landed `reason: null`), and [[GTC-168]]'s §9.3 backfill was never executed
(`PersonEvent.sentAt` measured 0/N on every sent event). Ruled 2026-08-04: **both are built before
the first pick.** Merging over them would have shipped a ledger that records gaps and a data-claim
that is false.

Also required before starting:

- [ ] [[GTC-168]], [[GTC-169]], [[GTC-196]], [[GTC-201]], [[GTC-197]], [[GTC-198]], [[GTC-202]] all closed.
- [ ] `grep -rn "requireNotFrozen\|canFreeze(\|isEditBlockedByFreeze" src` → zero matches.
- [ ] Each ticket's measured numbers collected for §10.2's table (below).
- [ ] The experiment branch's full gate suite green at its tip.

---

## Scope

### 1. Cherry-pick the series, tags stripped

Cherry-pick **A2 → A3a → A3b → A3c → A3d → A3b-2 → A3c-2** onto `feat/moment-one-redesign` in order,
**stripping `[EXPERIMENTAL]` from every commit message** — GTC-146 did exactly this
(`a27f781` → `e250f64`). Preserve the per-ticket commit boundaries; do not squash. The boundaries are
the reviewable unit and the revert unit.

**Order corrected 2026-08-04 — chronological, not the A3b-2-before-A3c order this ticket was filed
with.** The filed order cannot be executed: [[GTC-201]]'s `a4bbfd3` adds
`isSent={isSentJson(event)}` to the god file, and `isSentJson` is imported by [[GTC-197]]'s
`5c5d6c8`. Picking A3b-2 first applies cleanly, then fails `tsc` at that commit — which this
ticket's own "re-run the gate suite after every conflicted pick" rule makes unacceptable. The
chronological order is the order the work was actually built and verified in, it is conflict-free,
every intermediate compiles, and A3b and A3c still land in one push.

Rebase conflicts are expected — `feat/moment-one-redesign` moves while the experiment branch is
open, and the god file is a likely collision site. Resolve in favour of the experiment branch's
lifecycle semantics, and re-run the gate suite after every conflicted pick, not just at the end.

### 2. Dead-code removal — **DROPPED 2026-08-04: confirmed a no-op**

GTC-146's rule, verbatim: *"removing dead code is not the same change as introducing the new
architecture."* This ticket was filed expecting four files to survive the epic and need a separate
deletion commit:

| File | Superseded by | Actually deleted in |
|---|---|---|
| `src/app/api/events/[id]/frozen-edit/route.ts` | Ordinary routes + `recordChange()` | **[[GTC-196]]** (`d0c484d`) — its own scope item 1 |
| `src/components/plan/UnfreezeSection.tsx` | Nothing — the unfreeze path dies with FROZEN (Hinge §2) | **[[GTC-197]]** (`5c5d6c8`) |
| `src/components/plan/FrozenEditModal.tsx` | The general reason-prompt component ([[GTC-202]]) | **[[GTC-198]]** (`df5cb41`), with its only caller |
| `src/components/plan/ReadyToFreezeIndicator.tsx` | Nothing — a readiness judgement, refused by Moment 4 §2 | **[[GTC-197]]** (`5c5d6c8`) |

**All four were deleted inside the picked commits**, each in its owning ticket's scope table, so each
deletion is ruled where it happened. The separate commit would therefore be empty, and an empty
commit is not a boundary. Dropped.

The one piece of genuinely-dead code the review found *outside* that list — `TransitionModal`'s
unreachable freeze-warnings view, which A3c's scope said "deleted" and which was only disabled —
goes to **[[GTC-202]]**, where it is in scope, rather than into a merge-time deletion this ticket
forbids itself from making ("any behaviour change… belongs to the ticket that owns the site").

### 3. Carry the measured results

The merge commit body carries plan §10.2's table, filled in. This is what "measured" meant for a
correctness change, and the merge is where it gets reported:

| Metric | Target |
|---|---|
| Site coverage | 145 accounted for, 0 silently skipped |
| FROZEN residue | 0 outside `lifecycle.ts` |
| Guard residue (`requireNotFrozen`, `canFreeze`) | 0 |
| Hard-blocks from lifecycle state, post-send | 0 |
| Why-scope precision (20-mutation fixture) | exactly the T1–T5 subset |
| Ledger completeness | 1 entry per changed field; changeSets = requests |
| Ledger size | < 700 B p95 (T1/T4), < 2 KB (whole-row delete) |
| Nudge predicate | sent+live yields candidates; complete yields none |
| Gate suite | tsc / format / build clean, security 16/16 |
| Browser walks | host dashboard (V1+V2) + 4 token surfaces |

### 4. Documentation

- `BUILD_STATUS.md` updated for the send-lock model.
- `gather-architecture-contract` §5 rewrite is **[[GTC-199]]'s (A4)**, not this ticket's — §5 stays
  accurate until the enum actually drops. Add a forward-pointing note only.
- Close the plan doc's §11 table: A-merge → landed.

### Do not touch

- All Do-Not-Touch Zones.
- Any behaviour change. This ticket moves commits and deletes proven-dead files. If a fix is needed
  mid-merge, it belongs to the ticket that owns the site — stop and send it back.
- [[GTC-199]] (A4) territory: the enum values and freeze columns stay until Epic J closes.

### Acceptance

- [ ] All seven source tickets closed; residue grep zero **before** the first pick.
- [ ] Series cherry-picked in chronological order, `[EXPERIMENTAL]` stripped, boundaries preserved.
- [ ] Dead-code removal confirmed a no-op and dropped, with the four files' real deletion points recorded.
- [ ] Full gate suite green on `feat/moment-one-redesign` after the merge:
      `npx tsc --noEmit`, `npm run format:check`, `npx next build`, `npm run test:security` 16/16
      (with A3a's replacement assertions, which must still total 16 or more — never fewer).
- [ ] Browser re-walk on the merged branch: host dashboard + the four token surfaces.
- [ ] §10.2's table filled in and carried in the merge commit body.
- [ ] `experiment/send-lock-ledger` left intact, not deleted — it is the record of how this was built.

### Stop Conditions

Standard eight, plus:

9. Any of [[GTC-168]], [[GTC-169]], [[GTC-196]], [[GTC-201]], [[GTC-197]], [[GTC-198]] is not closed.
10. **A3d's residue grep is non-zero** — do not merge around a missed site.
11. A cherry-pick conflict cannot be resolved without a behaviour decision — that decision belongs to
    the owning ticket; stop and send it back.
12. The security suite drops below 16 passing tests — the zone-6 approval was to *replace at equal
    strength*, never to reduce coverage.

### Closed ticket format (for the executor)

```
# GTC-200 — A-merge: land Epic A — CLOSED
Status: Complete | Branch: feat/moment-one-redesign | Commit: [hash] | Effort: medium
## Commits picked (source hash → target hash, per ticket)
## Conflicts resolved (file, and which side won, and why)
## Dead code removed (separate commit hash)
## Measured results (plan §10.2 table, filled)
## Browser re-walk on the merged branch
## Assertions met
## Evidence
```

---

## Evidence — CLOSED 2026-08-04

**Epic A is on `feat/moment-one-redesign`.** 20 commits, 7 tickets, zero conflicts, `[EXPERIMENTAL]`
stripped from all 11 tags. `experiment/send-lock-ledger` is left intact — it is the record of how
this was built.

### The review that preceded the first pick

This ticket was executed by a **fresh session**, which is what the experiment-branch pattern is for.
It re-ran every gate cold on a reseeded database, read the whole 87-file diff against the A1 plan
section by section, verified [[GTC-201]]'s T2 catch by reading the route rather than trusting the
report, and walked the two surfaces [[GTC-198]] left unwalked (join/claim on a sent event; a
mutation surface on a past-dated event — both pass; the past-event unassign recorded
`DELETE_ASSIGNMENT reasonRequired=true`, which is plan §5.4 rule 3 working).

**It found two acceptance boxes ticked ahead of their evidence, and stopped before the first pick.**
Both were invisible from inside the sessions that wrote them, because both tickets' *prose* was
accurate about what had been done — it was the checkbox that ran ahead. Ruled 2026-08-04: build both
before merging. That is [[GTC-202]], and it is now the last commit in the series.

| Found | Consequence had it merged |
|---|---|
| [[GTC-197]]'s reason-prompt component was never built | Every post-send touching-someone change lands `reason: null` — trunk's ledger would be a record of gaps, not Kate's memory |
| [[GTC-168]]'s §9.3 backfill was never run (`PersonEvent.sentAt` 0/N on every sent event) | A ledger epic whose own data-claims are false, with `lifecycle.ts` asserting a backfill that had not happened |

Three smaller divergences were recorded rather than fixed: the anchor "move" is a dual-write
([[GTC-196]], [[GTC-178]] named as mover), `removePerson()` had zero callers (deleted), and the
dead-code commit this ticket planned is a no-op (dropped — see Scope §2).

### Commits picked (source → target, per ticket)

| Ticket | Source (experiment) | Target (trunk) |
|---|---|---|
| **A2** [[GTC-168]] | `0ae5cd4` · `041701b` · `aee6ed8` · `ad93a9e` | `55c10b5` · `9187e18` · `d290326` · `ae23665` |
| **A3a** [[GTC-169]] | `8d799a4` · `794e0ff` · `a904c00` · `20879e9` | `3db96ce` · `30333e6` · `dfc6758` · `626d503` |
| **A3b** [[GTC-196]] | `f679665` · `d0c484d` · `6cb430e` | `291b532` · `902093f` · `8aa65b7` |
| **A3c** [[GTC-197]] | `5c5d6c8` · `e4c8033` | `e5954e2` · `116488b` |
| **A3d** [[GTC-198]] | `df5cb41` · `4659047` | `963b3d2` · `063dacc` |
| **A3b-2** [[GTC-201]] | `e6783f2` · `3f53b6b` · `a4bbfd3` · `1efcfe7` | `5207b19` · `1e10529` · `3f71d3e` · `a6e0768` |
| **A3c-2** [[GTC-202]] | `856655f` | `4d9665f` |

**Not picked:** `93a88ae` (this ticket's own filing commit) — trunk already carries it as `bf38ae6`,
the same change cherry-picked the other way. Verified identical before skipping.

The per-ticket closed tickets still cite their **experiment-branch** hashes. That is deliberate: this
table is the map, and rewriting seven closed tickets to renumber history would destroy the link back
to the branch that is being kept as the record.

### Conflicts resolved

**None.** Nineteen picks, zero conflicts, and the proof is stronger than the absence of markers:

```
git diff experiment/send-lock-ledger HEAD    →    (empty)
```

The merged trunk tree is **byte-identical** to the experiment tip, docs included. Nothing was
resolved, dropped, or silently reconciled.

That outcome depended on the order correction in Scope §1. In the filed order (A3b-2 before A3c),
`a4bbfd3` applies cleanly and then fails `tsc`, because it uses an `isSentJson` import that `5c5d6c8`
introduces — an intermediate commit that does not compile, which this ticket's own
gate-after-every-pick rule forbids.

### Gate suite — ON TRUNK, after the final pick

Reseeded first (demo event rebuilt at 43 people / 56 items / 0 audit entries; security fixtures
regenerated).

| Gate | Result |
|---|---|
| `npm run test:security` | **51 / 51**, 0 failed |
| `npm run test:lifecycle` | 28 / 28 |
| `npm run test:ledger-why-scope` | 102 / 102 |
| `npm run test:ledger-record-change` | 30 / 30 |
| `npm run test:edit-item-frozen-block` | 9 / 9 |
| `npx tsc --noEmit` | clean |
| `npm run format:check` | clean |
| `npx next build` | Compiled successfully |
| `npx prisma migrate status` | 31 migrations, schema up to date |

The suite was **also run after every ticket's picks**, not only at the end, and each intermediate
total matches what that ticket recorded on the experiment branch — which is the real check that the
picks landed the same code in the same order:

| After | Security suite | Matches |
|---|---|---|
| A2 | 16 / 16 | [[GTC-168]]: *"unperturbed at 16/16"* |
| A3a | 38 / 38 | [[GTC-169]]: *"38/38"* |
| A3b + A3c | 45 / 45 | [[GTC-196]]: *"45/45"* |
| A3d + A3b-2 + A3c-2 | 51 / 51 | 47 at [[GTC-201]], +4 from [[GTC-202]]'s hardenings |

### Measured results — plan §10.2, filled

| Metric | Target | Measured on trunk |
|---|---|---|
| Site coverage | 145 accounted, 0 silently skipped | **145 accounted.** Every deferral is named in its owning ticket with reasoning |
| FROZEN residue | 0 outside the compat shim | **0 behavioural.** 6 references survive: 4 type-union members, 2 legacy enum keys (`STATUS_LABELS.FROZEN: 'SENT'`, `canTransition`'s `FROZEN: []`). Suite 9 enforces the distinction — and, since [[GTC-202]], is itself tested against known-bad lines |
| Guard residue (`requireNotFrozen`, `canFreeze`, `canMutate`, `isEditBlockedByFreeze`) | 0 | **0 live.** 4 tombstone comments explaining why each is gone |
| Hard-blocks from lifecycle state, post-send | 0 | **0** — Suites 5 and 6 drive real handlers across all four phases and assert HTTP status |
| Why-scope precision (20-mutation fixture) | exactly the T1–T5 subset | **9 of 20**, all five triggers exercised; 102/102 on the full truth table |
| Ledger completeness | 1 entry per changed field; changeSets = requests | **Every mutation route** produces at least one entry, except the five guest-side routes that write `InviteEvent` by design |
| Ledger size | < 700 B p95 (T1/T4), < 2 KB (whole-row delete) | 455 B (T4) · 549 B (T1) · 1,601 B (whole-row) — [[GTC-168]]'s measurement, unchanged by the merge |
| Nudge predicate | sent+live yields candidates; complete yields none | **Both directions asserted** — the positive half was added by [[GTC-202]]; before it, no fixture person on a live sent event had a phone or an anchor, so the passing half was passing for the wrong reason |
| Gate suite | tsc / format / build clean, security ≥ 16 | clean / clean / clean, **51** |
| Browser walks | host dashboard (V1+V2) + 4 token surfaces | **All five re-walked on trunk** — see below |

### Browser re-walk on the merged branch

| Surface | Observed on trunk |
|---|---|
| `/plan/[eventId]` host dashboard | Stepper reads **DRAFT → CONFIRMING → Send → PAST**; the 🧊 freeze step is gone and its copy now says *"you can send the plan to your guests"*. Plan Status: *"0 conflicts · 14 unassigned"* — a fact, no readiness score, no percentage, no progress bar. **No freeze, unfreeze, or mark-complete affordance anywhere.** |
| `/h/[token]` | Header, teams, responses, *"I've sent the invites"*. The whole bottom action bar — Freeze Event, its `Cannot freeze: N critical gaps` verdict, unfreeze, the COMPLETE confirm — absent |
| `/c/[token]` | Fully editable: Add Item live, no lock banner. **The reason prompt fires on a post-send T1** — Unassign on an ACCEPTED claim raised *"Keep a note?"*; saving recorded `reason: "Pete couldn't do it"`, Skip recorded `reasonRequired: true, reason: NULL`, and the identical click on the DRAFT fixture raised **no prompt at all** |
| `/p/[token]` | Response affordances live, no *"This plan has been finalised"* banner |
| `/join/[token]` | Name selection renders on a **sent** event; claiming completes and lands on the participant view |

**Walk artefacts cleaned up, unlike last time.** [[GTC-198]] left an `EventRole` on the demo event for
the next session to find; this walk's temporary host session, its `EventRole`, and the shared-link
token enabled on the SENT fixture were all torn down, and the database was reseeded afterwards and
verified: demo event at 43 people / 56 items / **0 audit entries**, zero `EventRole` rows on it.

### Assertions met

- [x] All seven source tickets closed; residue grep zero **before** the first pick.
- [x] Series cherry-picked in chronological order, `[EXPERIMENTAL]` stripped from all 11, boundaries preserved, nothing squashed.
- [x] Dead-code removal confirmed a no-op and dropped, with the four files' real deletion points recorded (Scope §2).
- [x] Full gate suite green on `feat/moment-one-redesign`; security **51**, never fewer than 16.
- [x] Browser re-walk on the merged branch: host dashboard + the four token surfaces.
- [x] §10.2's table filled in and carried here.
- [x] `experiment/send-lock-ledger` left intact, not deleted.

### What Epic A hands on

- **[[GTC-199]] (A4)** is unblocked: the data migration and the enum drop. It is the only
  irreversible ticket in the epic, and it also owns the physical `inviteSendConfirmedAt` → `sentAt`
  rename that [[GTC-168]]'s `@map` deferred.
- **[[GTC-183]] (F1)** is a **correctness** dependency, not a feature one: until it lands, a post-send
  date change is recorded but nobody is re-asked. The host is told so in the event-edit modal.
- **[[GTC-178]] (E1)** owns the `Person.inviteAnchorAt` → `PersonEvent.sentAt` read switch, which is
  what finally closes plan §8.2's per-person leak.
- `gather-architecture-contract` §5 still describes the FROM state. Rewriting it is [[GTC-199]]'s.
