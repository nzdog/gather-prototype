---
ticket: GTC-190
title: "I3 — The minutes-after screen"
status: open
branch: TBD
commit:
moment: 4
type: build
depends_on:
  - "[[GTC-182]]"
tags: [hinge, epic-i, minutes-after]
---

# GTC-190 — I3: The minutes-after screen

**Status:** Open | **Branch:** TBD | **Commit:** [pending] (filed `e5f6e82`) | **Effort:** high

*Effort level is a proposal from the discovery-report ticket breakdown — run `/effort` manually
before execution.*

### Context

Hinge spec §6: on the press landing, no delivery board, no per-person status — the assistant
asserts, one voice: *"All fourteen are away. I'll tell you the moment anything comes back."* The
screen in the gap: the amber grid (system's-on-it colour, opened at a lighter tint of amber that
deepens as waiting becomes real — same axis and the same single state as Moment 4's amber; a
volume knob on the shade, not a second colour and not a second state), under the assistant's
line.

*Quote updated 2026-08-30: the spec's "yellow … same axis as Moment 4's orange" was renamed to "amber" by founder ruling ([[GTC-192]] Ruling 13). The quote above tracks the corrected spec text; the substance is unchanged.* E5's (GTC-182)
nudge-clock shows under each box. The full four-sentence script: *"All fourteen are away. Nothing
for you to do. I'll tell you the moment anything comes back — and I'll be in touch if something
needs you."*

### Scope

**Fix here:**
- The immediate post-press screen state: the assertive one-voice message, the amber grid
  rendering (using J1's, GTC-192, person-primary grid shape once it exists — if sequenced before
  J1, use a placeholder grid shape and flag the gap rather than inventing a divergent one), E5's
  nudge-clock per box.

**Do not touch:**
- All Do-Not-Touch Zones in `GATHER-BUILD-CONSTANTS.md`.
- Bounce handling display (E4's territory, GTC-181) — this screen must render bounces once E4
  lands; coordinate rather than build a second bounce UI.

### Dependencies

E5 (GTC-182). Should coordinate with J1 (GTC-192) on grid shape even though not strictly blocked
by it.

### Acceptance

- [ ] The four-sentence script renders verbatim (or to the same effect) immediately after the
      press.
- [ ] The grid shows every box in the amber system's-on-it colour — at the opening lighter tint
      per Hinge §7 — with no per-person status
      board.
- [ ] Each box shows E5's nudge-clock.
- [ ] `npx tsc --noEmit` clean, build passes.

### Stop Conditions

Standard eight from `BUG-TICKET-TEMPLATE.md`.

### Closed ticket format (for the executor)

```
# GTC-190 — I3: The minutes-after screen — CLOSED
Status: Complete | Branch: [...] | Commit: [hash] | Effort: high
## What was built
## Test results
## Assertions met
## Evidence
```
