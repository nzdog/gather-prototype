---
ticket: GTC-242
title: 'Update gather-v1-v2-reconciliation-campaign SKILL.md for /demo/review deletion'
status: open
branch: feat/moment-one-redesign
commit:
moment: n/a
type: chore
severity: low
depends_on:
  - '[[GTC-240]]'
blocks: []
related:
  - '[[GTC-240]]'
  - '[[GTC-149]]'
  - '[[GTC-236]]'
  - '[[GTC-239]]'
tags: [docs, skill-file, v1-v2-reconciliation, dead-path-removal]
---

# GTC-242 — Update `gather-v1-v2-reconciliation-campaign` SKILL.md for `/demo/review` deletion

**Severity:** Low (doc accuracy — this skill file is loaded into future sessions; a stale
recon check or a "pending" step that's already done sends a future executor down a wrong
path, but nothing user-facing breaks)

---

## Citations (binding — GTC-222)

Per BUG-TICKET-TEMPLATE.md: cite the symbol, not the line, in every citation this ticket adds
and in every code/doc comment it produces. Line numbers below are cited as of GTC-240's commit
and will drift; re-verify against HEAD before executing.

---

### Signal

**Expected:** `.claude/skills/gather-v1-v2-reconciliation-campaign/SKILL.md` accurately
reflects that `/demo/review` no longer exists in the tree, following its removal in
[[GTC-240]].

**Actual:** The skill file, as of GTC-240's commit, still contains three references to
`src/app/demo/review/page.tsx`, none edited by GTC-240 (explicitly out of that ticket's scope
— "Do not edit it," per Nigel's direction 2026-08-23):

1. **Line 27** (Definitions table, "Tier 2 prune" row) — narrates past state: lists
   `src/app/demo/review/page.tsx` as one of the V1 surfaces GTC-152 *deliberately did not
   delete* as of 2026-07-09. Descriptive of history, not an instruction to run or a live claim
   about the current tree. **Likely fine as-is — confirm during execution, don't assume.**

2. **Lines 91-92** (Phase 0 recon script) — **load-bearing.** A literal command a future
   campaign session is told to run:
   ```
   grep -rn "/generate" src/app/demo/review/page.tsx
   # EXPECTED: one caller ~line 62 (deliberately unguarded; dies with Tier 2 prune per GTC-149)
   ```
   Post-GTC-240 this grep fails (file doesn't exist) — the `EXPECTED` comment is now false.
   Running the skill's Phase 0 recon block verbatim will surface an unexplained discrepancy.

3. **Line 339** (Phase 4 — V1 retirement decision gate, "delete in dependency order") — lists
   `/demo/review` page as **step 1** of a planned future deletion sequence that Phase 4 gates
   behind three unmet conditions: V2-covers-flows browser-walk evidence, a SQL check for zero
   live V1-only events, and founder sign-off recorded in the ticket. That step is now already
   done — executed via a separate, narrower founder decision recorded in GTC-240's own Phase 0,
   **outside** the formal Phase 4 gate.

**Smallest failing case:** a future session runs the Phase 0 recon block from this skill
verbatim; the line 91 grep against `src/app/demo/review/page.tsx` returns nothing, silently
contradicting its own `EXPECTED` comment.

---

### Open question for the founder (not answered here)

GTC-240 executed what SKILL.md's Phase 4 lists as step 1 of V1 retirement (`/demo/review`
deletion) via a standalone founder decision recorded in its own Phase 0 — not through the
Phase 4 gate (no browser-walk evidence that V2 covers the flows, no SQL check for live
V1-only events, no founder sign-off framed as a Phase 4 gate-pass).

**Does the Phase 4 gate still hold for the remaining steps** (`page.tsx` V1 fetch
handlers/dashboard-only UI → `/api/events/[id]/generate`, `/regenerate`,
`/regenerate/preview` → `src/lib/ai/generate.ts` pipeline remnants → legacy prompt surface →
`GuidedPlanBuilder.tsx`/`HostDescriptionModal.tsx`) — **or has GTC-240 established a
precedent that individual Tier 2 surfaces can be peeled off by standalone founder decision
without the full three-part gate?**

This ticket does not answer that. It is the founder's call, and the exact wording of the
line 339 fix (below) depends on the answer — whether it reads "step 1 done, gate still
applies to steps 2+" or whether the gate framing itself needs revisiting.

---

### Reproduce

Not applicable in the bug-reproduction sense — this is a documentation-accuracy ticket.
Verifiable by reading the three cited lines against the current tree (post-GTC-240,
`src/app/demo/review/` no longer exists) and running the line 91 grep to confirm it now
returns nothing.

---

### Scope

**Fix here:**
- `.claude/skills/gather-v1-v2-reconciliation-campaign/SKILL.md` — lines 27, 91-92, 339 only
  (line numbers as of GTC-240's commit; re-verify at HEAD). Exact wording for line 339
  depends on the founder's answer to the open question above.

**Do not touch:**
- All Do-Not-Touch Zones in `GATHER-BUILD-CONSTANTS.md`
- Any other skill file
- Closed ticket files (historical audit trail — not living docs)
- Anything unrelated to these three references

---

### Acceptance

**Test gate:** Severity Low — post-fix confirmation acceptable, no RED/GREEN test required
(doc-only change, no code path affected).

**Assertions:**
- [ ] Line 27 reviewed and confirmed accurate as historical narration (or corrected if not)
- [ ] Lines 91-92 recon command removed, replaced with a tree-accurate check, or annotated as
      historical — per the founder's answer to the open question
- [ ] Line 339 deletion-sequence updated to reflect step 1 already complete — per the
      founder's answer
- [ ] Founder's answer to the open question recorded in this ticket's Evidence section
- [ ] Grep confirms no other stale `/demo/review` references introduced or left inconsistent

**Evidence package**
- Founder's answer to the open question, quoted
- Diff of the three line edits
- Commit hash

---

### Unknowns

- Whether the Phase 4 gate (browser-walk evidence, SQL live-event check, founder sign-off)
  still applies to the remaining Tier 2 surfaces, or whether GTC-240 sets a precedent for
  peeling off individual surfaces by standalone decision. **Blocks the exact wording of the
  line 339 fix — do not draft past this point until the founder answers.**

---

### Stop Conditions

Stop and report (do not proceed) if:
1. `GATHER-BUILD-CONSTANTS.md` missing
2. Preflight fails
3. The founder's answer to the open question is not yet recorded
4. Fix requires touching Do-Not-Touch zones or expanding scope beyond the three cited lines

---

### Executor Checklist

- [ ] `GATHER-BUILD-CONSTANTS.md` read
- [ ] Citations section read; every citation in this ticket is a symbol, not a bare line number
- [ ] Preflight passed and results pasted
- [ ] Founder's answer to the open question obtained and recorded
- [ ] Fix implemented within declared scope
- [ ] No Do-Not-Touch zones modified
- [ ] Acceptance assertions checked
- [ ] Evidence package assembled
- [ ] Commit hash recorded
- [ ] Wait for Nigel's explicit approval before committing

---

## Evidence (Executor-Completed)

> Filled in when this ticket is executed. Not yet executed.

**Status:** Open — filed as a follow-up from [[GTC-240]] Phase 2, per Nigel's direction,
2026-08-23. Blocked on the founder's answer to the open question above.
