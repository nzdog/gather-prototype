---
ticket: GTC-319
title: 'The event date range renders as "13 Sept-Sept" — the end day is dropped, on the guest page, the host page and the family directory'
status: open
branch: TBD
commit:
moment: 4
type: bug
severity:
severity_proposed: medium
depends_on: []
blocks: []
related:
  - '[[GTC-191]]'
tags: [guest-side, display, i18n, date, guest-facing]
---

# GTC-319 — The date range drops the end day

**Status:** Open — filed 2026-09-18, not started | **Branch:** TBD
**Severity:** [ ] Critical [ ] High [x] Medium [ ] Low — proposed at filing; the founder's to
set. `severity_proposed` carries the proposal and `severity` stays empty until it is set.
Escalated (brand/trust risk): [ ] Yes [x] No — it is not a credential or a failed send. It is
the kind of thing a guest notices and a host is embarrassed by.

*Filed on founder instruction, 2026-09-18, found while reading the guest page for
[[GTC-191]]'s shape proposal: "File it. Guest-facing, on the page GTC-191 owns, and it is not
(a) or (b), so it does not ride in."*

---

### Signal

**Expected**
A two-day event reads as a range a guest can act on: `13 Sept – 15 Sept`.

**Actual**
It reads `13 Sept-Sept`. The end day is gone and the month is printed twice.

**Root cause — it is a locale assumption, not a typo.** `formatDateRange` builds an
`Intl.DateTimeFormat('en-NZ', { month: 'short', day: 'numeric' })` and then takes
`.split(' ')[1]` of the formatted END date, expecting the day to be the second token. That is
true of `en-US` (`"Sep 13"` → `"13"`). **`en-NZ` formats day-first** (`"13 Sept"`), so the second
token is the MONTH. The start is printed whole and the end contributes its month again.

**Smallest failing case**
Open any participant page for an event whose start and end differ.

**Verified at `87a35ef`**

```
start formatted: "13 Sept"
end formatted:   "15 Sept"
end.split(" ")[1]: "Sept"
RENDERED RANGE:  "13 Sept-Sept"
```

The event used is "GTC-192 glance — mixed", `startDate` 2026-09-13T11:40Z, `endDate`
2026-09-14T17:40Z. Note the end formats as **15** Sept, not 14: the formatter is pinned to
`Pacific/Auckland` and 17:40 UTC on the 14th is 05:40 on the 15th locally. So the correct output
for this event is `13 Sept – 15 Sept`, and whether that is the RIGHT end date is a separate
question — see Unknown 2.

---

### Reproduce

**Required state**
- Auth: a PARTICIPANT token for the guest page; a HOST token for the host page; none at all for
  the directory.
- Event state: any, with `startDate` and `endDate` on different days.
- Multi-actor: [ ] No.

**Steps**
1. Open `/p/<participant token>` for such an event.
2. Read the line under the participant name.

**Observe:** `13 Sept-Sept`.

---

### Where it lives — three copies of the same helper, and a fourth that is correct

- `formatDateRange` in `src/app/p/[token]/page.tsx` — the guest page. **Guest-facing.**
- `formatDateRange` in `src/app/h/[token]/page.tsx` — the host's token page.
- `formatDateRange` in `src/app/gather/[eventId]/directory/page.tsx` — the family directory,
  which is **unauthenticated and public to anyone holding the event id**. Same bug, written out
  over three lines instead of one (`startFormatted`, `endDay`).
- `formatDateRange` in `src/app/plan/events/page.tsx` is a **different** implementation that
  formats both ends and compares them. It does not have this defect and is not in scope except
  as the shape to copy.

⚠ **Three copies is the reason this survived.** Fixing one leaves two, and a reader who checks
one file concludes it is fixed.

---

### Scope

**Fix here:**
- `formatDateRange` in the three files above.

**Do not touch:**
- All Do-Not-Touch Zones in `GATHER-BUILD-CONSTANTS.md` are binding.
- **The `Pacific/Auckland` pin.** It is deliberate and every other formatter in the tree carries
  it. Whether the stored `endDate` is the right instant is Unknown 2, not this fix.
- **`formatEventDay` in `src/lib/messages/ask-register.ts`.** It formats a single date with
  `toLocaleDateString('en-NZ', …)` and does no splitting. It is correct and is the message's,
  not the page's.
- `formatDateRange` in `src/app/plan/events/page.tsx`.

---

### Acceptance

**Test gate** — Medium: regression test preferred.

**Assertions**
- [ ] A two-day event renders both days and one month where the months match, and both months
      where they do not (e.g. `30 Sept – 2 Oct`).
- [ ] A single-day event renders one date, not a range of a date with itself.
- [ ] The three surfaces agree, asserted rather than assumed — one helper or three identical
      ones, and a test that would fail if they diverged again.
- [ ] Adjacent flows unaffected: the composed message's date (`formatEventDay`) is unchanged;
      `npm run test:security` stays green.

---

### Unknowns

1. **One shared helper or three fixed copies?** A shared helper is the obvious answer and it
   crosses three route trees, one of which is unauthenticated. Recorded as a choice.
2. **Is the stored `endDate` the intended instant?** This event's end is 17:40 UTC, which is the
   next calendar day in Auckland. That may be correct (an event running into the evening) or may
   be a capture defect. **Out of scope here** — this ticket makes the renderer honest about
   whatever is stored. Measure before assuming.
3. **Whether the range belongs on the guest page at all.** §3's closed content list does not
   include it, and the §3 sweep follows [[GTC-189]] slice 5. If that sweep cuts the line, this
   fix is still right for the other two surfaces.

---

### Stop Conditions

Standard eight from `BUG-TICKET-TEMPLATE.md`.

---

### Executor Checklist

[ ] GATHER-BUILD-CONSTANTS.md read
[ ] Citations section read; every citation in this ticket is a symbol, not a line number
[ ] Preflight passed and results pasted
[ ] Bug reproduced — Observe confirmed
[ ] Plan documented (hypothesis, change set, test strategy, rollback risk)
[ ] Fix implemented within declared scope
[ ] No Do-Not-Touch zones modified
[ ] GREEN confirmed
[ ] Acceptance assertions checked
[ ] Adjacent flows verified
[ ] Evidence package assembled
[ ] Commit hash recorded
[ ] No stop conditions triggered without resolution

---

## Evidence (Executor-Completed)

> Filled in by the executor before committing.

**Root cause confirmed**

**Files changed**

**Test results**
- RED:
- GREEN:

**Assertions checked**

**Security suite:**

**Commit hash:**
