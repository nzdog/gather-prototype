---
ticket: GTC-302
title: 'A job is stored and read as a dish, so it cannot reach the ruled job sentence — and item names are stored in no consistent form'
status: open
branch: feat/moment-one-redesign
commit:
moment: 4
type: bug
severity: medium
depends_on: []
blocks: []
related:
  - '[[GTC-187]]'
  - '[[GTC-189]]'
  - '[[GTC-171]]'
  - '[[GTC-191]]'
  - '[[GTC-192]]'
  - '[[GTC-303]]'
  - '[[GTC-304]]'
tags: [items, tasks, data, message-register, ask, recipient-model]
---

# GTC-302 — A job is stored and read as a dish, and item names are stored in no consistent form

**Status:** Open — scoped in part 2026-09-13 (*Founder answers — scoping*). Fix 1, the article strip at
capture and the seed fix are built and committed — see *Evidence*. Two defects in that build's kind
change are filed, not fixed: [[GTC-303]] and [[GTC-304]]. The prompt changes, the display helper and
the qualifier move follow [[GTC-189]] slice 3. Filed 2026-09-13 | **Branch:** `feat/moment-one-redesign`
**Severity:** [ ] Critical [ ] High [x] Medium [ ] Low — **confirmed by the founder:** *"GTC-302 —
MEDIUM CONFIRMED."* Proposed at filing as Medium; the frontmatter records the confirmed value.
Escalated (brand/trust risk): [ ] No

**Two defects in one sentence, and the worse one leads.** Filed on the founder's instruction from
[[GTC-189]] *Founder answers — the slice 2 words*, where the ask was ruled a sentence: *"Would you
bring the pavlova?"*. The founder asked for the stored names to be checked before that sentence was
written; the check found the naming problem, and beside it a job being asked for as a dish. On
reading both, in the founder's words:

> ⚠ THE JOBS FINDING IS THE WORSE HALF and should lead GTC-302 rather than sit under the naming
> problem. "Would you bring the Wash the dishes?" is a different defect from "the The pavlova": one
> is untidy data, the other is a sentence written for a dish being used for a chore. The founder
> ruling behind this whole model has children doing jobs, so the common case for a carried ask IS a
> job, and the carried sentence is the one most likely to hit it. Say whether an item knows it is a
> job — a kind, a flag, anything — and if it does not, say so plainly, because that decides whether
> slice 3 can filter or whether something has to be added.

**The job sentence has since been ruled** — [[GTC-189]] *Founder answers — the slice 2 words*,
answer 6 — in the founder's words:

> RULE THE JOB SENTENCE. A job is DONE, not brought — that is the whole ruling and it changes one
> word.
>
> Own job: "Would you do the dishes?"
> Carried job: "Ollie has been asked to do the dishes. Would you also answer on Ollie's behalf?"

> ⚠ THE FIELD EXISTS AND THE DATA DOES NOT. Item.kind is ITEM or TASK, zero TASK rows in a
> thousand, and only plan generation ever sets it. So this sentence is ruled on a case that has
> never occurred, and the first job assigned by hand will be stored as ITEM and read "bring".

> ⚠ AND DO NOT FILTER JOBS OUT. Your own finding: a filtered job reaches nobody, and a child's job
> reaching an adult is exactly what the carried ask exists for. The answer was always a sentence,
> never a filter.

**So this ticket's first half is no longer the missing sentence — composition has it. It is what
keeps the sentence unreachable:** a job typed by a host is stored as a dish. Half 1 below is
rewritten on that footing; the sentence itself is recorded in [[GTC-189]].

**The sharpest part, said plainly.** The plan generator's prompt gives *"Wash the dishes"* as its own
example of a job name, so the first generated job reads **"Would you do the Wash the dishes?"**. In
the founder's words:

> The naming problem and the verb problem are the same defect arriving twice, and neither is fixable
> in the composer — a sentence cannot tidy its own nouns.

So Half 2's *"the The pavlova"* and Half 1's *"do the Wash the dishes"* are one defect — a name
stored in a form the sentence cannot use — met once by a dish and once by a job. The fix is where
names are made, not where sentences are.

Both halves were **found by reading and measuring gather_dev on 2026-09-13, NOT reproduced on a real
event**. Composition is at [[GTC-189]] slice 2.

---

## Corrections — recorded 2026-09-13, before any build

*Found by the scoping investigation and recorded on the founder's instruction, before anything was
built: "Correct GTC-302 and GTC-189 answer 6 rather than leaving them wrong." Corrections 1 and 2
correct the founder's scoping brief as well as this ticket. The wrong sentences below are left
standing, each marked in place and pointing here.*

1. **The host assign route DOES read `kind`.** Half 1 said neither assign route does. `POST` in
   `src/app/api/events/[id]/items/[itemId]/assign/route.ts` calls `mayHoldRow` in
   `src/lib/assignment/same-team.ts`, which lets any row whose kind is not `ITEM` through before the
   same-team rule ([[GTC-171]]). Only the coordinator's `POST` in
   `src/app/api/c/[token]/items/[itemId]/assign/route.ts` does not read it. So a typed job goes wrong
   two different ways, depending on its team:
   - **typed into an ordinary team** it is an `ITEM` like any dish — assignable to that team's
     members, taken by `POST` in `src/app/api/events/[id]/people/auto-assign/route.ts` (which reads
     `ITEM` rows only), and it reads *"bring"*. Silent.
   - **typed under a generated job team** (*Set up*, *Clean up*, *Other jobs*) it is an `ITEM` on a
     team with no members, so `mayHoldRow` applies the same-team rule and refuses every guest with
     `SAME_TEAM_ERROR`. Only the host's own pick passes. Loud, and unassignable. No job team exists
     in gather_dev, so this half is by reading.
2. **No stored name came from the prompt that makes jobs.** All 823 `GENERATED` rows are dated
   2026-03-13 to 2026-05-09. Task rows arrived with [[GTC-171]] at `e85ebfa`, on 2026-08-04: 332 rows
   came from the V1 prompt (`generatedBatchId` `gen_…`) and 491 from the single-call prompt before
   it had a `tasks` output (`m2-finalize-…`). None of the 6 `EventSetup` rows holds set-up,
   clean-up or other-jobs text, so no job could have been generated. **"Wash the dishes" is the
   prompt's example and has never written a row.** *"Would you do the Wash the dishes?"* is what the
   prompt would produce, not anything the data holds.
3. **Two prompt families are live, not one.** This ticket names `buildPlanGenerationPrompt`
   (`finalize-plan`, `regenerate-plan`), and it is the only one that makes jobs. The V1 prompts in
   `src/lib/ai/prompts.ts` — `PLAN_GENERATION_SYSTEM_PROMPT`, `PLAN_REGENERATION_SYSTEM_PROMPT`,
   `SELECTIVE_REGENERATION_SYSTEM_PROMPT` and their builders, called from `src/lib/ai/generate.ts` —
   are still reached from the plan page's `generate` and `regenerate` calls and from
   `RegenerateModal`. V1's own worked examples are where the Title Case and the bracketed qualifiers
   come from: *"Butter (for table) - 500 G"*, *"Whipped Cream (for pavlova) - 600 ML"*.
4. **The counts.** 56 article-led names and 324 carrying a qualifier character: reproduced. 546 in
   Title Case: **not reproduced** — 534 if short words (*and, of, with, for, in, on, the, or, a, to*)
   may stay lower case, 468 if every word must be capitalised. Nothing in this ticket turns on it.

---

## Founder answers — scoping (Nigel)

*The founder's answers to the scoping report, in the founder's words. No date was stated with them;
the machine clock read 2026-09-13 NZST. They instruct the corrections above, answer Unknowns 2, 4,
5, 6, 7 and 8, answer Unknown 3 in part, and order the build.*

#### The corrections, first

> RECORD THE FOUR CORRECTIONS FIRST, including both to my brief: the jobs prompt postdates every
> stored name, so "Wash the dishes" has never written a row; and the host assign route does read
> kind via mayHoldRow. Correct GTC-302 and GTC-189 answer 6 rather than leaving them wrong.

**Recorded** as *Corrections* above, and [[GTC-189]] answer 6 corrected in place.

#### Unknown 2 — a toggle on the add-item form; a coordinator may add a job

> UNKNOWN 2 — HOW A HOST MARKS A JOB. A toggle on the add-item form, worded as plainly as the thing
> itself: something is either brought or done. Propose the two words and show me; do not settle
> them.
> A coordinator MAY add a job. They run a team and a team has work in it — refusing them is an
> arbitrary line, and GTC-262 aside, they are trusted with the team's items already.

**The two words are PROPOSED, not ruled.** They live in one place, under
`ANCHOR(GTC-302): the toggle words — PROPOSED, not ruled`, so ruling them is one edit.

#### Unknown 8 — one name, noun form; the label is the fix

> UNKNOWN 8 — ONE NAME, NOUN FORM. No second field. Your reason carries it: two names drift apart,
> and a schema change to hold a verb the sentence supplies is the wrong trade.
> ⚠ AND YOUR FINDING IS THE REAL ONE: the field label is what stops a host typing "Wash up". "Do the
> ___" as the label, so the sentence they are completing is visible while they type. That is a
> better fix than any code could be, and it belongs in fix 1 rather than fix 2.

**So:** no schema change. The job name field reads *"Do the ___"*, and it is built with fix 1.

#### Unknown 4 — store lower case, capitalise on display

> UNKNOWN 4 — CAPITALISE ON DISPLAY, store lower case. The composer needs the noun form; every
> screen wants a name that looks like a name. One display helper, not eight components each doing
> it.

**Ordered after [[GTC-189]] slice 3.** Not built with fix 1.

#### Unknown 6 — qualifiers move to notes

> UNKNOWN 6 — YES, QUALIFIERS MOVE TO NOTES. "the Glazed Ham (bone-in leg)" is logistics inside an
> ask, and decision 1 already rules that out.
> Note that V1's JSON has no notes field, so say what that costs before it is built.

**Owed before it is built:** what adding `notes` to V1's item JSON costs. Ordered after slice 3.

#### Unknown 7 — fix the seed strings, pulled forward

> UNKNOWN 7 — YES, FIX THE SEED STRINGS. It is the cheapest of the lot, the board ids are fixed so
> the demo URLs survive, and I will be looking at those boards in slice 3. Pull it forward.

⚠ **A correction to the scoping report this answer leaned on: three of the four boards have fixed
ids, not all four.** `scripts/seed-gtc192-replay.ts` (*replay — arrival*, *replay — all green*) and
`scripts/seed-gtc192-phase7.ts` (*phase 7 — the two variants*) rebuild by fixed id.
`scripts/seed-gtc192-glance.ts` (*glance — mixed*, 20 of the 56 rows) deletes by tag and recreates
with generated ids, so re-seeding it gives *glance — mixed* **and** *glance — oversized* new URLs —
and the mixed board's rows were last updated three minutes after it was seeded, so a re-seed also
discards whatever changed them.

#### Unknown 5 — leave the 961

> UNKNOWN 5 — LEAVE THE 961. Your five reasons are sound and the fifth is decisive: rewriting
> through the PATCH marks every row HOST_EDITED, so the next regenerate cannot replace them. A
> tidy-up that freezes the data is worse than untidy data.

**So:** capture tidies a name only when one is submitted. An edit that does not send a name leaves
the stored one exactly as it is.

#### The build — fix 1, the article strip, the seed fix. Nothing else.

> NOW BUILD, and only this: fix 1 — the kind on both add routes, the PATCH, the form label — plus
> the article strip at capture, plus the seed fix. RED first.
>
> ⚠ NOT the prompt changes. Not the display helper. Not the qualifier move. Those follow slice 3,
> as you ordered them.

---

## Founder answers — the build (Nigel)

*The founder's answers to the build report, in the founder's words. No date was stated with them;
the machine clock read 2026-09-13 NZST. The founder numbers the choices as the build report did; the
Evidence list below numbers the same choices one higher, because it lists the toggle words first.*

#### The toggle words — ruled

> BRING | DO — ruled. They are the guest's own verbs and the form says what the guest will read. No
> explanation needed anywhere.

**So:** the anchor that held them as proposed is gone, and `ROW_KIND_WORDS` in
`src/lib/items/row-kind.ts` records the ruling. The toggle's accessible name (*"Brought or done?"*)
and the job field's placeholder (*"dishes"*) remain the executor's.

#### Glance — mixed — left as it is

> GLANCE MIXED — LEAVE IT. Your correction stands: new ids on re-seed means a changed URL and a wiped
> row the seed never wrote, and it is a test board rather than one I demo from. Record why it was
> left, so nobody re-seeds it later to tidy 20 names and loses the camp chairs.

**Recorded** here, in the Evidence, and in the header of `scripts/seed-gtc192-glance.ts` — where
someone about to re-run the script will read it.

#### Choice 5 — a kind change leaves the assignment and the quantity state: wrong. Filed as [[GTC-303]]

> Choice 5 — a kind change leaving an assignment in place. That is wrong and it should not ship
> quietly. A job assigned across teams and then made a dish keeps a holder the same-team rule would
> now refuse, and the row also keeps quantity state NA. Do not fix it here — file it, and say
> plainly in the ticket that the kind change can produce a row no route would have created. An
> inconsistency a route allows but would never make is the kind that surfaces months later as a
> mystery.

#### Choice 4 — a kind change is a change to the ask: wrong. Filed as [[GTC-304]]

> Choice 4 — a kind change not counting as a change to what the guest was asked. Wrong the other
> way, and it matters more. Changing a dish to a job changes the sentence the guest receives, from
> "would you bring" to "would you do". On an answered row that is a material change to an ask
> someone has already said yes to.
> File it against the material-change machinery rather than fixing it here — but say in the ticket
> that this is a change to the ask, not a reclassification, because the composer reads kind to
> choose the verb.

#### The rest as built

> THE REST AS BUILT. Choices 1, 2, 3, 6, 7, 8 and 9 are all fine. Two notes:
> - Choice 9, stripping "a" and "an": "A la carte platter" losing its "A" is a real cost and it is
>   acceptable. Record the example so the trade is visible rather than discovered.
> - Choice 2, dishes keeping "Item Name" while jobs get "Do the ___": right. The lead is there
>   because jobs needed one, not for symmetry.

**The trade, recorded:** `itemNameForStorage` stores *"A la carte platter"* as *"la carte platter"*.
Accepted, not overlooked — the example is in the function's docstring as well.

#### The stale comments — fixed

> THE STALE COMMENTS — fix both. The whatIsAsked comment and the carried-ask test's layer J comment
> both say the add routes set no kind, and that is now false. A comment that was true yesterday is
> exactly the trap you recorded in the TNZ work.

**Fixed:** the docstring of `whatIsAsked` in `src/lib/messages/ask-register.ts`, and layer J of
`tests/carried-ask-composition-test.ts` — its header, and its `[DEFECT GTC-302]` comment, which said
*"The one writer of TASK rows is plan generation"* and was false for the same reason.

> Then commit and push.

---

## Half 1 — a job stored and read as a dish, so the ruled sentence cannot reach it

### Signal

**Expected**
A job reaches its owner, or its owner's carrier, in the ruled sentence: *"Would you do the dishes?"*,
*"Ollie has been asked to do the dishes."* Never filtered out.

**What composition now does** ([[GTC-189]] slice 2). `whatIsAsked` in
`src/lib/messages/ask-register.ts` says *"do"* of a job and *"bring"* of a dish, and one person
holding both is one sentence — *"Would you bring the pavlova and do the dishes?"*. It is handed the
two kinds apart, as `AskRecipient.jobNames` and `CarriedChildAsk.jobNames`. It cannot tell them
apart itself: composition is handed names only ([[GTC-187]] decision 1).

**Actual — three ways a job still reaches a guest wrong, the first this ticket's defect:**

1. **A job a host adds is stored as a dish.** No route that creates a row sets `kind` (see the
   limits below), so a typed job is an `ITEM`, arrives in `itemNames`, and reads *"Would you bring
   the Wash the dishes?"* — carried, *"Ollie has been asked to bring the Wash the dishes."* **This is
   what makes the ruling unreachable today.** And it is reachable now: neither `POST` in
   `src/app/api/events/[id]/items/[itemId]/assign/route.ts` nor `POST` in
   `src/app/api/c/[token]/items/[itemId]/assign/route.ts` reads `kind`, so a job can be assigned by
   hand the moment it is typed; only `POST` in `src/app/api/events/[id]/people/auto-assign/route.ts`
   leaves `TASK` rows out. *[CORRECTED 2026-09-13 — Corrections 1 above. The host assign route does
   read `kind`, through `mayHoldRow`. A job typed into an ordinary team is assignable exactly as
   described here; one typed under a generated job team is refused for every guest.]*
2. **The preview reads no kind.** `GET` in `src/app/api/events/[id]/pre-flight/message/route.ts`
   selects a person's assignments with no `kind`, so every caller of `composeAsk` passes
   `jobNames: []` and even a generated `TASK` row reads *"bring"* on the pre-flight page. **[[GTC-189]]
   slice 3's to split** — split, not filter. Layer D of `tests/carried-ask-composition-test.ts` pins
   the empty list until then. *[2026-09-13 — split at [[GTC-189]] slice 3: the
   preview reads `kind` and passes every row through the list for its kind, and layer D is gone.]*
3. **The generator names jobs as verb phrases.** `buildPlanGenerationPrompt` in
   `src/lib/ai/prompts.ts` asks for day-of jobs with *"normalise the phrasing"* and gives *"Wash the
   dishes"* as the example name. The ruled sentence wants a noun phrase — *"do the dishes"* — so the
   first generated job, once split, reads *"Would you do the Wash the dishes?"*. Pinned
   `[DEFECT GTC-302]` in layer J. **This is Half 2's defect arriving a second time**, and like Half 2
   it is not fixable in the composer.

**⚠ For slice 3, not this ticket, and recorded so it is not rediscovered:** the chooser's
`ChooserMembership.holdsItems` in `src/lib/eligibility/channel-chooser.ts` must count jobs. A child
whose only row is a job, counted as holding nothing, is `CHILD_WITHOUT_ITEM` and carried to no one —
a filter by another name. It is written into slice 3's entry in [[GTC-189]]'s build shape as well.

**Why the carried sentence is the one most exposed.** The recipient model carries a child's ask and
nothing else ([[GTC-189]], THE ASK), and the model has children doing jobs — Hinge §4 has children as
*"owners of tasks, never recipients of messages"*, and Moment 4 §6 is *"one assignment machine, two
kinds of rows"*. So the carried ask's common case is a job.

### Does an item know it is a job? **Yes.**

- **`Item.kind`**, of type `RowKind` — `ITEM` or `TASK` — added by [[GTC-171]]. The schema docstring:
  *"Task rows are Item rows, so `Assignment` reaches both through the identical relation,
  unchanged."*
- **It is already read to tell them apart:** `POST` in
  `src/app/api/events/[id]/people/auto-assign/route.ts` takes only `kind: 'ITEM'` rows; `GET` in
  `src/app/api/events/[id]/summary/route.ts` counts the two kinds separately.
- **So slice 3 can split on it. No field has to be added to tell a job from a dish** — for the rows
  that carry it.

**Three limits, stated plainly because they decide what the flag is worth:**
1. **Only plan generation marks a job.** `POST` in `src/app/api/events/[id]/finalize-plan/route.ts`
   is the one writer of `kind: 'TASK'` in `src/` — the plan's set-up, clean-up and other-jobs
   buckets. No row-creating route reads a kind from its request, so a job a host types through
   `POST` in `src/app/api/events/[id]/teams/[teamId]/items/route.ts` or `POST` in
   `src/app/api/c/[token]/items/route.ts` is stored as `ITEM`, the default, and nothing marks it.
   **The flag is reliable for generated jobs and absent for typed ones.**
2. **gather_dev has never held a job.** 0 of 1,017 rows are `TASK`, so no job has been assigned,
   composed or carried in this database, and everything in this half is by reading. A search of
   `ITEM` names for job verbs (*wash, clean, set up, pack, tidy, carve, serve, …*) found none that
   read as a job.
3. **Not a filter — a sentence, and the sentence is now ruled.** A job filtered out of the message is
   a job the message no longer asks for, and a child's job filtered out of a carried ask reaches no
   adult — which is what the carried ask exists to prevent. The founder: *"The answer was always a
   sentence, never a filter."* Ruled as [[GTC-189]] answer 6, on a case gather_dev has never held.

---

## Half 2 — untidy data: item names in no consistent form

### Signal

**Expected**
A name that reads correctly after *"the"*, mid-sentence: *"Would you bring the pavlova and the
trifle?"*

**Actual — what gather_dev holds, 1,017 `Item` rows, measured 2026-09-13**

- **Generated, 823 rows** (`source = GENERATED`): every one capitalised, none beginning with an
  article, and 546 in Title Case (*Garden Salad*, *Roast Kumara*, *Sparkling Grape Juice*) against
  82 in sentence case.
- **Manual, 194 rows** (`source = MANUAL`): 182 capitalised, 90 in sentence case (*Beef sausages*,
  *Christmas cake*), 14 in Title Case, and **56 that already begin with an article** (*The
  pavlova*, *the glazed ham*). All 56 are on four GTC-192 seed boards (*GTC-192 glance — mixed*,
  *replay — arrival*, *phase 7 — the two variants*, *replay — all green*). The strings are written
  by `buildBoard` in `scripts/seed-gtc192-replay.ts` and `build` in `scripts/seed-gtc192-phase7.ts`.
- **Qualifiers inside the name**, 324 rows with `( & / + , : ;` — *Serving Platters (large)*, *Butter
  (for table)*, *Glazed Ham (bone-in leg)*, *Sparkling Wine / Prosecco*.
- **Proper nouns and brands** — *Marlborough Sauvignon Blanc*, *Bluff oysters*, *Whittaker's
  chocolate*, *Beer (DB Export, Speights)*.
- **Names that are already lists** — *Guacamole and Corn Chips*, *Crackers and dips*.
- **Characters outside GSM-7**, 15 rows, among them em dashes inside names (*Seafood on the Grill
  (mixed — prawns, fish fillets, mussels)*) and accents (*Rosé*, *Crudités Platter*).
- **Fixture rows** — *Row 12-0*, *Item Team A 2*.

Of 185 assigned rows, 143 belong to a non-child, non-host member — the people the pre-flight preview
lists today.

**No write site normalises a name.** By grep and by reading each one's name handling:
`applyPlanSections` in `src/lib/ai/plan-write.ts` writes the generated `item.name`; `POST` in
`src/app/api/events/[id]/teams/[teamId]/items/route.ts` and `POST` in
`src/app/api/c/[token]/items/route.ts` write the typed name; `PATCH` in
`src/app/api/events/[id]/items/[itemId]/route.ts` writes `body.name`. The JSON examples in
`src/lib/ai/prompts.ts` show `"name": "Item Name"`, and `buildPlanGenerationPrompt` gives
`"name": string` with no guidance on form.

**The founder chose to see this rather than hide it.** Offered three ways on, the founder chose to
build the ruled sentence with each name exactly as stored and file the data, to be fixed **before
[[GTC-189]] slice 5 sends anything**. Not taken: holding the colon list, and fixing names inside the
composer — the papering over the founder refused, which also breaks proper nouns.

**Smallest failing case**
`composeAsk` with own items `['The pavlova', 'Berry Trifle']` composes *"Would you bring the The
pavlova and the Berry Trifle?"* — pinned as `[DEFECT GTC-302]` in
`tests/carried-ask-composition-test.ts`. On other gather_dev names: *"Would you bring the Berry
Trifle and the Beer (DB Export, Speights)?"*, and *"the Pavlova and the Guacamole and Corn Chips"*.

---

### Reproduce

**Required state**
- gather_dev, or any database the plan generator has written to.
- Multi-actor: [ ] Yes [x] No

**Steps**
1. `select kind, source, count(*) from "Item" group by kind, source;`
2. `select source, sum((name ~ '^[A-Z]')::int), sum((name ~* '^(the|a|an|some) ')::int) from "Item"
   group by source;`
3. Read `POST` in `src/app/api/events/[id]/teams/[teamId]/items/route.ts` for a `kind` on the row it
   creates, and `GET` in `src/app/api/events/[id]/pre-flight/message/route.ts` for a `kind` it reads.
4. `npm run test:carried-ask` and read both `[DEFECT GTC-302]` assertions.

**Observe:** no `TASK` rows; no kind written by the add route and none read by the preview;
capitalised generated names and article-led seeded names; the composer reads names as stored, and a
job named as the generator is prompted reads *"do the Wash the dishes"*.

---

### Scope

**Scoped in part** by *Founder answers — scoping*, above. **Built:** fix 1 — the kind on both add routes
and the host PATCH, the toggle, the *"Do the"* label — the article strip at capture, and the seed
strings. **Ordered after [[GTC-189]] slice 3:** the prompt changes (casing, qualifiers to `notes`, how
a job is named) and the display helper. **Left as stored:** the 961 rows (Unknown 5).

**Do not touch:**
- All Do-Not-Touch Zones in `GATHER-BUILD-CONSTANTS.md` are binding. **Zone 5** if the answer to
  Unknown 2 or 3 is a column or a backfill migration.
- `whatIsAsked` in `src/lib/messages/ask-register.ts` — the job sentence is ruled ([[GTC-189]]
  answer 6), and this ticket makes it reachable rather than rewording it.
- `theItems` in the same file, unless Unknown 3 is answered *composition* — the founder refused that
  at filing. The two `[DEFECT GTC-302]` assertions are **expected** to change only with the names
  fix.
- **Filtering jobs out** anywhere — ruled against.

---

### Acceptance

**For this pass — fix 1, the article strip, the seed fix** (*Founder answers — scoping*):
- [x] A job a host or a coordinator adds is stored as `TASK`, with no quantity.
- [x] A kind the routes cannot read is refused, never defaulted to a dish.
- [x] A job's name field reads *"Do the ___"* on all three add forms.
- [x] A name submitted with a leading article is stored without it, at every capture site; a stored
      name nobody submitted is left exactly as it is.
- [x] No seed script writes an article-led item name, and the three fixed-id boards hold none.
- [x] No job is filtered anywhere; the composer is untouched; security suite 157/157; `tsc` clean.

**Still owed by this ticket, after [[GTC-189]] slice 3:** a generated job's name reads correctly after
*"do the"* (the prompt); a generated name reads correctly after *"the"* — casing and qualifiers (the
prompt, `notes`, the display helper).

*The minimum written at filing, kept:* a job a host adds is stored as a job and reads *"do"*, own or
carried; no job is filtered out, and a job carried for a child still reaches an adult; a job's name
reads correctly after *"do the"*; and a name read after *"the"*, mid-sentence, reads correctly for
generated, typed and seeded rows.

---

### Unknowns

1. ~~**The sentence for a job.** What the register says of a `TASK` row, own and carried, and whether
   one message can hold both a dish and a job.~~ **ANSWERED — [[GTC-189]] answer 6: done, not
   brought; one person holding both kinds is one sentence** — with a comma before *"and do"* when
   there are two or more to bring (answer 7).
2. ~~**A typed job — the defect that keeps answer 6 unreachable.** How a host marks a row as a job: a
   kind on the add-item routes, a separate way to add a job, or something else. Today only
   generation can.~~ **ANSWERED — a toggle on the add-item form, brought or done; a coordinator may
   add a job. BUILT. The two words are proposed, not ruled.**
3. **Where the name fix lives.** At capture (normalise on write), at generation (the prompt asks for
   lower-case common-noun phrases), a separate field for how a sentence names the item (schema), or
   composition after all. **PARTLY ANSWERED — not a field, not composition. The leading article at
   capture: BUILT. Casing and qualifiers at generation, with capitals added on display: after slice
   3.**
4. ~~**The host's own form.** Whether a host who types *"Nana's Pavlova"* keeps those capitals.~~
   **ANSWERED — store lower case, capitalise on display, one helper. After slice 3.** Until then
   capture changes no case, so a typed *"Nana's Pavlova"* is stored as typed.
5. ~~**Existing rows.** Whether they are rewritten, and whether that reaches the ten events carrying a
   `stripePaymentIntentId` ([[GTC-189]] ruling L; [[GTC-256]] Ruling 12).~~ **ANSWERED — leave the
   961. Nothing rewrites them, so the paid events are not reached.**
6. ~~**Qualifiers.** Whether *(for table)* and *(bone-in leg)* are part of the name or logistics that
   [[GTC-187]] decision 1 keeps on the tap page.~~ **ANSWERED — logistics; they move to `notes`.
   After slice 3, and the cost of `notes` on V1's JSON is owed first.**
7. ~~**The seed scripts.** Whether the GTC-192 boards keep article-led names, which read correctly on
   the glance and wrongly in the ask.~~ **ANSWERED — fix the strings, pulled forward. BUILT; *glance
   — mixed* not re-seeded (see Evidence).**
8. ~~**How a job is named.** The ruled sentence wants *"the dishes"*; the generator is prompted with
   *"Wash the dishes"*. Whether generation names jobs as nouns, or the stored verb phrase is kept for
   the board and something else feeds the sentence.~~ **ANSWERED — one name, noun form, no second
   field; the *"Do the ___"* label is the fix for a typed job. BUILT. How the generator names a job
   is the prompt's, after slice 3.**

---

### Stop Conditions

Standard eight from `BUG-TICKET-TEMPLATE.md`. Live notes:

- **7 (schema migration):** fires if Unknown 2 or 3 is answered with a field or a backfill.
- **8 (unknowns):** Unknown 2 before any change to how a job is stored; Unknowns 3 and 8 before any
  change to names.

---

## Evidence (fix 1, the article strip, the seed fix) — Executor-Completed

> **Part of GTC-302, not all of it — the ticket stays open.** Built on *Founder answers — scoping*
> and **held before commit** on the founder's instruction. NOT built, by the founder's order: the
> prompt changes, the display helper, the qualifier move — all after [[GTC-189]] slice 3.

**What was built**
- **A job added by hand is stored as a job.** `POST` in
  `src/app/api/events/[id]/teams/[teamId]/items/route.ts`, `POST` in
  `src/app/api/c/[token]/items/route.ts` and `PATCH` in `src/app/api/events/[id]/items/[itemId]/route.ts`
  read a submitted kind through `readSubmittedKind` in `src/lib/items/row-kind.ts`. Absent is a dish;
  anything but `ITEM` or `TASK` is refused with 400, never defaulted. A job is written with
  `kindFields('TASK')` — `quantityState` NA, no quantity — the shape `finalize-plan` gives a generated
  job. A coordinator may add one (Unknown 2).
- **The toggle and the label.** `RowKindToggle` in `src/components/plan/RowKindToggle.tsx`, on
  `AddItemModal` (the plan page), `AddItemForm` in `src/components/plan/Moment2PlanView.tsx` (the setup
  page) and the coordinator's add modal in `src/app/c/[token]/page.tsx`. A job's name field leads with
  `JOB_NAME_LEAD`, *"Do the"* (Unknown 8).
- **The article strip at capture.** `itemNameForStorage` in `src/lib/items/name.ts` trims, collapses
  spaces and strips one leading *the / a / an*. No case change, no verb rewriting. Called at every
  write site that stores a name, and only on a name that was submitted — a request that sends no name
  leaves the stored one as it is (Unknown 5). `restoreFromRevision` does not call it.
- **The seed strings** in the three GTC-192 seed scripts: 55 article-led literals, every one an item
  name, now stored lower case with no article.

**Root cause addressed.** No route that creates a row read a kind (Half 1 item 1, and *Corrections* 1),
and no write site tidied a name (Half 2).

**Files changed**
- `src/lib/items/name.ts`, `src/lib/items/row-kind.ts`, `src/components/plan/RowKindToggle.tsx` (new).
- The routes: the two add routes and the host `PATCH` above; `PATCH` in
  `src/app/api/c/[token]/items/[itemId]/route.ts` (name only); `applyPlanSections` in
  `src/lib/ai/plan-write.ts`; `selectTaskRows` in `src/lib/ai/tasks.ts`, which names the rows
  `finalize-plan` writes; `POST` in `src/app/api/events/[id]/generate/route.ts` (two writes) and
  `src/app/api/events/[id]/regenerate/route.ts`; `POST` in `src/app/api/templates/[id]/clone/route.ts`;
  `createItem` and `createTeam` in `src/app/api/events/[id]/conflicts/[conflictId]/execute-resolution/route.ts`.
- The forms: `src/components/plan/AddItemModal.tsx`, `src/components/plan/Moment2PlanView.tsx`,
  `src/app/plan/[eventId]/setup/page.tsx` (passes the kind, sends no quantity for a job),
  `src/app/c/[token]/page.tsx`.
- `scripts/seed-gtc192-replay.ts`, `scripts/seed-gtc192-phase7.ts`, `scripts/seed-gtc192-glance.ts`.
- `tests/item-kind-capture-test.ts` (new), `package.json` (`test:item-kind-capture`).
- `docs/tickets/GTC-302.md`, and `docs/tickets/GTC-189.md` answer 6 (the correction).
- **Not touched:** `src/lib/messages/ask-register.ts`, `src/lib/messages/compose.ts`,
  `src/lib/ai/prompts.ts`, `src/lib/assignment/same-team.ts`, `src/lib/ledger.ts`,
  `prisma/schema.prisma`, `prisma/migrations`.

**Choices made in building — and how the founder answered them** (*Founder answers — the build*; the
founder's numbers are one lower than these, which list the toggle words first)
1. **The toggle words** — *Bring* / *Do*. **RULED.** The accessible name *"Brought or done?"* and the
   placeholder *"dishes"* remain the executor's.
2. **A job's form asks no quantity and no dietary tags**, and the routes write none. *Critical* stays
   on the form for a job. **Fine as built.**
3. **Only the job's field got a lead.** A dish keeps its *"Item Name"* label; *"Bring the ___"* was
   not built. **Right as built — the lead is there because jobs needed one, not for symmetry.**
4. **The toggle starts on Bring everywhere**, including under a generated job team (*Set up*, *Clean
   up*, *Other jobs*). Not inferred from the team, for the reasons in scoping. **Fine as built.**
5. **`kind` is versioned but is NOT an ask field**, so changing the kind of an answered row asks for
   no why. **WRONG — a kind change is a change to the ask, not a reclassification, because the
   composer reads kind to choose the verb. Filed as [[GTC-304]]; pinned `[DEFECT GTC-304]`.**
6. **A kind change leaves an existing assignment standing.** A job assigned across teams and then
   made a dish keeps a holder the same-team rule would now refuse, and keeps `quantityState` NA; a
   dish made a job keeps its quantity. **WRONG — the kind change can produce a row no route would
   have created. Filed as [[GTC-303]]; pinned `[DEFECT GTC-303]`.**
7. **A kind change is substantive**, so a GENERATED row becomes HOST_EDITED — without it,
   `disposableItemWhere` would let the next regenerate delete a re-kinded row under its new kind.
   **Fine as built.**
8. **The coordinator's `PATCH` reads no kind** — the founder named the host PATCH. The suite pins the
   absence as `[SCOPE]`. **Fine as built.**
9. **A blank name is now refused with 400** by the four item routes. Before, `POST` accepted
   `"   "` and both PATCHes stored whatever arrived. **Fine as built.**
10. **"a" and "an" are stripped as well as "the".** **Accepted, with the cost recorded so it is
    visible rather than discovered:** *"A la carte platter"* is stored as *"la carte platter"*.

**The seed fix — what was done with each board**
- *replay — arrival*, *replay — all green* (`npx tsx scripts/seed-gtc192-replay.ts`) and *phase 7 —
  the two variants* (`npx tsx scripts/seed-gtc192-phase7.ts`): re-seeded by fixed id, URLs unchanged.
  Article-led names after: **0, 0 and 0** (were 13, 10 and 13). Their rows were untouched since
  seeding (last `updatedAt` equal to the seed time), so nothing was lost.
- ⚠ **glance — mixed: NOT re-seeded, and LEFT THAT WAY BY FOUNDER RULING.** Its script recreates by
  tag with generated ids, so a re-seed changes its URL and *glance — oversized*'s; and it holds a row
  its seed never writes — *"The camp chairs"* — which a re-seed would delete. It is a test board, not
  a demo one. Its script's strings are fixed; **the board itself still holds 20 article-led names, on
  purpose.** Do not re-seed it to tidy them. The same warning sits in the header of
  `scripts/seed-gtc192-glance.ts`.
- ⚠ **Stored lower case shows lower case** until the display helper lands: the re-seeded boards now
  read *gravy*, *pavlova*, *glazed ham*.

**Test results**
- **RED — 91 assertions, 23 passed, 68 failed**, with `src/` and `scripts/` untouched and the two new
  modules as stubs whose functions throw. Every passer holds without the change: the words, anchor and
  client-safety of the stub module (5, constants the stub already carried); the unchanged gate (G, 3);
  the write-site classification, its control, the restore and the four no-name writers (W, 8 —
  structure and absences); `[UNRULED]` and `[SCOPE]` (H, 2 — absences); two coordinator-route
  properties the change had to keep (C, 2); and the three *"words neither the toggle nor the lead"*
  absences (U, 3). The seed matcher found 23, 13 and 19 literals — exactly the item names.
- **GREEN — 91/91.**
- **Mutation checks** — each confirmed landed, the suite run, the file restored and compared
  byte-identical:
  - article not stripped → 8 failed (N 5, C 3 — one of the C three is the blank-PATCH check, whose
    expected name depends on the rename before it);
  - an unreadable kind defaulted to ITEM → 2 (K, C);
  - a job keeping a quantity state → 2 (K, C);
  - the coordinator `POST` dropping the kind → 2 (C);
  - kind not substantive in the host `PATCH` → 1 (H);
  - `applyPlanSections` writing the raw name → 1 (W);
  - the coordinator `PATCH` writing the raw name → 3 (W, C 2);
  - a seed literal regaining *"The"* → 1 (S).
- **Adjacent:** `test:carried-ask` 69/69 (both `[DEFECT GTC-302]` pins unchanged — they compose names
  handed to them, and the composer did not change), `test:message-composition` 55/55,
  `test:task-row-eligibility` 20/20, `test:task-row-regenerate` 14/14, `test:task-row-revision` 8/8,
  `test:task-row-auto-assign` 8/8, `test:plan-write-preserve` 26/26, `test:host-holds-items` 22/22,
  `test:host-never-messaged` 20/20, `test:ledger-record-change` 30/30, `test:ledger-why-scope` 102/102,
  `test:edit-item-frozen-block` 9/9, `test:glance-read` 152/152, `test:glance-actions` 79/79,
  `test:glance-replay` 298/298, `test:channel-chooser` 116/116, `test:child-exclusion` 19/19.
- `npx tsc --noEmit` exit 0.

**Browser, against the dev server** — the host routes read a session cookie and the suite can only
read them, so they were driven for real on *GTC-129 Test BBQ* (DRAFT):
- The setup page's add row shows *Bring | Do*; *Do* swaps Name, Quantity and Serves for *"Do the
  [dishes]"*.
- Typed *"The dishes"* under *Do* and pressed Add: stored `dishes`, `TASK`, `MANUAL`, `quantityState`
  NA, no quantity; the ledger's `CREATE_ITEM` carries `"kind": "TASK"`.
- Host `PATCH` from the page: `{kind: 'CHORE'}` → 400 *"kind must be ITEM or TASK"*; `{name: '   '}` →
  400; `{kind: 'ITEM'}` → 200; `{kind: 'TASK', name: 'The washing up'}` → 200, stored `washing up`. The
  ledger holds `EDIT_ITEM` `kind` TASK→ITEM, `name` dishes→washing up, `kind` ITEM→TASK, none needing
  a reason.
- **The test row and its four ledger rows were then deleted** — this change's own writes on a test
  event.

**Added after the build report, on *Founder answers — the build***
- **The stale comments, fixed.** The docstring of `whatIsAsked` in `src/lib/messages/ask-register.ts`
  and layer J of `tests/carried-ask-composition-test.ts` (its header and its `[DEFECT GTC-302]`
  comment) said the add routes set no kind, or that plan generation is the one writer of TASK rows.
  Both false since this change; both rewritten. Comments only — no word of the register changed, and
  `test:carried-ask` is 69/69.
- **The toggle words ruled.** `ROW_KIND_WORDS` in `src/lib/items/row-kind.ts` records the ruling; the
  `ANCHOR(GTC-302)` is gone, and the suite asserts none remains in `src/`.
- **Two defects filed, not fixed:** [[GTC-303]] (a kind change can leave a row no route would have
  created) and [[GTC-304]] (a kind change is a change to the ask, and the ledger does not see it).
  Each is pinned `[DEFECT …]` in `tests/item-kind-capture-test.ts` and marked at the cause in
  `PATCH` in `src/app/api/events/[id]/items/[itemId]/route.ts`.
- **Glance — mixed**, left by ruling, with a warning in the header of `scripts/seed-gtc192-glance.ts`.
- **The "A la carte" trade** recorded in the docstring of `itemNameForStorage`.
- Suite after: `test:item-kind-capture` 92/92 (one assertion added, `[DEFECT GTC-303]`);
  `test:message-composition` 55/55; `test:ledger-why-scope` 102/102; `test:glance-read` 152/152;
  `npx tsc --noEmit` exit 0.
- **The two defect pins, mutation-checked** — each confirmed landed, the file restored byte-identical:
  the PATCH calling `mayHoldRow` fails exactly `[DEFECT GTC-303]`; `'kind'` added to `ASK_FIELDS`
  fails exactly `[DEFECT GTC-304]`. One failure each, no collateral.

**Preflight:** `prisma migrate status` — 43 migrations, up to date; run instead of `npm run db:migrate`
(`migrate dev`). No migration in this change.

**Security suite:** 157/157.

**Commit:** the commit that adds `tests/item-kind-capture-test.ts` —
`git log --diff-filter=A --format=%h -- tests/item-kind-capture-test.ts`. A commit cannot carry its
own hash.
