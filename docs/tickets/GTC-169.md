---
ticket: GTC-169
title: "A3a — Flip the server gates: delete requireNotFrozen, invert the nudge predicate"
status: open
branch: experiment/send-lock-ledger
commit:
moment: 4
type: architecture-build
depends_on:
  - "[[GTC-168]]"
blocks:
  - "[[GTC-196]]"
tags: [moment-4, hinge, epic-a, state-machine, migration, security, experimental]
---

# GTC-169 — A3a: Flip the server gates

**Status:** Open | **Branch:** `experiment/send-lock-ledger` | **Commit:** [pending] | **Effort:** high

**Plan of record:** [`docs/04_roadmap/send-lock-reconciliation-plan.md`](../04_roadmap/send-lock-reconciliation-plan.md)
§4.2 Layer 1 (GTC-167, commit `76943e4`).

> **Refiled 2026-08-03.** The original GTC-169 covered the whole ~145-site consumer migration. Per
> A1 §11 and Nigel's approval, A3 splits four ways: **this ticket is A3a**; A3b is [[GTC-196]],
> A3c is [[GTC-197]], A3d is [[GTC-198]]. The a/b seam is the one that matters — A3a is a small,
> high-risk diff that changes what the system *permits*; A3b is a large, low-risk diff that changes
> what the system *records*. Together they would bury 39 security-relevant lines in a thousand
> mechanical ones.

---

### Context

A2 built the predicates and the ledger, additively. This ticket is the first that **changes
behaviour**: every lifecycle-derived hard block is removed, and the nudge predicate inverts.

It is the smallest diff in Epic A and the only one that touches the security contract.

### ⚠ Zone 6 — explicit founder instruction on record

`gather-change-control` Do-Not-Touch zone 6 forbids weakening or skipping security-suite assertions.
**Nigel approved rewriting them for this ticket, 2026-08-03, in these terms:**

> "Security suite: APPROVED to rewrite — replace, never delete. The new assertions must hold the
> send-lock contract at equal strength: post-send mutations carry ledger entries, why-scope triggers
> fire, hard-blocks absent. Explicit instruction per zone 6; note it in the A3a ticket."

Approval is **scoped to this ticket and to that standard**. Assertions are *replaced at equal
strength*, never deleted to make a build pass. Any assertion that cannot be replaced at equal
strength is a stop-and-report.

| Removed assertion | Replacement (equal strength) |
|---|---|
| 1.2 `requireNotFrozen` blocks FROZEN (`security-validation.ts:62–108`) | A post-send coordinator mutation **succeeds AND writes a ledger entry** — asserts both halves |
| 1.4 `requireNotFrozen` allows FROZEN with override (`:160–206`) | No direct successor (override was a FROZEN concept). Replaced by: **no lifecycle-derived 4xx on any Layer-1 route post-send** |
| 3.4 Coordinator items route has frozen validation (`:386–403`) | Coordinator items route calls `recordChange()` on every mutation path |
| 3.5 Coordinator item edit route has frozen validation (`:414`) | Same, for PATCH and DELETE |
| — *(new)* | `touchesSomeone()` fires on T1–T5 and stays silent on every enumerated non-trigger |
| — *(new)* | `restoreFromRevision` is **refused post-send** — the only new post-send restriction (Hinge §2, no undo at the mechanism level) |
| — *(new)* | A touching-someone change **without** a reason still succeeds, recorded as `reasonRequired: true, reason: null` (§13.1, endorsed) |

`tests/security-fixtures.ts` `Security Test Event (FROZEN)` (`:205–212`) becomes
`Security Test Event (SENT)` with `sentAt` set.

### Ruling folded in: coordinators (Nigel, 2026-08-03)

> "(a) — same always-allow + ledger as the host. **No walls anywhere, ledger is actor-agnostic.**"

So `requireNotFrozen` is deleted outright rather than narrowed to a host-only bypass, and the
coordinator routes lose their gates entirely.

---

## Scope — the sites (plan §4.2 Layer 1)

**~39 sites across 16 files.** Re-verify counts at ticket start; code moves.

### State machine and guards

| File | Change |
|---|---|
| `src/lib/workflow.ts` | `canTransition` → `{ DRAFT: ['CONFIRMING'], CONFIRMING: [] }`. `canMutate` loses its FROZEN and COMPLETE denials **and** its CONFIRMING critical-delete branch (plan §4.5 — unreachable dead code encoding a forbidden hard block; its only caller passes `itemCritical: false`). `checkFreezeReadiness` → `checkSendReadiness`, `canFreeze` field dropped, the `<80% ⇒ freezeReason` requirement **deleted** (a demand for justification at a threshold, forbidden by Moment 4 §7). |
| `src/lib/auth/guards.ts` | **`requireNotFrozen` deleted.** |

### Nudge predicate — the inversion (plan §0.2)

| File | Change |
|---|---|
| `src/lib/sms/nudge-eligibility.ts` (3 sites: `:62`, `:71`, `:237`) | `event: { status: 'CONFIRMING' }` → `SENT_AND_LIVE(now)`. Today **freezing stops the nudges**; the ruled model says the send is when chasing *starts*. |
| `src/lib/sms/proxy-nudge-eligibility.ts` (`:38`) | Same. |

⚠ **Zone 7 boundary.** Change ONLY the event-status filter in the candidate query. Explicitly
unchanged: `isOptedOut()`, `isValidNZNumber()`, quiet hours, the 24h/48h cadence windows,
`nudge24hSentAt`/`nudge48hSentAt`. Cadence is [[GTC-178]] (E1). If the diff reaches any opt-out
path, that is Stop Condition 5.

### API routes

| Route | Change |
|---|---|
| `api/c/[token]/items/route.ts` | `requireNotFrozen` + `canMutate` removed (2 sites) |
| `api/c/[token]/items/[itemId]/route.ts` | Same, PATCH + DELETE (4 sites) |
| `api/c/[token]/items/[itemId]/assign/route.ts` | Same, POST + DELETE (4 sites) |
| `api/events/[id]/items/[itemId]/assign/route.ts` | `requireNotFrozen(event, auth.role === 'HOST')` removed (2 sites) |
| `api/p/[token]/ack/[assignmentId]/route.ts` | **Semantic inversion.** Today 400s with *"Plan is frozen — responses are locked"*; post-send responding is the whole of Moment 4 (§7: *"they are the plan being answered"*). Gate deleted. |
| `api/join/[token]/claim/route.ts` | `CONFIRMING \|\| FROZEN` → `status === 'CONFIRMING' \|\| isSent(event)` |
| `api/events/[id]/shared-link/route.ts` | Same predicate swap |
| `api/events/[id]/transition/route.ts` | CONFIRMING→FROZEN branch **deleted**; FROZEN→COMPLETE branch **deleted**. Route reduces to the DRAFT→CONFIRMING handler (7 sites) |
| `api/h/[token]/status/route.ts` | **Whole `status` branch deleted** — `unfreezeReason`, `OVERRIDE_UNFREEZE`, all of it. Route keeps `guestCount` and its path. |
| `api/events/[id]/wrap-up/route.ts` | `status !== 'FROZEN'` → `!isComplete(event)`; **`confirmEarly` bypass deleted**; stops writing `status`, writes `wrappedAt` only; audit `TRANSITION_TO_COMPLETE` → `WRAP_UP_SENT` |
| `api/events/[id]/confirm-invites-sent/route.ts` + `api/h/[token]/confirm-invites-sent/route.ts` | Write target `inviteSendConfirmedAt` → `sentAt`. CONFIRMING gate retained (you cannot send a plan you have not built — a sequencing fact, not a host block). Superseded wholesale by [[GTC-189]] (I2) later. |
| `api/sms/test-send/route.ts` | Dev fixture lookup; predicate swap |
| `api/events/[id]/revisions/[revisionId]/restore/route.ts` | **New guard:** refuse when `isSent(event)`. The plan's only *added* post-send restriction — Hinge §2 rules out undo at the mechanism level, and restore is recall by another name. |

### Do not touch

- Ledger *writes* — that is [[GTC-196]] (A3b). This ticket removes gates; it does not add
  `recordChange()` calls. **The gap between A3a and A3b (mutations allowed but not yet recorded)
  exists only between two commits on a branch nobody deploys** — this is why Epic A merges as one
  unit (plan §4.3).
- All UI — [[GTC-197]] / [[GTC-198]].
- All Do-Not-Touch Zones except zone 6, and only within the scope quoted above.
- Epic B–K territory.

### Acceptance

- [ ] Every site in the map migrated or explicitly deferred with reasoning — none silently skipped.
- [ ] `grep -rn "requireNotFrozen\|canFreeze(" src` → **zero matches**.
- [ ] `grep -rn "'FROZEN'" src --include="*.ts" | grep -v lifecycle.ts` → zero matches in
      `src/lib` and `src/app/api` (UI sites remain until A3c/A3d).
- [ ] Scripted probe: a post-send mutation against every route above returns 2xx. **Hard-block count
      caused by lifecycle state = 0.**
- [ ] Nudge predicate verified both ways: a sent event with a future `endDate` yields candidates
      (today it does **not**); a past-`endDate` event yields none.
- [ ] `restoreFromRevision` refused post-send, allowed pre-send.
- [ ] Security suite rewritten per the table above and **exits 0**, with every replacement assertion
      reviewed against the "equal strength" standard.
- [ ] `npx tsc --noEmit` clean, `npx next build` succeeds, `npm run format:check` clean.

### Stop Conditions

Standard eight, plus:

9. [[GTC-168]] (A2) is not closed.
10. A security-suite assertion cannot be replaced at equal strength — stop, do not delete it.
11. The nudge-eligibility diff reaches any SMS opt-out path (zone 7).
12. Any site in the map turns out to be larger than the plan describes.

### Closed ticket format (for the executor)

```
# GTC-169 — A3a: Flip the server gates — CLOSED
Status: Complete | Branch: experiment/send-lock-ledger | Commit: [hash] | Effort: high
## Sites migrated (per the map, with what changed at each)
## Sites deferred (with reasoning)
## Security suite: assertions replaced (old → new, equal-strength justification each)
## Hard-block probe results / nudge predicate verification
## Assertions met
## Evidence
```
