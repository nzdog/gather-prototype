---
ticket: GTC-239
title: 'STUB — retire the selective-regeneration branch of /api/events/[id]/generate'
status: open
branch: TBD
commit:
moment: 2
type: chore
severity: low
depends_on:
  - '[[GTC-240]]'
blocks: []
related:
  - '[[GTC-236]]'
tags: [stub, tier-2, v1-retirement, dead-path-candidate, not-current-sprint]
---

# GTC-239 — STUB: the selective keep/regenerate branch is Tier 2 retirement material

**Status:** Open — stub, blocked on [[GTC-240]] | **Effort:** TBD

*Source: founder direction during [[GTC-236]], 2026-08-22, with facts corrected against the
tree at `cfa6845`. Founder initially asked to fold this deletion into GTC-236; recorded here
instead because the deletion breaks a surface [[GTC-240]] explicitly defers (see below), and
GTC-236's own Do-Not-Touch shields V1's regenerate pipeline.*

## Signal

The selective branch of `POST` in `src/app/api/events/[id]/generate/route.ts`
(`keepItemIds`/`regenerateItemIds`) has exactly two consumers at HEAD — verified by grep,
not assumed:

1. `handleReviewRegenerateSelected` in `src/app/plan/[eventId]/page.tsx` — the V1 dashboard
   review flow (with `GenerationReviewPanel`, `mark-for-review`, `review-items` routes).
2. `handleRegenerateSelected` in `src/app/demo/review/page.tsx` — which nothing links to
   (its only reference is its own doc comment; URL-only surface).

A prior instruction named "the last Moment2Section consumer" — **no symbol `Moment2Section`
exists in the tree**; recorded so the phantom does not get re-derived.

## Sequencing

Retiring this branch breaks `/demo/review`'s regenerate, so it **depends on [[GTC-240]]**
(or an explicit decision to break it). The V1 dashboard consumer retires with the broader
Tier 2 V1 cleanup. Note: the branch also carries a cross-event authorization hole
(client-supplied id lists, unscoped `updateMany`/`deleteMany`) — flagged separately as a fix
task; if this retirement lands first, the hole dies with the branch.

## Do not touch

All Do-Not-Touch Zones in `GATHER-BUILD-CONSTANTS.md`.
