---
ticket: GTC-320
title: 'The participant and coordinator ack routes duplicate their write and their audit line, and the two have already drifted'
status: open
branch: TBD
commit:
moment: 4
type: chore
severity:
severity_proposed: medium
depends_on: []
blocks: []
related:
  - '[[GTC-191]]'
  - '[[GTC-174]]'
  - '[[GTC-294]]'
tags: [refactor, duplication, ack, audit, zone-3-adjacent]
---

# GTC-320 — Two ack routes, one write, and a line that has already drifted

**Status:** Open — filed 2026-09-18, not started | **Branch:** TBD
**Severity:** [ ] Critical [ ] High [x] Medium [ ] Low — proposed at filing; the founder's to
set. `severity_proposed` carries the proposal and `severity` stays empty until it is set.
Escalated (brand/trust risk): [ ] Yes [x] No

*Filed on founder instruction, 2026-09-18, out of [[GTC-191]]: "File it. Do not extract it here
— that is a refactor inside a Zone 3 route on a ticket that is already carrying a token
authorising a write outside its own person."*

⚠ **PROVENANCE, STATED PLAINLY BECAUSE THIS LEDGER CARES ABOUT IT.** This was found by READING
the two routes while deciding where [[GTC-191]]'s carried case belonged, and then measured. It
was **not** found by a surviving mutation, and no mutation run was performed.

---

### Signal

**Expected**
One place decides what an item response writes and what the ledger records about it.

**Actual**
Two routes do, and they no longer agree on the second half.

- `POST` in `src/app/api/p/[token]/ack/[assignmentId]/route.ts` — the guest's tap.
- `POST` in `src/app/api/c/[token]/ack/[assignmentId]/route.ts` — a coordinator answering their
  own item.

**Measured at `87a35ef`+, comments stripped, matching blocks of three lines or more:**

- 37 shared non-blank lines.
- The core — the `assignment.update`, the `verb` ternary, and the `logAudit` call with its
  three-way `actionType` ternary, `targetType` and `targetId` — is **17 of those, in two blocks
  of 6 and 11**.
- The participant route is 131 non-blank lines; the coordinator route is 57.

**⚠ And the single line BETWEEN those two blocks is the one that has drifted.** `details`:

- participant: `` `${verb} assignment for item ${assignment.itemId}` `` — an opaque cuid.
- coordinator: `` `${verb} ${assignment.item.name}` `` — the item's name.

So the same action, taken through two doors, writes two different sentences into the ledger, and
one of them is unreadable to a human. The ledger is described in
`prisma/schema.prisma` as Kate's memory of what happened; a row reading "Accepted assignment for
item cmu6rj7tl000srjfafy14ufnq" is not a memory of anything.

**Smallest failing case**
Read the two `logAudit` calls side by side.

---

### Why it matters beyond tidiness — it is why the carried case landed in one route and not both

[[GTC-191]] added the carried answer to the participant route only. That is **correct** — a
coordinator who carries a child holds a PARTICIPANT token as well since [[GTC-294]], so her ask
goes through `/p/`, and `/c/` is the job. But a reader of `/c/` now sees a near-identical route
that behaves differently, with nothing in either file saying why. Duplication is what made the
divergence invisible; the drifted `details` line is proof the mechanism is already running.

---

### Scope

**Fix here:**
- The shared write-and-audit core of the two routes named above, and `AuditLifecycleAction` in
  `src/lib/workflow.ts` only insofar as the extracted helper names it.
- The `details` wording — one form, and it should name the item rather than its id.

**Do not touch:**
- All Do-Not-Touch Zones in `GATHER-BUILD-CONSTANTS.md` are binding.
- **[[GTC-191]]'s authorisation.** `resolveCarriedSubjects`, the 403, and the
  carried-vs-own decision stay in the participant route and are not generalised into a shared
  helper. That is Zone 3 reach and it belongs to the route that was approved to have it.
- `parseAssignmentResponse` and the attendance derivation. [[GTC-174]] already put those in one
  place and neither is duplicated.
- The coordinator route's own-row check. A coordinator answers their own item; that is
  [[GTC-174]]'s ruling and this ticket does not widen it.

---

### Acceptance

**Test gate** — Medium: regression test preferred.

**Assertions**
- [ ] One helper performs the response write and the audit line; both routes call it.
- [ ] Both doors write the same `details` form, and it names the item rather than its id.
- [ ] The carried answer's "on behalf of {name}" survives, and appears only where a carried
      answer was given.
- [ ] Adjacent flows unaffected: `npm run test:carried-answer`, `npm run test:guest-response`,
      `npm run test:security` and `npm run test:ledger-why-scope` stay green.

---

### Unknowns

1. **Does the coordinator route keep a separate audit action?** Both write the same three
   `AuditLifecycleAction` values today. Whether a coordinator's own answer should be
   distinguishable in the ledger from a guest's is a product question, not a refactor.
2. **Existing rows.** The drifted `details` values are already written. Whether historical rows
   are left as they are is a data question and is not assumed here.

---

### Stop Conditions

Standard eight from `BUG-TICKET-TEMPLATE.md`. Live note:

- **5 (scope):** the participant route carries a Zone 3 approval specific to it. An extraction
  that moves the authorisation into shared code is out of scope and needs its own approval.

---

### Executor Checklist

[ ] GATHER-BUILD-CONSTANTS.md read
[ ] Citations section read; every citation in this ticket is a symbol, not a line number
[ ] Preflight passed and results pasted
[ ] Plan documented (hypothesis, change set, test strategy, rollback risk)
[ ] Fix implemented within declared scope
[ ] No Do-Not-Touch zones modified
[ ] GREEN confirmed
[ ] Acceptance assertions checked
[ ] Adjacent flows verified
[ ] Evidence package assembled
[ ] Commit hash recorded
[ ] No stop conditions triggered without resolution

---

## Evidence (Executor-Completed)

> Filled in by the executor before committing.

**Root cause confirmed**

**Files changed**

**Test results**
- RED:
- GREEN:

**Assertions checked**

**Security suite:**

**Commit hash:**
