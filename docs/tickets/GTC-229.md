---
ticket: GTC-229
title: 'TNZ MO webhook — authenticate it, and fail closed'
status: open
branch: TBD
commit:
moment: 4
type: feature
depends_on:
  - '[[GTC-214]]'
  - '[[GTC-264]]'
blocks:
  - '[[GTC-288]]'
related:
  - '[[GTC-220]]'
  - '[[GTC-231]]'
  - '[[GTC-258]]'
  - '[[GTC-288]]'
tags: [sms, tnz, inbound, webhook, mo, shared-secret, security, fail-closed, moment-4, tnz-go-live-checklist]
---

# GTC-229 — TNZ MO webhook: authenticate it, and fail closed

**Status:** Open — **rescoped 2026-09-12, split in two.** | **Branch:** TBD | **Commit:** [pending]
**Severity:** [ ] Critical [x] High [ ] Medium [ ] Low
Escalated (brand/trust risk): [x] Yes — `POST` in `src/app/api/sms/inbound/route.ts` is an
unauthenticated write into Do-Not-Touch Zone 7. Anyone who can reach the URL can opt an
arbitrary number out of an arbitrary host's sends.

---

## ⚠ THIS TICKET WAS SPLIT — read this before anything else

**Ruled 2026-09-12.** GTC-229 was one ticket blocked in full behind a payload contract
Gather did not have. It divides cleanly, and the half that ships first is the half that
matters most.

**This ticket is now the authentication half**, and it is unblocked:

- Verify the TNZ shared secret on `src/app/api/sms/inbound/route.ts`.
- Fail closed when the secret is unset.
- Stop returning `200` from the catch block.
- Give the route a real response contract against TNZ's documented retry behaviour.
- Add the route to `route-classifications.json` with its real auth type.

**[[GTC-288]] is the parse half** — the MO payload, attribution, and the `SmsOptOut` /
`Person.smsOptedOut` sync. Everything this ticket used to say about opt-out state now lives
there, and this ticket **blocks** it.

**Why the split.** The authentication half depends on none of the open unknowns and closes
an unauthenticated write into Zone 7. The parse half was, at filing, blocked behind a
payload contract answered only with a URL. Holding the security fix hostage to that was the
wrong trade. *(Rationale recorded per the founder's ruling of 2026-09-12: "a fully blocked
ticket becomes a partly shippable one, and the half that ships first is the half that
matters most.")*

**The audit sections below are retained here deliberately.** They are the evidence base for
both halves, and [[GTC-288]] cites them rather than duplicating them. Read audit sections
B, C and D as context for what this ticket does *not* do.

---

## ⚠ TWO RECORDS THAT PREDATE NO TICKET — carry them forward

**1. GTC-229's central framing carries an unstated precondition.** The *Purpose* section
below says the legal promise is kept by TNZ whether or not Gather processes the reply.
**That holds only when the STOP lands on Gather's TNZ account.** TNZ's 2026-08-18 answer 4
says opt-out keywords add the number to *"your Dashboard's Opt-Out list"* — per account. On
a shared shortcode, a reply attributed to a different TNZ customer (H1 in the
correspondence record) may never reach Gather's account at all, in which case Gather's
sends to that guest keep going out **and keep being delivered**. A guest who texted STOP
goes on receiving Gather's messages.

Whether that is so is **asked of TNZ as of 2026-09-12 and outstanding.** If the blacklist
is platform-wide, the shared shortcode is a reporting gap. If it lands only on the
attributed account, it is a compliance exposure and the dedicated shortcode moves from
nice-to-have to close to mandatory before volume. **Do not build on either reading until
TNZ answer.** The *Purpose* section is sound but conditional; read it with this attached.

**2. A dedicated shortcode is a partial fix, and the partition matters.** It closes
cross-customer interception (H1). It does **not** close two Gather hosts messaging one
guest (H2) — TNZ's own answer says that would need a shortcode per host, which is not
purchasable at any realistic price. Cost it as compliance insurance against H1, not as an
attribution fix. Price asked 2026-09-12, outstanding.

Both records, with their evidence, are in
`docs/05_ops/tnz-inbound-and-delivery-correspondence-2026-08.md`.

---

## Provenance

**Filed 2026-08-29**, on TNZ's answers to the capability questions (below). Scoped by
re-derivation from code at `1cbf014`. Every symbol cited was checked against the tree at
that commit (GTC-222 rule 5). Line numbers appear only as parenthetical hints.

**This ticket unblocks the number.** [[GTC-231]] *Unknowns* 4 records that GTC-229 was held
unfiled by design — "the number was proposed during the inbound-STOP scoping and is held
unfiled pending TNZ's reply, because TNZ's own verification scheme decides what the ticket
says. It will be filed when they answer." They answered. This is that file. The absence
noted in [[GTC-231]] and the two forward references in [[GTC-214]] are now resolved.

---

## Purpose — read this before anything else

**Opt-out is not unimplemented, and this handler is not the compliance mechanism.**

TNZ detect opt-out keywords themselves, at their layer, and add the number to their own
opt-out list. Subsequent sends to that number fail with `Destination is blacklisted`. The
guest stops receiving messages whether or not Gather ever processes the reply. That is
answer 5 below, and it is the single most important fact in this ticket.

**The handler's job is keeping Gather's local state in sync with TNZ's.** Without it,
Gather does not know the person opted out, and three things stay wrong:

1. **They stay counted as pending.** `getNudgeStatus` in
   `src/app/api/events/[id]/invite-status/route.ts` never consults opt-out at all — with a
   phone number and a chaseable mark it returns `'pending'`.
2. **They still show as awaitable to the host.** `canReceiveSms` on `PersonInviteStatus` is
   `!!person.phoneNumber && !hasOptedOut`, and `hasOptedOut` is read from the `SmsOptOut`
   table via `getOptOutStatuses`. No local row, no opt-out — the host sees a guest they can
   still chase, and the `optedOut` count in both `counts` and `smsSummary` reads zero.
3. **Nudges keep being queued at them, and fail silently.** `checkOptOut` in
   `src/lib/sms/send-sms.ts` reads the same empty table, so `sendSms` proceeds to the
   provider, TNZ rejects the send, and the result is an `SMS_SEND_FAILED` InviteEvent — a
   failure indistinguishable from a network blip, arriving on a schedule, forever.

**A future session must not read this ticket as "opt-out is unimplemented".** The legal
promise is kept by TNZ — **subject to the precondition recorded at the top of this
ticket.** What is missing is Gather's *knowledge* of it. Do-Not-Touch Zone 7 remains
binding for the same reason it always was.

⚠ **Since the 2026-09-12 split, none of the three consequences above are fixed by THIS
ticket.** They are [[GTC-288]]'s, and they are stated here only because they are why the
handler exists at all. This ticket authenticates the door; [[GTC-288]] decides what comes
through it.

---

## TNZ's answers — the factual basis (2026-08-18)

**Dating corrected 2026-09-12.** This section previously read "(2026-08-29)". The answers
below are dated **18 August 2026**; 2026-08-29 is the date the follow-ups were sent, and
plausibly the date this ticket was filed. The two were conflated.

**Source, added 2026-09-12:**
`docs/05_ops/tnz-inbound-and-delivery-correspondence-2026-08.md` — the greppable
transcription of both PDFs, which sit beside it in `docs/05_ops/`. Until that record was
filed this section cited nothing. Cite the record, not this summary.

Recorded verbatim in substance. These are the ticket's premises; do not re-derive them from
guesswork about how other providers behave.

1. **Inbound IS supported**, for NZ and Australia.
2. **The sending number is auto-allocated** — a shortcode for NZ mobiles, an Australian
   mobile number for AU. A dedicated shortcode is purchasable if needed.
3. **The MO webhook contract is documented** at
   `https://www.tnz.co.nz/Docs/RESTAPI/?version=2.04#receive-webhook`. It is **distinct from
   the delivery-receipt callback format** — do not assume one shape covers both.
4. **Authentication is a shared secret.** Headers `X-Timestamp`, `X-Sender`, `Authorization`,
   plus `Sender` and `APIKey` repeated in the JSON body. Configured in the TNZ dashboard
   under **Users > API User > API > Reporting**.
5. **TNZ detect opt-out keywords themselves** and add the number to their own opt-out list.
   Subsequent sends fail with `Destination is blacklisted`. **The full message is still
   forwarded to the webhook.**

Answer 5 is what makes this a sync ticket rather than a compliance ticket. Answer 3 is what
makes "point the existing handler at TNZ" wrong. Answer 2 is what makes attribution hard —
see *Flags*.

### The follow-up answers (2026-08-31)

Three follow-ups were put to TNZ while the conversation was open. **Two of them belong to
[[GTC-258]]** and are recorded there in full — a send to a blacklisted number returns **200 and is
accepted**, with the failure arriving later through status reporting as `Status=Failed`,
`Result="Destination is blacklisted"`; and status webhooks are **distributed per destination**.
Those two confirmed [[GTC-258]]'s premise and produced [[GTC-264]]. See *Unknowns* 3 below, which
they close.

**The third is this ticket's, and it answers F1's central question:**

6. **On the shared shortcode, TNZ attribute an inbound reply to the LAST SENT message to that
   number.** A dedicated shortcode does not by itself improve this — **it only helps if Gather
   sends from different shortcodes per customer.**

Answer 6 is recorded where it bears, in **F1** below. It does not change this ticket's scope; it
changes what F1 has to report, and it settles the shape of the answer rather than leaving it to
the executor's reading of the doc.

---

## Signal

**Expected**

A guest texts STOP. TNZ blacklists the number and forwards the message to Gather's MO
webhook. Gather authenticates the request, resolves which person and which event the reply
belongs to, writes the `SmsOptOut` row and sets `Person.smsOptedOut`, and logs
`SMS_OPT_OUT_RECEIVED`. The host's invite-status view stops counting that guest as
awaitable; the scheduler stops queueing nudges at them.

**Actual**

`POST` in `src/app/api/sms/inbound/route.ts` is a Twilio webhook handler. It parses
`multipart`/`x-www-form-urlencoded` `From` / `Body` / `MessageSid`, performs no
authentication of any kind, and returns `200 OK` to everything including its own catch
block. TNZ will not send it a form-encoded body, will not use those field names, and will
present a header-plus-body shared secret it does not look for. Nothing in the file is
reachable from a TNZ request.

**Smallest failing case**

An AU guest texts STOP. Even if the handler spoke TNZ's format, `normalizePhoneNumber`
returns `null` for `+61412345678` and the handler returns `200 OK` having done nothing.

---

## Audit at `1cbf014`

### A. What `src/app/api/sms/inbound/route.ts` does today

`POST` runs, in order:

1. `await request.formData()` — reads `From`, `Body`, `MessageSid`.
2. Missing `From` or `Body` → `200 OK`, no action.
3. `normalizePhoneNumber(from)`; `null` → `200 OK`, no action.
4. `isOptOutMessage(body)` from `src/lib/sms/opt-out-keywords.ts` — exact match, trimmed
   and case-folded, against `OPT_OUT_KEYWORDS` (`stop`, `stopall`, `unsubscribe`, `cancel`,
   `end`, `quit`). Non-match → `200 OK`, **message discarded**. There is no storage of
   inbound messages, no reply handling, nothing else consumes an inbound SMS.
5. **Attribution:** `prisma.inviteEvent.findFirst` for the most recent `NUDGE_SENT_AUTO`
   whose `metadata.phoneNumber` equals the normalised number, `orderBy createdAt desc`,
   including the event's `id`, `hostId`, `host.name`. No match → `200 OK`, no action.
6. Finds the `Person` with that `phoneNumber` who is a member of that event.
7. `prisma.smsOptOut.upsert` on `phoneNumber_hostId`, storing `rawMessage`.
8. If a person was found, sets `Person.smsOptedOut` / `smsOptedOutAt`.
9. `logInviteEvent` with type `SMS_OPT_OUT_RECEIVED`.

`GET` returns `200` with the string `SMS webhook endpoint`.

**Authentication: none.** There is no signature check, no shared secret, no allowlist. The
route is not in `middleware.ts`'s `config.matcher` (which covers only `/p/`, `/h/`, `/c/`,
`/api/p/`, `/api/c/`). Anyone who can reach the URL can post a form body and opt an
arbitrary number out of an arbitrary host's sends — provided a matching `NUDGE_SENT_AUTO`
event exists. It is unauthenticated write access to a Do-Not-Touch Zone 7 table.

[[GTC-214]] Ruling 1 is the reason there is no half-measure here: `validateTwilioSignature`
was **deleted, not wired**, and the comment left in `src/lib/sms/twilio-client.ts` says so
and says why. Do not restore it from history.

**Every path returns `200`, including the catch block.** With Twilio that suppressed
retries. With TNZ it means a handler that throws on every request is indistinguishable from
one that works, from TNZ's side and from ours — there is no error signal anywhere except a
`console.error`.

**Also of note:** `/api/sms/inbound` is absent from `route-classifications.json`. That file
holds 79 entries against 102 `route.ts` files under `src/app/api`, so 28 routes are
unclassified and this is one of them. **The absence is staleness, not a ruling** — do not
cite it as evidence the route was assessed and found to need no auth.

### B. Is it Twilio-shaped? Yes. What would have to change

Twilio-shaped in five respects, each of which has to change:

| Today (Twilio) | Required (TNZ) |
|---|---|
| `request.formData()` | JSON body per the MO webhook contract (answer 3) |
| Fields `From`, `Body`, `MessageSid` | TNZ's MO field names — **read the doc, do not guess** |
| No authentication | Verify `X-Timestamp`, `X-Sender`, `Authorization` headers **and** the `Sender` / `APIKey` body fields against the stored shared secret (answer 4) |
| `200` on every path incl. catch | A real status contract per TNZ's retry semantics; a thrown handler must not read as success |
| Doc comment: *"Webhook endpoint for inbound SMS messages from Twilio"* | Accurate, and citing the doc URL |

Two further changes are not format changes but are required by the same rewrite:

- **The `GET` handler** exists for "Twilio's webhook validation". Whether TNZ needs a `GET`
  at all is a question for the doc. If not, delete it rather than leave an unauthenticated
  endpoint answering `200`.
- **Message retention.** Answer 5 says the full message is forwarded even when TNZ has
  already actioned the keyword. Today a non-keyword body is discarded at step 4. Whether
  Gather stores inbound messages is a product question this ticket must *raise*, not settle
  — see *Unknowns*.

**Note the direction of travel.** Because TNZ blacklist at their layer, the handler's
keyword matching is no longer the thing that stops the send. `isOptOutMessage` becomes
Gather's *interpretation* of a message TNZ has already acted on. If the two disagree —
Gather's list is six exact keywords; TNZ's list is theirs and is not documented here — TNZ's
decision wins on delivery and Gather's local state is wrong in whichever direction they
diverge. That divergence is a recorded unknown, not something to paper over by widening
`OPT_OUT_KEYWORDS`.

### C. The phone normaliser — the earlier finding is confirmed at HEAD, and it is worse

`normalizePhoneNumber` in `src/lib/phone.ts` validates through `isValidNZNumber`, whose
pattern is `/^\+64\d{8,10}$/`. Anything not `+64` returns `null`. Measured at `1cbf014`:

| Input | `normalizePhoneNumber` | Note |
|---|---|---|
| `+64211234567` | `+64211234567` | correct |
| `021 123 4567` | `+64211234567` | correct |
| `+61412345678` | `null` | **AU E.164 rejected** |
| `+61 412 345 678` | `null` | AU rejected |
| `61412345678` | `null` | AU rejected |
| `0412345678` | `+64412345678` | **AU local format silently mangled into a bogus NZ number** |

The mangling is the sharper of the two: the leading-`0` branch prepends `+64` before any
validation, so an Australian local-format number does not fail, it becomes a different,
wrong, valid-looking NZ number.

**Why this matters here.** TNZ deliver to AU (answer 1) on an Australian mobile number
(answer 2). `TNZ_COUNTRY_CODES` in `src/lib/sms/send-sms.ts` is `['+64', '+61']`, and
`findDecideByFollowupCandidates` in `src/lib/sms/decide-by-eligibility.ts` deliberately does
**not** call `isValidNZNumber` precisely so `+61` numbers are admitted — its own comment
records this. So Gather sends to AU, and the inbound handler cannot process an AU reply:
step 3 returns `null` and the STOP is dropped on the floor.

Widening the normaliser is **not** in this ticket's scope, and must not be done casually.
`normalizePhoneNumber` has 13 call sites across people, households, contact-info, CSV import
and `reconcileMembers`, plus assertions in `tests/sms-infrastructure-test.ts` and
`tests/moment1-input-test.ts` that pin the current behaviour (`'Non-NZ number returns null'`).
Changing it changes what gets written to `Person.phoneNumber` everywhere. The handler must
therefore either normalise defensively for its own lookup or take TNZ's `From` as already
E.164 — a decision the executor makes with the doc in hand. **File the widening as its own
ticket if it proves necessary.**

### D. How `SmsOptOut` is written today, and by what

The model (`prisma/schema.prisma`): `phoneNumber`, `hostId`, `optedOutAt`, `rawMessage`,
`host` relation to `Person` with `onDelete: Cascade`, unique on `[phoneNumber, hostId]`.
**Opt-out is scoped per host, not per account and not per event.**

**Two production writers, both `upsert`, and neither is reachable in normal operation:**

1. `POST` in `src/app/api/sms/inbound/route.ts` — the handler audited above. Reachable only
   from a Twilio-shaped request, which nothing sends. **Effectively dead.**
2. `manualOptOut` in `src/lib/sms/opt-out-service.ts` — writes `rawMessage: reason || 'Manual
   opt-out'`. **Zero callers.** A repo-wide grep for `manualOptOut` returns only its own
   definition. There is no host-facing UI or API route that opts a number out by hand.

`removeOptOut` in the same file is the only deleter, and likewise has **zero callers**.

**Everything else reads.** `isOptedOut` (called by `nudge-eligibility.ts` and
`proxy-nudge-eligibility.ts`, re-exported by `decide-by-eligibility.ts`),
`getOptOutStatuses` (invite-status), `checkOptOut` and `checkOptOutBatch` in
`src/lib/sms/send-sms.ts`, and direct `findUnique` / `findFirst` reads in the per-person
nudge route and the invite-detail route.

**Conclusion: in production today, nothing writes `SmsOptOut`.** Every consumer reads a table
that only tests populate. `Person.smsOptedOut` is the same story — the inbound handler is its
only writer, while `wrap-up.ts` and `chooseManualNudgeChannel` in
`src/lib/sms/manual-nudge-recipient.ts` gate on it and `PersonInviteDetailModal` displays it.
This ticket is what makes the read side mean something.

---

## Flags

**Rescoped 2026-09-12.** F1 and F3 are resolved or relocated; F2 is closed. What remains a
flag for *this* ticket is F2's successor note only.

### F1 — attribution — RESOLVED BY DOCUMENTATION, and moved to [[GTC-288]]

**This flag asked how an inbound reply finds its person and event, and named the condition
under which the answer would change:**

> *"whether TNZ's MO payload carries anything better — a message reference tying the reply
> to a specific outbound send would change the answer completely. If it does, that is a new
> ticket, not a widening of this one."*

**It does.** The MO documentation, read 2026-09-12, documents `MessageID` in the SMS-reply
payload as *"MessageID parameter supplied when sending your original API call. If you did
not supply one, the API generated one for you."* It is echoed back in both the
delivery-status webhook and the reply webhook.

**Gather already stores it.** `sendViaTnz` in `src/lib/sms/tnz-client.ts` supplies no
`MessageID`, so TNZ generate one; `sendViaTnz` reads it off the send response, and `sendSms`
in `src/lib/sms/send-sms.ts` writes it into `InviteEvent.metadata` as `messageId`. Nothing
has ever read it back. The documented join key has been captured on every send since the
TNZ transport went in.

**Consequences, all [[GTC-288]]'s to act on:**

- Attribution does not need the newest-`NUDGE_SENT_AUTO` heuristic. TNZ state which
  outbound message the reply belongs to; Gather can read their answer instead of
  re-deriving it.
- **The corruption link to [[GTC-258]] is weakened, not merely mitigated.** A handler that
  joins on `MessageID` never consults the newest-wins index, so a false `NUDGE_SENT_AUTO`
  cannot misdirect it. The sequence makes this safe for the case that matters: a guest is
  not blacklisted until they STOP, so the first STOP is a reply to a message that really
  was delivered, resolved before any false stamp exists.
- **H2 substantially closes** — two Gather hosts messaging one guest no longer produces an
  ambiguous attribution, because the reply names the message.
- **H1 does not close.** If the reply is attributed to another TNZ customer, no webhook
  reaches Gather and there is no `MessageID` to read. That is an absence, not a
  mis-attribution, and it has no handler-side mitigation.

**Two residual gaps in the join, both real and both [[GTC-288]]'s:**

- `sendViaTnz` treats a 2xx with an unparseable body as success with `messageId` left
  `undefined`. Those sends store no join key.
- Whether TNZ attribute to the last *accepted* or last *delivered* message is unstated. It
  bears only on a second or later inbound message from an already-blacklisted number.

### F2 — storing a new shared secret — CLOSED, owned by [[GTC-264]]

**Ruled 2026-09-12.** This flag asked how many env vars, their naming, and whether the body
`APIKey` and the header `Authorization` are one secret in two places or two secrets.

**Answered by the example values**, in TNZ's 2026-08-18 answer 4 and confirmed by the
documentation: `X-Sender` and body `Sender` carry the same value; `Authorization` and body
`APIKey` carry the same value. One identity plus one secret, echoed in two places.

**And the work is not this ticket's.** [[GTC-264]] needs a TNZ callback authenticated
against the same dashboard configuration (Users > API User > API > Reporting), and it lands
first. **[[GTC-264]] owns the shared-secret verification and its environment variables;
this ticket consumes them.** That collapses [[GTC-264]] *Unknowns* 2 and this flag into one
question with one answer, and removes a duplicated build.

⚠ **Still binding:** an unset MO secret must **fail closed**. See the correction below — the
house precedent for that now exists and the ticket F2 pointed at was never the right one.

### F3 — per-host versus account-wide opt-out — RULED ACCOUNT-WIDE

**Ruled 2026-09-12. This flag was "report only"; it is now decided.**

The question was whether an opt-out in Gather means *not from this host* or *not from
Gather*. **It means not from Gather.** `SmsOptOut` is unique on `[phoneNumber, hostId]`;
TNZ's blacklist belongs to the account.

**The argument that carries it:** TNZ's blacklist is account-wide, so a per-host opt-out
leaves every other host sending messages that are **guaranteed to fail**, while
`canReceiveSms` on `PersonInviteStatus` goes on reporting the guest as chaseable. That is
the board lying to the host, which is the failure the invite-status screen exists to avoid.

A second argument points the same way: attribution can be wrong (H1, H2), and a *wrong*
per-host opt-out is worse than a *right* account-wide one.

**Implementation is [[GTC-288]]'s, and it carries schema** — the unique constraint and the
required `hostId` both encode the per-host assumption. **Stop Condition 7 fires there.**
This ticket records the ruling; it does not act on it.

### ⚠ [[GTC-220]]'s pull-forward — RULED, THEN WITHDRAWN ON EVIDENCE, 2026-09-12

**Recorded as a withdrawn ruling rather than silently dropped**, because the reasoning that
produced it was sound and the finding it turned up is the more useful half.

**What was ruled, and why it was withdrawn.** [[GTC-220]] was ruled forward on the
reasoning that this ticket and [[GTC-264]] independently need a fail-closed secret check,
and that deciding once beats two executors each stopping at the same place. The reasoning
holds. **The premise did not survive verification against the tree at `d1577b7`**, and the
ruling was withdrawn the same day, before anything was built on it.

This ticket's F2 and [[GTC-264]]'s *auth shape* section both described the three cron routes
as guarding with `if (CRON_SECRET && providedSecret !== CRON_SECRET)` — an unset secret
leaving the route open — and both attributed the fix to [[GTC-220]]. **Both halves of that
are wrong, and were already wrong when those tickets were written against `1cbf014` and
`5df28ba`.**

- **The fail-open is fixed.** [[GTC-270]] closed it at `bf06cc1`, status `fixed`. All three
  cron routes now call `isCronSecretConfigured` in `src/app/api/cron/cron-secret.ts` and
  answer `401` to every caller when the secret is unset. `GATHER-BUILD-CONSTANTS.md` records
  this under *Async Trigger Methods*.
- **[[GTC-220]] was never that ticket.** It is a *transport* change — move `CRON_SECRET`
  from the `?secret=` query param to an `Authorization: Bearer` header, with a coordinated
  EasyCron config change. Severity Low. It has nothing to do with failing closed, and
  neither this ticket nor [[GTC-264]] needs it.

**So there is nothing to pull forward.** The requirement both tickets correctly identified —
an unset callback secret must fail closed — is already satisfied by house precedent rather
than blocked behind a ticket. **Follow [[GTC-270]]'s shape.**

**⚠ AND THE WITHDRAWN RULING PRODUCED THE MORE USEFUL FINDING — this is the part to carry
forward.** Reading [[GTC-270]] to check the premise turned up a constraint that bears
directly on both TNZ endpoints and appears in no ticket:

⚠ **The route scanner will not recognise a new secret, and this is the finding to act on.**
Verified directly against `tests/security-route-scan.ts` at `d1577b7` rather than taken from
any ticket's description of it — see the correction note below.

**`SHARED_SECRETS` in `tests/security-route-scan.ts` is exactly `new Set(['CRON_SECRET'])`.**
It is the only env var the scanner treats as a shared secret: `absValue` compares a
`process.env.X` access against that set and returns `ABSENT` only for a member. **A TNZ
callback secret will not be recognised as a credential at all** — not because of where the
check lives, but because the scanner has never heard of the variable.

**Consequence for this ticket and for [[GTC-264]], which both update
`route-classifications.json`:** a route whose credential the scanner cannot see is a route
about to be classified wrongly, in the very file that exists to record how it is guarded.

⚠ **`tests/security-route-scan.ts` is Do-Not-Touch Zone 6.** Adding the new secret to
`SHARED_SECRETS` *strengthens* the contract rather than weakening it, which is what Zone 6
forbids — but it is still a Zone 6 edit and needs saying out loud rather than doing in
passing. **Raise it, get it ruled, then do it.**

**Two structural constraints, both verified at `d1577b7`:**

- **Cross-file resolution is one level and relative-only.** `MAX_MODULE_DEPTH = 1`, and its
  comment attributes this to [[GTC-273]] blind spot 2. So a verification helper reached by a
  **relative** import one level away is followed; one reached through the `@/lib/...` path
  alias is **not**. [[GTC-264]] owns the shared credential, so where that helper lives and
  how it is imported decides whether the scanner can see the check at all.
- **Local helper depth is bounded** at `MAX_HELPER_DEPTH = 3`.

⚠ **CORRECTION — [[GTC-270]]'s stated reason is superseded, and its cited symbol no longer
exists.** `src/app/api/cron/cron-secret.ts` documents why the DECISION lives in a pure
exported predicate while the refusing `if` stays in each route file, and gives as the reason
that [[GTC-268]]'s scanner *"follows local helpers within a file and does NOT follow
imports"*, naming `collectSharedSecret` in `tests/security-route-scan.ts`. At `d1577b7`
**that symbol does not exist**, and [[GTC-273]] (`b50dffc`, fixed) has since added one-level
relative-import resolution. The measured outcome [[GTC-270]] records — a helper that
swallowed the whole check made the scanner report all six cron handlers as *"no credential
of any kind"* — stands as history, and keeping the refusing `if` in the route file remains
the safe shape. **But do not cite `collectSharedSecret`, and do not repeat "does not follow
imports" as a current fact.** Read the scanner.

⚠ **And note the tension with [[GTC-264]] owning the credential (F2).** The credential
*verification* is shared; the *refusal* must stay visible in each route file. Getting that
split wrong in either direction either duplicates the credential or blinds the scanner.


## Scope

**This is the authentication half only.** The parse half is [[GTC-288]].

**Fix here:**

- `src/app/api/sms/inbound/route.ts` — shared-secret verification, fail-closed, and a real
  response contract. Released from [[GTC-214]]'s "do not touch, that is GTC-229, blocked on
  TNZ" by this filing.
- `route-classifications.json` — add the route with its real auth type. Note the standing
  staleness: an absence there is staleness, not an assessment.
- A test asserting the route rejects an unauthenticated request, rejects a wrong secret, and
  fails closed with the secret unset.

**Consumed, not built here:** the shared-secret configuration and its environment variables
are **[[GTC-264]]'s** (F2). If [[GTC-264]] has not landed, stop — do not build a second
credential path.

**Do not touch:**

All Do-Not-Touch Zones in `GATHER-BUILD-CONSTANTS.md` are binding. Zone 7 (`SmsOptOut`,
`Person.smsOptedOut`) is **not written by this ticket at all** — that is [[GTC-288]]'s.
Additionally:

- **The MO payload parse, attribution, and opt-out sync** — [[GTC-288]]'s, all of it.
- **`normalizePhoneNumber` / `isValidNZNumber`** in `src/lib/phone.ts` (audit C). 13 call
  sites and two test files pin current behaviour. Separate ticket.
- **`sendSms`, `shouldUseTnz`, `TNZ_COUNTRY_CODES`, `sendViaTnz`** — outbound is settled.
- **`validateTwilioSignature`** — deleted by [[GTC-214]] Ruling 1. Do not restore it.
- **The cron routes** — already fail closed since [[GTC-270]]. Read
  `src/app/api/cron/cron-secret.ts` as the precedent to follow; do not modify it.
  [[GTC-220]]'s query-param-to-header transport change is unrelated and is not a
  prerequisite.

---

## Acceptance

**Test gate** — Severity High: test-first required. RED before any fix, GREEN after, both
pasted into Evidence. **Stop Condition 6 is live and specific: a test that reaches TNZ is a
failed test.** Drive the handler with fabricated MO payloads only.

**Assertions**

- [ ] A request with no shared secret is rejected, and writes nothing.
- [ ] A request with a wrong secret is rejected, and writes nothing.
- [ ] A request whose headers verify but whose body `Sender` / `APIKey` do not is rejected.
      The credential arrives in both places (F2); both are checked.
- [ ] With the secret env var **unset**, the route fails closed — every caller refused.
      Contrast the cron shape, which admits everyone when unset.
- [ ] A handler that throws does **not** answer `200`. A thrown handler must not be
      indistinguishable from a working one.
- [ ] The route's success and failure status codes are **asserted and recorded** against
      TNZ's documented retry behaviour — failures retried every five minutes for up to 24
      hours. A permanent rejection that TNZ will retry for a day is a design error; so is a
      transient failure answered in a way that discards the message.
- [ ] `GET` is resolved explicitly — kept with a stated reason, or deleted. It must not be
      left as an unauthenticated endpoint answering `200` by inertia.
- [ ] Adjacent flows unaffected: outbound `sendSms` routing, the nudge scheduler, wrap-up
      dispatch, and the decide-by follow-up sweep.

**Evidence package**

- RED/GREEN test output
- Assertion checklist confirmed
- Security suite result
- Commit hash

---

## Unknowns

**Unknown 1 is CLOSED. This ticket has no blocking unknown.**

1. **~~The MO payload shape itself.~~ RESOLVED 2026-09-12** by reading
   `https://www.tnz.co.nz/Docs/RESTAPI/?version=2.04#receive-webhook`. The page is static
   HTML and is not thin. Method `POST`; Content-Type `application/json` or `text/xml`; the
   authentication headers exactly as TNZ mailed them; the full payload field list; and a
   documented retry contract — **failures retried every five minutes for a maximum of 24
   hours** on a non-2xx. Transcribed in
   `docs/05_ops/tnz-inbound-and-delivery-correspondence-2026-08.md`.
   **⚠ Two things the page does not give, and they are DECISIONS THIS TICKET MAKES AND
   RECORDS — not guesses, and not omissions.**

   **(a) The expected success status code.** The page documents the retry contract but not
   what it wants back. **Read those two facts together:** failures are retried **every five
   minutes for up to 24 hours**, so an endpoint that answers a successful call with a status
   TNZ reads as failure will have that call **retried for a day** — 288 duplicate deliveries
   of a message Gather already processed. The inverse is as bad: a genuine failure answered
   with something TNZ read as success is a STOP dropped with no retry and no trace. **The
   status contract is not a detail; it is the difference between idempotency pressure and
   silent loss.** Decide it, record the reasoning, and assert it.

   **(b) Clock-skew tolerance on `X-Timestamp`.** The header is presented on every callback
   and nothing documents how much skew TNZ tolerate or expect. Whether this route enforces a
   replay window is an authentication decision, so it is this ticket's. **Decide it and
   record it; do not leave it unconsidered.** Note that a replay window interacts with the
   24-hour retry: a window tight enough to be useful against replay may reject TNZ's own
   legitimate retry of a message first sent hours ago.
2. **TNZ's keyword list vs Gather's** — **moved to [[GTC-288]]**, which owns keyword
   matching. Re-asked of TNZ 2026-09-12, outstanding.
3. **~~How a blacklisted send actually fails.~~ ANSWERED 2026-08-31.** Confirmed defect,
   filed as [[GTC-258]], which depends on [[GTC-264]]. **No longer bears on this ticket**,
   and since the 2026-09-12 documentation read it bears less on [[GTC-288]] than F1
   originally feared — see F1.
4. **Whether Gather stores inbound messages at all** — **moved to [[GTC-288]]**.

**The replay question stays here** — it is Unknown 1(b) above, and it is an authentication
decision, so it does not travel to [[GTC-288]] with the rest of the parse work.

---

## Stop Conditions

Standard eight from `BUG-TICKET-TEMPLATE.md`. Live notes:

- **8 (required unknown surfaces):** **no longer pre-fired.** Unknown 1 is closed by the
  documentation read of 2026-09-12.
- **6 (real-world side effects):** binding. No test may reach TNZ. Fabricated payloads only.
  ⚠ Note that `scripts/test-tnz-sms.ts` performs a **live send** and must not be wired into
  any suite.
- **5 (scope expansion):** the MO parse and opt-out sync ([[GTC-288]]), the normaliser
  widening (audit C), the credential build ([[GTC-264]]), and inbound message retention are
  all named and excluded. If any becomes load-bearing → stop.
- **7 (schema migration):** **does not fire on this half.** It fires on [[GTC-288]], where
  the account-wide ruling (F3) meets a `[phoneNumber, hostId]` unique constraint.
- **Sequencing:** [[GTC-264]] lands first and owns the credential. If it has not landed,
  stop rather than building a second credential path.

---

## Executor Checklist

[ ] `GATHER-BUILD-CONSTANTS.md` read
[ ] Citations section read; every citation in this ticket is a symbol, not a line number
[ ] Preflight passed and results pasted
[ ] The split banner and the two carried-forward records at the top read and understood
[ ] `docs/05_ops/tnz-inbound-and-delivery-correspondence-2026-08.md` read — it is the source
[ ] [[GTC-264]] confirmed landed, and its credential consumed rather than duplicated
[ ] Plan documented (hypothesis, change set, test strategy, rollback risk)
[ ] RED test confirmed
[ ] Fix implemented within declared scope — authentication only
[ ] Zone 7 NOT written by this ticket
[ ] GREEN confirmed
[ ] Acceptance assertions checked
[ ] Adjacent flows verified
[ ] `route-classifications.json` updated
[ ] Scanner recognition of the callback secret confirmed or its absence recorded — see the
    [[GTC-220]] withdrawal note; `SHARED_SECRETS` holds only `CRON_SECRET`
[ ] Evidence package assembled
[ ] Commit hash recorded
[ ] No stop conditions triggered without resolution

---

## Evidence (Executor-Completed)

> Not started. This ticket is filed, not executed.

**Root cause confirmed**
[pending]

**Files changed**
[pending]

**Test results**
- RED: [pending]
- GREEN: [pending]

**Assertions checked**
[pending]

**Security suite:** [pending]

**Commit hash:** [pending]
