---
ticket: GTC-166
title: "Correct gather-domain-reference and gather-frontier-and-roadmap skills against Moment 4 + Hinge specs"
status: closed
branch: feat/moment-one-redesign
commit: 3e24048
moment: 4
type: docs
depends_on:
  - "[[GTC-163]]"
  - "[[GTC-165]]"
blocks: []
tags: [moment-4, hinge, docs, skill-correction]
---

# [[GTC-166]] — Correct stale Moment 3/4/freeze framing in two skills — CLOSED

**Status:** Complete | **Branch:** feat/moment-one-redesign | **Commit:** `3e24048`

**Severity:** Medium (living doctrine correction — these skills are loaded by every future session touching this territory; stale framing compounds)
**Type:** Chore (docs/skill). No code or schema changes.
**Surfaced by:** [[GTC-163]]'s superseded-reference scan, explicitly deferred to "the discovery pass (its Task 4)"; executed here as directed by Nigel, 2026-08-03, alongside [[GTC-165]].

---

## What this ticket does

Applies corrections to `.claude/skills/gather-domain-reference/SKILL.md` and
`.claude/skills/gather-frontier-and-roadmap/SKILL.md` wherever their text asserted or implied
that Moment 3/4 product decisions, the freeze model, or freeze-derived metrics were still open,
undecided, or a stable permanent target — all now superseded by `gather-moment-4-spec-v1.md` and
`gather-hinge-spec-v1.md` (GTC-163, updated by GTC-164; no open questions remain in either).

Two skills were explicitly **not** touched by scope, per the original discovery brief and
confirmed against the repo:
- `gather-architecture-contract/SKILL.md` §5 — an accurate, current description of the live
  `EventStatus`/FROZEN code. Rewriting it now would document architecture that hasn't changed
  yet. It gets a full rewrite once Epic A (`docs/tickets/GTC-167.md` onward) actually lands the
  send-lock model — not before.
- `gather-v1-v2-reconciliation-campaign/SKILL.md` — checked directly (grep for "freeze",
  "FROZEN", "Phase 3/4/5"); no freeze-ceremony framing tied to its phases, nothing to correct.

## Corrections applied

### `gather-domain-reference/SKILL.md`

| Location | Before | After |
|---|---|---|
| "Host pain removed" bullet | "Gather turns the plan into an external, confirmable state that can **freeze** into shared reality." | "...that **locks at the send** into shared reality" — cites Hinge §2, cross-refs the Moments table note |
| Moments table, row 3 ("Who's bringing what?") | No spec-status note; `Assignment.response` listed with no target-model note | Added: design CLOSED per Hinge spec + Moment 4 §6; target model is yes/no/maybe with attendance inferred (Hinge §3), current RSVP/Assignment-response pair flagged as superseded-not-migrated |
| Moments table, row 4 ("Is everyone sorted?") | "Today: V1 invite-status/reachability views + freeze"; data column listed `EventStatus`/`PlanSnapshot`/freeze warnings with no forward note | Added: design CLOSED per Moment 4 spec (no open questions); data column now states current vs. target explicitly, target being the send as lock point + why/version model, reconciliation = Epic A |
| Key facts: "Moments 3 & 4... open roadmap" | Read as if product decisions were still open | Clarified: open in the *build* sense only; product decisions CLOSED 2026-08-03 with all sixteen discovery-gap questions ruled; pointer to the discovery report |
| Key facts: "Frozen = the psychological finish line" | Asserted FROZEN as the settled emotional/architectural pivot | Corrected: the SEND is now the specified pivot/lock point (Hinge §2); FROZEN is marked for reconciliation (Moment 4 §7, Epic A); COMPLETE survives but becomes calendar-triggered (§10.1); added a consolidated forward-pointing note covering the RSVP/Assignment-response table, the compliance-threshold bullet, and the freeze-gating section below — none of those three needed content changes since they're still accurate about current code, but each now carries this pointer rather than being silently left to look like settled architecture |

### `gather-frontier-and-roadmap/SKILL.md`

| Location | Before | After |
|---|---|---|
| Jargon table, "Freeze" entry | Defined only as the current code transition | Added: slated for reconciliation into the send-lock/ledger model, cites Epic A |
| Epic 4 bullet ("Freeze Enhancements") | "surgical edit while frozen" listed as a plain roadmap item | Added note: this shape (exception-carved-out-of-locked-state) is superseded by the Hinge §2 model (editable-with-scoped-why + universal versioning); revisit alongside Epic A |
| Epic 6 bullet ("Metric Instrumentation") | "frozen-rate metric" listed with no caveat | Flagged `[SUPERSEDED TERM]`; added a consolidated note that "frozen rate" has no referent once FROZEN dissolves and needs redefinition against the send event — covers the term's five occurrences across §§1-3 without mechanically rewriting each (Epic 6 is already GENUINELY OPEN with zero code built, so this is a definition update at ticket time, not a live-code correction) |
| Section C, "Epic 6 metric instrumentation" current-state line | No pointer to the new specs | Added a one-line pointer back to the Section 1 note |

## Why some flagged locations were left as accurate current-code descriptions

GTC-163's scan also flagged `gather-domain-reference` lines describing the RSVP/Assignment-response
table (`Assignment.response`... "blocked while FROZEN"), the compliance-threshold bullet
(`<80% triggers a LOW_COMPLIANCE freeze warning`), and the "what conflicts gate" section
(CONFIRMING→FROZEN warnings-only behavior, GTC-154). All three are still **true of the code as it
exists today** — rewriting them now to describe a send-lock model that doesn't exist yet would
make the skill wrong in the other direction. Each is now reachable from the single consolidated
forward-pointing note added to the "Frozen = psychological finish line" correction above, which
states plainly that FROZEN-keyed claims elsewhere in the skill describe pre-reconciliation code.
Same treatment as `gather-architecture-contract` §5: correct now would be premature; a full
rewrite is owed once Epic A actually ships.

## Preflight note

Preflight sanity sequence not run — this ticket touches only two files under `.claude/skills/`,
both markdown. No source, schema, config, or dependency is affected. `.prettierignore` excludes
`*.md`, so the pre-commit hook is a no-op on these files.

## Assertions met

1. ✅ Both skills read in full before editing; corrections cross-checked against GTC-163's
   superseded-reference table AND verified directly against current file content (not assumed
   from the table alone — line numbers shift).
2. ✅ Every location GTC-163 flagged for these two files was either corrected or deliberately
   left with a rationale recorded (the three "still accurate about current code" locations,
   consolidated under one forward-pointing note rather than individually rewritten).
3. ✅ `gather-architecture-contract` and `gather-v1-v2-reconciliation-campaign` confirmed out of
   scope, with reasoning recorded.
4. ✅ No code, schema, or non-skill doc touched by this ticket.
5. ✅ One commit containing the ticket + both skill edits together.
6. ✅ Branch verified: `feat/moment-one-redesign`.

## Evidence

- Diffs applied via targeted edits to both `SKILL.md` files (see table above for before/after).
- Cross-check source: `docs/tickets/GTC-163.md` "Known-superseded references" table, section A.
- `gather-v1-v2-reconciliation-campaign/SKILL.md` grep for `freeze|FROZEN|Phase 3|Phase 4|Phase 5`
  — no matches tying freeze framing to its phases; confirmed out of scope.
- Commit: `3e24048` — ticket + both skill edits together, one commit (executor output contract).
