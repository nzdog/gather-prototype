---
ticket: GTC-160
title: "Wire client personEventId into household edit payload (completes GTC-159) + backfill campaign-skill corrections"
status: open
branch: feat/moment-one-redesign
commit:
moment: 1
type: bug
severity: high
depends_on:
  - "[[GTC-159]]"
blocks: []
tags: [moment-1, households, data-loss, v1-v2-reconciliation, handoff-sonnet]
---

# GTC-160 — Client personEventId wiring (completes GTC-159)

> **Handoff ticket for a Sonnet session.** GTC-159 fixed the server seam
> (`reconcileHouseholdMembers`) but its benefit reaches no-email members
> (kids, partners — the common case for a family-event app) only once the
> client sends each member's `personEventId`. This ticket is the completion
> of GTC-159, not polish. Zero frontier cost — executable on a cheap model.

## Why this is the real completion
`PUT /api/events/[id]/households/[householdId]` now reconciles non-primary
members by `personEventId`. If the client omits it:
- members **with email** are still preserved (matched via the
  `(personId,eventId)` upsert branch in `createNewMember`);
- members **without email** (Matt/kids) fall through to create → the old
  PersonEvent is then deleted as "removed" → team/rsvp/nudge loss persists
  for exactly the population most events care about.

The GET `/api/events/[id]/households` response **already returns** each
member's PersonEvent id as `member.id` (verified) — no server GET change
needed. This is pure client plumbing.

## Scope — the wiring sites (all in the client)
1. `src/app/plan/[eventId]/page.tsx` — `apiHouseholdToSaved`: capture the
   PersonEvent id for each non-primary slot (`partnerMember.id`, each helper
   `m.id`, each guest `g.id`) into a new `personEventId` field.
2. `src/components/plan/HouseholdCardList.tsx` — `SavedHousehold`: add
   `personEventId?: string` to `partner`, `helpers[]`, `guests[]`.
3. `src/components/plan/Moment1InputForm.tsx` — `Moment1PersonInput`: add
   `personEventId?: string` to `partner`, `helpers[]`, `guests[]`.
4. `Moment1InputForm.tsx` edit-mode init: when populating the form from
   `editingHousehold`, thread each member's `personEventId` into the form's
   internal per-member state (the local `GuestForm.id` is a UI key, NOT the
   PersonEvent id — keep them separate).
5. `Moment1InputForm.tsx` — `buildPayload()`: include `personEventId` for
   partner/helpers/guests when present (edit mode). Add-new keeps it absent.
6. Route body types are already done in GTC-159 (`HouseholdMemberInput.personEventId`).

Primary contact needs NO id — it is matched server-side by `householdRole`.

## Acceptance
- [ ] End-to-end: the existing 9 assertions of
      `tests/household-edit-preserves-membership-test.ts` hold when driven
      through the **real PUT payload** the wired client produces (extend the
      test, or add an integration variant that posts the client-built body).
- [ ] Moment 1 browser walk: seed via `npx tsx scripts/seed-gtc-133-test-event.ts`,
      assign a no-email member (e.g. Matt) to a team, edit that household
      (change primary phone), save, refresh → member stays on team, RSVP
      intact, no duplicate Person. (This is the GTC-159 adjacent check.)
- [ ] `tsc --noEmit` clean · `format:check` clean · `test:security` 16/16.

## Also in this ticket — backfill campaign-skill corrections
Correct `.claude/skills/gather-v1-v2-reconciliation-campaign/SKILL.md` from
GTC-159's authoritative source read (the skill was written from a brief and
is wrong on mechanism):
- **D1 (mechanism):** the Phase-1 defect table frames the bug as "createMember
  recreates … never sets teamId". The real root cause is the **delete-then-
  recreate ordering** — `personEvent.deleteMany` runs *before* `createMember`,
  which defeats the `(personId,eventId)` upsert branch that already existed in
  `createMember`. Outcomes in the table are correct; the mechanism/fix framing
  is not. Note that the fix (GTC-159) removed the blanket deleteMany rather
  than "adding" an upsert.
- **D2 (provenance):** "deleteMany was :45,:156" lumps two handlers — `:45` is
  the DELETE handler (whole-household delete, correct/intended cascade); only
  `:156` in PUT was the data-loss bug.
- **Cite-by-symbol:** the skill still uses line numbers in places; the new
  house rule is symbol names (GTC-158 re-anchored the skills once already).

## Stop conditions
Standard set. No schema change. No auth/Stripe/SMS. If the edit-form state
threading (site 4) turns out to require a broader form-state refactor, STOP
and report before expanding scope.
