# GTC-035 — Plan/Generate: Guided Builder breadcrumbs show selected item instead of level name

**Type:** UX
**Severity:** P3
**Status:** Complete

## Evidence (Executor-Completed)

### What was built
Replaced selected-item values in Guided Builder breadcrumbs with short labels derived from each level's question text. Added `shortenLevelQuestion()` helper that extracts the key noun from question patterns (e.g. "Any specific dishes?" → "Dishes", "What style of mains?" → "Mains"). Changed the breadcrumb loop to iterate all intermediate levels (not just the previous one), so breadcrumbs scale correctly beyond 3 levels. The current level renders as `text-gray-700 font-medium` (visually distinct). Jump-back navigation preserved on all intermediate crumbs.

### Files changed
- `src/components/plan/GuidedPlanBuilder.tsx` — added `shortenLevelQuestion()` + `capitalize()` helpers; rewrote breadcrumb intermediate-level logic in `renderBreadcrumbs()`

### Test results
```
GREEN: Security suite 16/16
```

### Assertions checked
- [x] Breadcrumbs show level labels not selected values
- [x] Current level visually distinct (font-medium, darker color)
- [x] Jump-back navigation still works (onClick handlers on all intermediate crumbs)
- [x] Quick Generate path unaffected (separate modal path, doesn't use GuidedPlanBuilder)
- [x] Security suite: 16/16

### Commit hash
[pending — not yet committed]
