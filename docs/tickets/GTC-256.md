---
ticket: GTC-256
title: "Moment-flow events have no host PersonEvent — the host is a stranger in their own event"
status: open
branch: TBD
commit:
moment: 1
type: bug
depends_on: []
tags: [moment-1, household-capture, identity, host, latent, gtc-188-followup, ruled]
---

# GTC-256 — Moment-flow events have no host PersonEvent

**Status:** Open — RULED, not started | **Branch:** TBD | **Commit:** [pending] | **Effort:** TBD

⚠ **THE FILING INSTRUCTION IS DISCHARGED.** This ticket was filed under an explicit founder
instruction to propose no fix, because the shape of the fix was a capture decision. That
decision was made on 2026-08-29 — see *Founder rulings — the host's identity* below. **Nine
rulings** fix the direction (1-6 in the first pass, 7-9 in the second, same day); **three**
questions are held open and are named as held open. The original filing text is preserved
unedited above the rulings so the ticket still reads as it was filed.

*Filed as bookkeeping during [[GTC-188]] (I1), which surfaced this while implementing founder
Ruling 3 (2026-08-29). Pre-existing, NOT caused by I1. The founder's instruction on filing it
was explicit: state the consequence, note what closing it would touch, and **do not propose a
fix** — the shape of the fix is a capture decision, not an executor's.*

### Scope, one line

On events created through the Moment flow the host has **no `PersonEvent` at all**, and the
person the host captures as themselves during Moment 1 is a **separate `Person` row with no
link back to the host `User`**.

⚠ **THE SCOPE LINE IS TOO NARROW — corrected by the Phase 1 audit, 2026-08-29.** See *Scope
correction* below. This is not a Moment-flow bug. It is an event-creation bug that the Moment
flow adds a second half to.

### The evidence

Measured across every event in the development database on 2026-08-29:

| Event | Path | `PersonEvent` for the host Person | `PersonEvent` with `role: HOST` |
|---|---|---|---|
| Henderson Family Christmas 2026 | V1 seed | 1 | 1 |
| Security Test Event (DRAFT / SENT / PAST / SENT-LEGACY) | V1 | 0 | 0 |
| GTC-133 Sub-commit (g) Test | **Moment flow** | **0** | **0** |
| GTC-236 Regenerate Fixture | **Moment flow** | **0** | **0** |

`PersonEvent` rows for the host `Person` **across all events**: **0**.

⚠ **THAT LAST LINE IS WRONG AS WRITTEN — it is 1, and this ticket's own table above says so.**
Re-measured 2026-08-29: `PersonEvent` rows whose `personId` is any `Event.hostId` = **1**
(Sarah Henderson, on the demo seed). What is genuinely 0 is the count for the two *Moment-flow*
host Persons — "Test Host" and "Security Test Host" — across every event. The same error is
copied into a code comment; see *The wrong comment* below.

Concretely, on `GTC-133 Sub-commit (g) Test`:

- `Event.hostId` → `Person { name: "Test Host", email: "nigel@mckorbett.co.nz", userId: <User> }`.
  That Person has no membership row on this event, or on any event.
- The guest list contains `Person { name: "Kate", email: "kate@example.com", userId: null }`,
  captured through Moment 1 as a household primary contact. Nothing in the data connects Kate
  to the host — not `personId`, not `role`, not `userId`.

Note `Event.hostId` is a **`Person`** FK (`host Person @relation("EventHost")` in
`prisma/schema.prisma`), not a `User` FK. The two identities are joined by `Person.userId`,
which is null on every person the Moment flow captures.

### The consequence, beyond auto-assign

Auto-assign is where this was noticed, and it is the least of it. Auto-assign's host exclusion
was hardened by [[GTC-188]] to resolve the host by three identity paths (`personId`,
`role: HOST`, shared `Person.userId`) and is null-safe and tested — but on a Moment-flow event
**all three paths find nothing, because there is no row to find**.

The general statement: **the host appears in their own event as a stranger, and a duplicate of
them sits in the guest list.** Anywhere the system needs to know which of these people is the
host inherits this:

- **Who not to message.** Nudges, the decide-by follow-up and the proxy path all reason over
  `PersonEvent`. The captured duplicate is an ordinary participant and is a live recipient.
- **Who not to chase.** The per-person `nudgeMark` and the household channel picker
  ([[GTC-172]], [[GTC-179]], surfaced by [[GTC-188]]) offer the host to herself as somebody to
  go gentle on.
- **Whose name goes on the invitation.** The from-whom of a send has two candidate rows and no
  rule distinguishing them.
- **Who is excluded from placement and distribution.** Any current or future "not the host"
  filter, including auto-assign's.
- **Headcount and coverage.** The host is counted as a guest by anything that counts
  `PersonEvent` rows.

### Not in scope, and why this is not small

Closing this is a **Moment 1 capture decision**: it requires ruling how a captured person is
identified as the host — whether the flow creates a membership row for the host, whether the
host is captured as a household member and then linked, what happens to events already in this
state, and what the identity of record is when a `Person` and a `User` disagree. It touches
household capture, which is load-bearing and has its own incident history
(`reconcileHouseholdMembers`, [[GTC-159]], [[GTC-172]]).

**It is a bigger piece than the rest of [[GTC-188]] combined.** No fix is proposed here, per the
founder's instruction on filing.

---

### Founder rulings — the host's identity (Nigel, 2026-08-29)

*Nine rulings made after a Phase 1 audit of every event-creation path, every host-identity
consumer, and the live development database. Rulings 1-6 came first; **Rulings 7-9 were made the
same day, after the audit reported the delete asymmetry and the role/items collision**, and
close open question 2. They supersede the filing instruction directly above. Three questions
remain deliberately NOT ruled and are listed after them — an executor must stop at those, not
interpret them.*

**Ruling 1 — Moment 1 opens with the host's own household.** Of the candidates weighed
(A: write a host `PersonEvent` at event creation; B: link the captured `Person` back to the
host `User`; C: recognise the host at read time and store nothing; D: capture the host as a
household member in Moment 1), **D is ruled.**

*Rationale, recorded because it is the reason and not a preference:* it is the only candidate
that makes the system's picture match reality. **The host is at her own party.** A and C leave
her absent from her own guest list — A adds a row that names her without placing her, C finds
nothing to name. B needs a rule for detecting which captured person is the host, and no such
rule exists: the only automatic candidates are name-matching and email-matching, and
email-matching produces the third state recorded below instead of a link.

**Ruling 2 — the host's household is optional.** Moment 1 offers **"I'm hosting alone"** as an
explicit choice. Taking it makes her a **household of one, not absent** — she is still eating.

The distinction is the whole ruling: "no household" and "a household containing only me" are
different facts, and only the second one survives Ruling 3. A host who declines to name anyone
else must still appear in the headcount, hold items, and be excluded from her own ask.

**Ruling 3 — the host counts in the headcount.**

*Consequence, recorded rather than left to be discovered:* **every existing event's plan was
sized one adult short.** `buildPlanGenerationInput` derives `totalAdults` by counting household
members (`src/lib/ai/plan-input.ts`, the "Headcount from households" block). The host is in no
household, so every plan generated to date fed everyone except the person cooking. This is not
a rounding error in a quantity — it is a missing person in the input the whole plan is scaled
from.

**Ruling 4 — the host may hold items.** She holds them **silently**: they appear on her own
plan rather than arriving as an ask.

This is the ruling that makes Ruling 5 non-trivial. "Holds items but is never messaged about
them" is a combination the system has no existing shape for.

**Ruling 5 — the host never receives her OWN ask.** No invitation, no auto-nudge, no proxy
nudge, no decide-by follow-up, no wrap-up thank-you. Her name is **not claimable through the
shared link**.

⚠ **"IN THE GUEST LIST, COUNTED, HOLDING ITEMS, NEVER MESSAGED" DOES NOT EXIST TODAY.** It is a
new concept, and it must be built as one.

The only existing never-message rule is **the child rule** (Moment 4 §10.6,
`src/lib/eligibility/child-exclusion.ts`). It is deliberately narrow and absolute, and
[[GTC-207]] pins it against exactly this kind of borrowing — the same file's own docstring
forbids importing `MESSAGEABLE_PERSON_EVENT` into any assignment path. **It must not be widened
to carry Ruling 5.** A `CHILD`-role host would also be barred from holding items, which
contradicts Ruling 4, and from being a household channel, which contradicts Ruling 6.

Note also that **`PersonRole.HOST` carries no messaging meaning today.**
`MESSAGEABLE_PERSON_EVENT` reads `householdRole` and nothing else. Giving the host a
`role: 'HOST'` row makes her *findable*; it does not make one send path stop. Ruling 5 must
hold across **all fourteen sites with no host exclusion** enumerated below, not four of them.

**Ruling 6 — the host may be her household's contact, and chooses whether those messages
send.** This is **separate from Ruling 5**. Her own ask is suppressed by *what she is*. Her
household's messages are *a setting she controls*.

**Ruling 7 — the host is always the `PRIMARY_CONTACT` of whatever household she is in.**

This closes the delete-eligibility risk **by construction rather than by guard**:
`reconcileHouseholdMembers` computes `existingNonPrimary` as
`members.filter(m => m.householdRole !== 'PRIMARY_CONTACT')` and the delete loop iterates only
that list, so a primary is updated in place and can never be deleted by an ordinary household
edit. No guard is needed, and none should be added.

*Cost, recorded:* **she cannot join an existing household as a `PARTNER`.** If she is in her
partner's household, **she is the primary and the partner is the partner.**

⚠ *Consequence that promotes Ruling 6 — see "What Ruling 7 does to Ruling 6" below.* A
`PRIMARY_CONTACT` is her household's **default proxy channel**, because
`resolveHouseholdChannel` falls back to the primary on a null pick. Ruling 7 therefore makes
Ruling 6's switch default-ON rather than default-off.

**Ruling 8 — the host's `PersonEvent` carries `role: HOST`.** She gets **no `PARTICIPANT`
token** and **no guest-side page**. She is not being invited, so she does not need one.

This resolves open question 2, which is struck below. It does **not** contradict Ruling 4 — see
Ruling 9.

**Ruling 9 — the host holds items by an explicit toggle she controls.** **Off by default.** The
system never assigns her anything: **the existing rule keeping `role: HOST` out of auto-assign's
`PARTICIPANT` pool stays exactly as it is, unchanged.** She opts in and chooses her own items.
Items she holds appear on **her host view**. She gets no guest view.

*Verified against the code, 2026-08-29 — Ruling 9 needs no eligibility change:*
`POST /api/events/[id]/items/[itemId]/assign` has **no role gate at all**, deliberately
([[GTC-207]] keeps role gates out of that route), so a `role: HOST` row is assignable today.
And `/api/h/[token]` already returns every team's items with their named assignee, so **"appear
on her host view" costs nothing to build.** The one real constraint is team membership, not
role — see the phasing.

#### An observation, NOT a ruling

The host now has **three self-directed switches**: whether she has a household (Ruling 2),
whether her household's messages send (Ruling 6), and whether she holds items (Ruling 9). **All
default off.**

Whether they belong together as one control in Moment 1 or three scattered ones is **a design
question for the build, not settled here.** Recorded so the build sees them as a set rather than
discovering them one at a time.

⚠ One caveat on "all default off": Ruling 7 makes the *messages* switch default **on** in its
current mechanism, because a null channel pick resolves to the primary contact. The switch's
default and the ruling's intent disagree, and that gap is Ruling 6's to close.

---

### Ruling 6, checked before building — is the switch already the picker?

*Asked by the founder as a pre-condition: is "she chooses whether those messages send" already
expressible as the existing household channel picker, where choosing nobody means the household
gets nothing? Reported, not built.*

**Verdict: half of Ruling 6 already exists; the switch does not. Ruling 6 needs new machinery.**

**What already works — the reference half.** She can be pointed at as a household's channel
today, with no change. `Household.contactPersonEventId` is a `PersonEvent` reference and
`validateChannelTarget` (`src/lib/households/channel.ts`) admits any non-`CHILD` row in the same
event. It is cross-household capable by design. A host captured under Ruling 1 is a valid
target the moment her row exists.

**What does not work — "nobody".** *The picker cannot hold that value.* `contactPersonEventId`
has exactly two states, and neither is "nobody":

| Stored | Means | Resolves to |
|---|---|---|
| a `PersonEvent` id | picked | that person |
| `null` | **not picked** | the household's `PRIMARY_CONTACT`, computed at read time |

`resolveHouseholdChannel` falls back to the primary contact on null. That is not incidental —
it is the property [[GTC-172]] deliberately bought, and the schema docstring records why:
*"NULL means 'not picked' and resolves to the household's PRIMARY_CONTACT at read time. That is
why this ships with no data backfill: the default is computed, not stored."* **Redefining null
as "nobody" would silently mute every household on every existing event** and would destroy the
no-backfill property. The write paths agree: the pre-flight channel PATCH and the households
POST/PUT accept `string | null` only, and `validateChannelTarget` requires a target that exists.

**What comes closest, and why it is not the answer.** Two existing controls *do* suppress the
proxy path, both from [[GTC-179]]:

- `PersonEvent.nudgeMark = DONT_CHASE` on the channel — `findProxyNudgeCandidates` skips the
  household (its Ruling 3).
- `Event.nudgePace = OFF` — suppresses every household on the event (its Ruling 12).

So a host who is her own household's channel could mute it today by marking **herself**
`DONT_CHASE`. That is a coincidence of mechanism, not the control Ruling 6 describes:
`nudgeMark` is *"a hosting judgement about that person"* — an opinion about someone else's
temperament — and it is per-person, so it fires across **every** household she is channel for,
not the one. Overloading it would make the pre-flight offer the host herself under "go gentle
on", which is the precise absurdity this ticket already lists as a consequence.

**Also note the scope of what the picker gates.** Today the channel governs the **proxy nudge
path only** (`findProxyNudgeCandidates`). The ask itself is composed per-recipient, not per
household, and its dispatch is [[GTC-189]]'s and unbuilt. So "her household's messages" is a
smaller surface today than the phrase implies, and will grow when GTC-189 lands.

**Not built, and no shape proposed** — which of the two obvious shapes (a third channel state,
or a household-level boolean) is correct is a decision, not an implementation detail, and it is
not among the nine rulings.

#### What Ruling 7 does to Ruling 6 — it promotes it to a blocking dependency

*Re-checked 2026-08-29 after Ruling 7. This changes the phasing, so it is recorded here rather
than left in the plan.*

Ruling 7 makes the host **always a `PRIMARY_CONTACT`**. `resolveHouseholdChannel` returns the
primary whenever `contactPersonEventId` is null, and null is the state every household starts
in. **Therefore the host is her own household's proxy channel by default, on every event, with
no pick made.** Ruling 6's switch is not later polish — it is the only thing standing between
Ruling 7 and a live send path.

Worse under Ruling 2. `findProxyNudgeCandidates` has **no member-count gate**: a household of
one is an eligible household, and `memberCount` is simply `1`. The template
(`getProxyHouseholdReminderMessage`) then produces, to a host hosting alone:

> `<Event>: 1 person in your group hasn't confirmed yet. Can you check in with them? <link>`

**She is texted about herself.** Ruling 5 does not stop it — that path reads `householdRole`,
never `role`, so Ruling 8's `role: HOST` is invisible to it and it needs no `PARTICIPANT` token.

Two ways out, neither ruled: Ruling 6's switch defaulting to off for the host, or a member-count
gate on the proxy finder. **A member-count gate is the larger change** — it alters proxy
behaviour for ordinary one-person households too, which is a Moment 4 §10.7 question and not
this ticket's.

---

### Still open — do not decide

*Named as open by the founder on 2026-08-29. An executor reaching one of these stops and asks.
None may be resolved by inference from the nine rulings above.*

1. **`Person.userId` linkage, and whether two `Person` rows may share one `userId`.**
   `personForUser` (`src/lib/auth/actor.ts`) is `findFirst({ where: { userId } })` **with no
   `orderBy`** — two rows make the ledger actor non-deterministic. `POST /api/events` has the
   same shape at its find-or-create.

   ⚠ **THIS IS NOW LOAD-BEARING ON RULINGS 7-9, which it was not before.** Those rulings say
   *the host's* `PersonEvent` — they do not say **which `Person` row it points at**. If Moment 1
   reuses `Event.hostId`'s `Person` (the natural reading of Ruling 1: the flow knows who is
   signed in, so no detection is needed) then `wrap-up.ts`'s path-1 filter and
   `PeopleSection`'s host badge both start working for free, and `ensureEventTokens` issues her
   `HOST` token against the same row that carries her membership. If it creates a second
   `Person`, none of that holds and the `HOST` token is issued to a row with no membership.
   **Rulings 7-9 are coherent only under the first reading, and it is not ruled.**
2. ~~**What the host's `PersonEvent.role` carries.**~~ **RULED 2026-08-29 by Ruling 8 —
   `role: HOST`.** Struck rather than deleted so the phase gating below stays readable. Ruling 9
   reconciles it with Ruling 4: `role: HOST` keeps her out of auto-assign's pool, which is the
   intent, and the manual assign route has no role gate, so she can still hold what she picks.
3. **Backfill of existing events.** See the lapsing condition recorded below.
4. **Whether the three-path resolution survives.** `src/app/api/events/[id]/pre-flight/message/route.ts`
   declines to extract it into a shared helper *on the grounds that this ticket may delete the
   need for it*. That comment is now load-bearing on a decision that has not been made.

---

### Recorded from the Phase 1 audit (2026-08-29)

*Four findings the ticket did not have when it was filed. Recorded here because each one
changes what closing it means.*

#### Scope correction — this is not a Moment-flow bug

**No event created through `POST /api/events` has ever had a host `PersonEvent`, on either
path.** The split is **paid-creation versus clone-or-seed**, not V1 versus V2.

| Path | Host `PersonEvent` after creation |
|---|---|
| `POST /api/events` — the only production path, V1 and V2 alike | **None.** The transaction writes `User`, `Person`, `Event.hostId` and `EventRole{HOST}`. It makes no `PersonEvent` write at all. |
| `POST /api/templates/[id]/clone` | Yes — `role: 'HOST'`, parked on the first team. |
| `prisma/seed.ts` | Yes — Sarah Henderson is listed with `role: 'HOST'` in the seed data. |

This ticket's own evidence table already showed it: the four Security Test Events are **V1** and
read `0 / 0`. The title and scope line name the Moment flow because that is where it was
noticed. The Moment flow's actual contribution is only the **second** half of the scope line —
the captured duplicate.

*Corollary for Ruling 1:* fixing capture in Moment 1 does not by itself give a V1 event a host
row. Whether V1 gets one is inside open question 3 (backfill) and open question 2 (role).

#### The third state — the host who types her own email

The scope line describes two states. There is a **third**, and which one a real host lands in
depends on whether she retypes her account email exactly.

`Person.email` is `@unique`, and `createMember` in the households route does
`findUnique({ where: { email } })` before creating. So if the host enters **her own account
email** as a household primary contact, **no duplicate is created** — her existing host `Person`
is reused and given a `PersonEvent` with `role: 'PARTICIPANT'` and
`householdRole: 'PRIMARY_CONTACT'`.

The result is one person, correctly identified by identity path 1, **wrongly roled**: she holds
both a `HOST` and a `PARTICIPANT` token, is a live auto-nudge recipient, is her household's
default proxy channel, is thanked at wrap-up, and **her name is claimable through the shared
link** (the join page lists `people where role: 'PARTICIPANT'`). Ruling 5 forbids all six.

This state is *worse* than the duplicate in one respect: because path 1 matches, the pre-flight
sets `hostIdentityResolved: true` and **the amber banner that currently warns her disappears.**

#### Zero duplicates exist today — a condition that lapses

*Recorded in [[GTC-178]] Ruling 6's style, because it has the same property: it is true now, it
is load-bearing on the backfill decision, and it can lapse.*

**Verdict: the condition HOLDS. No event in the database has two host records.** Measured
2026-08-29 against `gather_dev`:

| Check | Count |
|---|---|
| Events | 9 |
| Events whose host `Person` has **no** `PersonEvent` | **8** of 9 |
| `PersonEvent` rows whose `personId` is any `Event.hostId` | **1** (Sarah Henderson, demo seed) |
| Guests sharing the host's **name** on the same event | 0 |
| Guests sharing the host's **email** on the same event | 0 |
| `Person` rows sharing one `userId` (`GROUP BY userId HAVING count(*) > 1`) | **0** |
| `Person` rows with any `userId` | 3 of 132 |
| Events created through `POST /api/events` (`stripePaymentIntentId` not null) | **0** |

**The captured duplicate is PREDICTED, NOT OBSERVED.** The code plainly produces it; nothing
has produced it yet, because no host has ever gone through Moment 1 typing their own name.
Every fixture in the database was written by a script that seeds guests only — including the two
labelled "Moment flow" in the evidence table above, which are
`scripts/seed-gtc-133-test-event.ts` and `scripts/seed-gtc236-test-event.ts`. Those scripts
reproduce the production route faithfully (User → Person → Event → EventRole, no `PersonEvent`)
and then the households route, so they model the bug correctly — but they are not evidence of it
in the wild.

⚠ **THE CONDITION LAPSES THE MOMENT A REAL HOST USES MOMENT 1.** Re-query it immediately before
any backfill decision (open question 3) rather than inheriting this table. What a backfill must
do to a duplicated event is a different problem from what it must do to a merely hostless one:
the first requires a merge-or-delete choice that moves or cascades `Assignment`, `AccessToken`
and `NudgeLog` rows; the second requires only an insert.

#### The wrong comment

`src/app/api/events/[id]/people/auto-assign/route.ts:89` states, inside THE HOST EXCLUSION
block: *"verified: zero rows for the host Person across every event in the database."*

**It is 1.** Correct it when this ticket is worked. The parallel comment at
`src/app/api/events/[id]/pre-flight/message/route.ts:70` states the same fact **correctly**,
with the exception carried — *"0 except on the V1 seed"* — and is the one to copy.

---

### The fourteen sites with no host exclusion

*Ruling 5 must hold across all of these. Four other sites already attempt a host exclusion and
are listed separately at the end. Grouped by the ruling that binds each, because the rulings do
not bind the same sites. The count is 5 + 4 + 5 = 14.*

**Bound by Ruling 5 (never messaged) — 5 sites, none with any host filter**

| Site | Today |
|---|---|
| `src/lib/sms/nudge-eligibility.ts` (`findNudgeCandidates`) | Iterates every `PersonEvent` with `sentAt`. No host filter of any kind. |
| `src/lib/sms/proxy-nudge-eligibility.ts` | Gates on `householdRole`, phone, SMS, opt-out, `nudgeMark`, pace. **No token requirement and no host filter** — so it survives a `role: 'HOST'` fix untouched. This is the one path where Ruling 5 and Ruling 6 must be kept apart. |
| `src/lib/sms/decide-by-eligibility.ts` | Keyed off `Assignment`. No host filter — so **Ruling 4 walks straight into Ruling 5 here**: the moment the host holds an item, this chases her about it. |
| `src/lib/tokens.ts` (`ensureEventTokens`) | Issues a `PARTICIPANT` token to every `role: 'PARTICIPANT'` row. That token is the **enabling condition** for the auto-nudge path and for the shared-link claim below. |
| `src/app/join/[token]/page.tsx` + `src/app/api/join/[token]/claim/route.ts` | The claim list is `people where role: 'PARTICIPANT'`. A captured host is a **claimable name** — anyone holding the link can claim her identity. |

**Bound by Ruling 3 (counted) — 4 sites**

| Site | Today |
|---|---|
| `src/lib/ai/plan-input.ts` | Counts household members for `totalAdults`. Host absent ⇒ **the plan is sized one adult short** — the consequence Ruling 3 records. |
| `src/components/plan/Moment1Summary.tsx` + `src/app/plan/[eventId]/setup/page.tsx` | The host-facing "X people coming" and the Moment 2 `planGuestCount`. One formula, duplicated; same omission in both. |
| `src/app/api/events/[id]/invite-status/route.ts` | Attendance totals over `event.people`. ⚠ Its comment claims *"event.people already excludes host via the PersonEvent relation"* — **false on V1** (Sarah is counted on Henderson) and **accidentally true on Moment-flow**. Ruling 1 makes it false everywhere. |
| `src/app/api/events/[id]/confirm-invites-sent/route.ts` | Logs `recipients: event.people.length` and stamps `sentAt` on every row — the send clock every nudge counts from. |

**Identity and plumbing — 5 sites**

| Site | Today |
|---|---|
| `src/lib/households/reconcileMembers.ts` | Hard-codes `role: 'PARTICIPANT'` on create, and **deletes** non-primary members absent from the payload. See the delete asymmetry in the phasing below. |
| `src/lib/auth/actor.ts` (`personForUser`) | `findFirst` with no `orderBy` — open question 1. |
| `src/app/api/events/[id]/pre-flight/route.ts` | Lists every `PersonEvent` so a `nudgeMark` is reachable. The host is offered to herself as somebody to go gentle on — the "who not to chase" consequence, live. |
| `src/app/api/events/[id]/pre-flight/cadence/route.ts` | Accepts that `nudgeMark` write against her row. No host check. |
| `src/components/plan/PeopleSection.tsx` | Names the host by `person.personId === hostId` — path 1. No "Host" badge on a Moment-flow event; a duplicate renders as an ordinary, editable, deletable guest. |

**What Rulings 7-9 close, and what they leave open** — re-checked against the code 2026-08-29

| Site | After Rulings 7-9 |
|---|---|
| `nudge-eligibility.ts` | **CLOSED by construction.** No `PARTICIPANT` token (Ruling 8) ⇒ the loop skips her with `'No participant token'`. ⚠ *A consequence, not an intention* — see the stale-token caveat below. |
| `join/[token]/page.tsx` (the list) | **CLOSED.** The claim list filters `role: 'PARTICIPANT'`; she is not in it. |
| `join/[token]/claim/route.ts` (the endpoint) | **NOT closed.** Unauthenticated, takes `personId` from the body, applies **no role filter**, and on finding no `PARTICIPANT` token returns `person.tokens[0]` — for her, the **`HOST` token**, with `redirectPrefix: 'h'`. Pre-existing and not enumerable (her id is no longer on the page), but Ruling 5 says her name is not claimable, and *an excluded list is not a refusing endpoint*. Verify, do not assume. |
| `tokens.ts` | **CLOSED for new rows** — step 4 issues `PARTICIPANT` tokens only to `role: 'PARTICIPANT'`. ⚠ **The prune step handles `COORDINATOR` tokens ONLY.** A stale `PARTICIPANT` token — from the third state, or from any event re-roled by a backfill — **is never revoked**, and re-opens both closed sites above. **Any backfill must revoke, not merely re-role.** |
| `proxy-nudge-eligibility.ts` | **NOT closed, and Ruling 7 makes it worse.** Reads `householdRole`, never `role`; needs no token. See *What Ruling 7 does to Ruling 6*. |
| `decide-by-eligibility.ts` | **NOT closed.** Rooted on `Assignment`; requires only `response: 'MAYBE'`, a phone, the child gate and opt-out — **no token, no role check**. Ruling 9 hands her items, so this is live the moment she opts in and answers MAYBE. |
| The four Ruling-3 counting sites | **NOT closed.** A `role: HOST` row is still a `PersonEvent`; `confirm-invites-sent` still counts it in `recipients`, and `invite-status` still counts it in the attendance totals. Ruling 3 wants it counted in the *headcount* — that is a different number from *recipients*. |
| `reconcileMembers.ts` delete risk | **CLOSED by construction (Ruling 7).** A `PRIMARY_CONTACT` is never in `existingNonPrimary`, so no delete guard is needed and none should be written. |
| `PeopleSection.tsx`, `wrap-up.ts` (path 1) | **Depends on open question 1** — they match on `personId === Event.hostId`, so they work for free if her `PersonEvent` points at the host `Person`, and not at all if it points at a second row. |

**The four sites that DO attempt a host exclusion, for contrast**

| Site | What it attempts, and what it gets |
|---|---|
| `src/app/api/events/[id]/people/auto-assign/route.ts` | The three identity paths, plus a promotion write — and the `role: 'PARTICIPANT'` participant pool. **This is where Ruling 4 binds**, and where open question 2 decides whether the host can be placed at all. All three paths return empty today. |
| `src/app/api/events/[id]/pre-flight/message/route.ts` | The same three paths, copied deliberately rather than extracted. Returns empty; falls back to `Event.host.name` and reports `hostIdentityResolved: false`. |
| `src/app/plan/[eventId]/pre-flight/page.tsx` | The amber banner that tells the host the truth. ⚠ The third state below **silently removes it**. |
| `src/lib/wrap-up.ts` (`selectWrapUpRecipients`) | `pe.personId !== hostId` — identity **path 1 only**, not the three. Never catches a duplicate, so it cannot satisfy Ruling 5 as written. |

Ruling 1 makes all three identity paths resolve. **It does not make the other fourteen sites
correct** — `PersonRole.HOST` is invisible to every one of them.

---

### Execution phasing — REVISED against Rulings 7-9 (2026-08-29, proposed, NOT agreed)

*Supersedes the first-pass plan, which was written before Rulings 7-9. Three things changed:
the delete guard is gone, phase 4 shrank, and Ruling 6 moved from last to blocking. Same
discipline as [[GTC-178]] and [[GTC-179]]: each phase moves one variable, so a failure names its
own cause.*

#### What Ruling 7 did to the `reconcileMembers.ts` phases — it simplified them, but did not remove them

**Removed: the delete guard, entirely.** The first-pass plan flagged that a host stored as a
`PARTNER` is delete-eligible by an ordinary household edit, cascading her `NudgeLog` rows, and
said the fix was *"either a delete guard in `reconcileMembers` or a rule that the host is always
her household's primary."* **Ruling 7 is that rule.** `existingNonPrimary` excludes
`PRIMARY_CONTACT` and the delete loop iterates only that list, so the risk is closed by
construction. **No guard is needed and none should be written** — a guard would be a second
mechanism enforcing what the data shape already guarantees.

**Not removed: the create path.** `createNewMember` hard-codes `role: 'PARTICIPANT'`
(`reconcileMembers.ts`), and Ruling 8 needs `HOST` preserved. `updateExistingMember` never
touches `role`, so an existing host row already survives a rename untouched — that half is
safe today. **The change is one branch on the create path, plus a decision about whether the
primary-contact update path may ever demote her.** Materially smaller than the first-pass plan
assumed.

**⚠ Phases still touching `reconcileMembers.ts`: 2 only.** Phase 4 no longer does. It is still
the load-bearing household write path with its own incident history ([[GTC-159]] delete-and-
recreate data loss, [[GTC-172]], [[GTC-201]] transaction wrapping).

#### What Ruling 8 did to phase 4 — CONFIRMED, with three corrections

**Confirmed as stated: phase 4 is a toggle plus a manual pick, not a change to assignment
eligibility.** Both halves check out against the code:

- Auto-assign selects `role: 'PARTICIPANT'`, so `role: HOST` is out of the pool **with no code
  change at all**. Ruling 9's *"stays exactly as it is, unchanged"* is satisfied by doing
  nothing, which is the correct amount of work.
- `POST /api/events/[id]/items/[itemId]/assign` has **no role gate**, deliberately — [[GTC-207]]
  keeps role gates out of that route. A `role: HOST` row is assignable today. The pick needs no
  eligibility change either.

**Correction 1 — there IS a constraint, and it is team membership, not role.** The assign route
rejects with 400 when `item.kind === 'ITEM' && personEvent.teamId !== item.teamId`, and
`PersonEvent.teamId` is **singular**. So "she chooses her own items" is bounded to items on the
one team she is on. Free choice across the plan needs either a `teamId` write as part of the
pick, or an exemption. *(Task rows — `kind: 'TASK'` — bypass the rule entirely and are freely
assignable.)* This is a build constraint, not a conflict with Ruling 9.

**Correction 2 — "items appear on her host view" is already true, for free.**
`GET /api/h/[token]` already returns every team's items with their named assignee. Nothing to
build for visibility; a "mine" filter is optional polish, not phase 4 work.

**Correction 3 — phase 4 keeps the counting sites, and Ruling 8 does not help there.** A
`role: HOST` row is still a `PersonEvent`, so `confirm-invites-sent` still counts her in
`recipients` and `invite-status` still counts her in the attendance totals. Ruling 3 wants her
in the **headcount**; `recipients` is a different number and must not silently inherit the fix.

#### Does the ordering constraint still hold? — YES, unchanged

The first-pass plan required **phase 4 (items) after phase 3 (never-messaged)**, because Ruling
4 puts items in her hands while `decide-by-eligibility.ts` has no host filter. **Ruling 8 does
not relax this.** That finder is rooted on `Assignment` and requires only `response: 'MAYBE'`, a
phone number, the child gate and opt-out — **no `PARTICIPANT` token and no role check**. So
`role: HOST` is invisible to it, and the moment Ruling 9's toggle gives her an item she answers
MAYBE on, it chases her about her own food. **Order unchanged.**

Ruling 8 *does* close two other Ruling-5 sites by construction — the auto-nudge finder and the
shared-link claim list — but both rest on the absence of a `PARTICIPANT` token, and
`ensureEventTokens` prunes `COORDINATOR` tokens only. **A stale `PARTICIPANT` token is never
revoked**, so both re-open on any event where one already exists.

#### The revised phases

1. **The audit corrections, alone.** Fix the wrong comment at `auto-assign/route.ts:89`; fix the
   false "already excludes host" comment in `invite-status/route.ts`. No behaviour change, no
   schema, trivially revertible.
2. **Capture — Moment 1 opens with the host's household** (Rulings 1, 2, 7, 8): her row is
   created as `householdRole: PRIMARY_CONTACT` + `role: HOST`, with "I'm hosting alone" as a
   household of one. ⚠ *Touches `reconcileMembers.ts`* — the create path only, per above. **New
   events only.** **No migration**: `PersonEvent` exists, `PersonRole.HOST` is in the enum, and
   `@@unique([personId, eventId])` already prevents a second row.
   **Gated on open question 1** — the phase cannot be written without knowing which `Person` row
   her membership points at.
3. **The never-messaged concept** (Ruling 5), as one named thing, in one commit. **Now five
   sites, not seven** — Ruling 8 closes the auto-nudge finder and the claim list by
   construction. The five: `proxy-nudge-eligibility.ts`, `decide-by-eligibility.ts`,
   `wrap-up.ts`, the pre-flight recipient list, and the **claim endpoint** (which the list's
   closure does not cover). ⚠ Must NOT be built by widening the child rule — see Ruling 5.
   ⚠ Must also **revoke stale `PARTICIPANT` tokens**, not merely rely on not issuing them.
4. **Headcount and items** (Rulings 3, 4, 9) — the four counting sites, plus the opt-in toggle
   and the manual pick. **No longer touches assignment eligibility, and no longer touches
   `reconcileMembers.ts`.** Resolve the singular-`teamId` constraint here. **After phase 3, not
   before** — see the ordering check above.
5. **Ruling 6 — the household-channel switch. ⚠ MOVED UP: this is now a blocking dependency of
   phase 2, not a later phase.** Ruling 7 makes the host her household's default channel, so
   shipping phase 2 without it puts a live proxy-nudge path on her from the first event — and
   under Ruling 2, texts a solo host about herself. **Gated**: the shape is undecided and is not
   among the nine rulings. Either it is ruled before phase 2 ships, or phase 2 ships with the
   proxy path explicitly suppressed for the host as an interim, which is itself a decision.
6. **Existing events.** **Gated on open question 3.** Re-query the lapsing condition first;
   split hostless events from duplicated ones; and **revoke `PARTICIPANT` tokens for any row
   re-roled to `HOST`**, or the two construction-closed sites re-open on exactly the events a
   backfill touches.

**Not phased, because it is not this ticket's:** whether the three-path resolution is extracted
or deleted (open question 4). It resolves once phase 2 lands and can be retired or kept
afterwards, on evidence.

### Stop Conditions

Standard eight from `BUG-TICKET-TEMPLATE.md`.
