---
ticket: GTC-076
title: "Past Events reuse flow with plan page overlay"
status: closed
branch: gtc-042-part1-nudge-schema
commit: 9f1ca68
moment: null
type: build
depends_on: []
blocks: []
tags:
  - build
  - templates
---

# [[GTC-076]] — Past Events reuse flow with plan page overlay — CLOSED

**Status:** Complete
**Branch:** gtc-042-part1-nudge-schema
**Commit:** 9f1ca68

### What was found
- Nav item labelled "Templates" at `Navigation.tsx:51`
- Templates page heading "Templates" at `templates/page.tsx:62`
- Action button text "Use Template" in `TemplateList.tsx:153`
- Clone modal title "Clone Template:" in `CloneTemplateModal.tsx:153`
- Existing save-as-template logic in plan page (`handleSaveAsTemplate`) and `POST /api/templates`
- COMPLETE status wrapup section in plan page at lines ~2910-3060 with dispatch status
- Clone API (`POST /api/templates/[id]/clone`) creates DRAFT event with teams/items from template, requires eventName + dates
- `StructureTemplate.createdFrom` stores source event ID; clone sets `Event.clonedFromId`
- Event API returns full model including `clonedFromId`

### What was changed

**`src/components/shared/Navigation.tsx`**
- Renamed nav item label: "Templates" → "Past Events"

**`src/app/plan/templates/page.tsx`**
- Page heading: "Templates" → "Start with one of your past events"
- Subtitle updated
- Added `handleUseAgain` — direct clone flow that skips modal, calls clone API with placeholder dates (+30 days), redirects to `/plan/{eventId}?fromReuse=true`
- Added `useToast` import and `reusingTemplateId` state for loading indicator
- CloneTemplateModal still used for Gather curated templates

**`src/components/templates/TemplateList.tsx`**
- Added `onUseAgain` and `reusingTemplateId` props
- Button text: "Use this again" for My Past Events tab, "Use Template" for Gather tab
- Tab label: "My Templates" → "My Past Events"
- Empty state: shows ticket-specified message + Gather templates inline below
- Loading state shows "Creating..." when reusing

**`src/components/templates/CloneTemplateModal.tsx`**
- Title: "Clone Template:" → "Use Template:"
- Error toasts: "Error cloning template" → "Error creating event"

**`src/components/templates/SaveTemplateModal.tsx`**
- "you'll set these when cloning" → "you'll set these when reusing"

**`src/app/plan/[eventId]/page.tsx`**
- Added `clonedFromId` to Event interface
- Added `savingForNextYear`, `savedForNextYear`, `showReuseOverlay`, `reuseOverlaySummary` state
- Added `handleSaveForNextYear` — saves template with event name, shows toast "Saved — find it in Past Events when you're ready."
- Added "Save for next year" CTA in COMPLETE wrapup section (sage-colored card)
- Added `fromReuse` detection effect — fetches source event people/data, builds overlay summary
- Rendered `PastEventOverlay` with 4 navigation buttons wired to existing modals
- Imported `PastEventOverlay` component

**`src/components/plan/PastEventOverlay.tsx`** (new)
- Dismissible overlay showing "{eventName} — last year's details"
- Summary: guest count/names (truncated after 5 with "+ X more"), team count, item count
- Four buttons: "Enter new dates" (→ EditEventModal), "Edit guest list" (→ People section), "Edit items" (→ Items section), "Edit event details" (→ EditEventModal)
- "Start planning" dismiss button + X close button

### Known limitations / follow-up flags
- Placeholder dates (+30 days) are used when creating event via "Use this again" — host should update via "Enter new dates" button in overlay
- If source event was deleted, overlay falls back to current event data (no guest names)
- Overlay does not reappear after dismiss (state-only, no persistence needed since URL param is cleaned)

### Assertions met
- [x] Nav item reads "Past Events"
- [x] Page heading reads "Start with one of your past events"
- [x] Action label reads "Use this again"
- [x] "Save for next year" CTA visible on COMPLETE events
- [x] Save for next year triggers toast confirmation
- [x] Clicking "Use this again" creates new DRAFT event and redirects to plan page
- [x] Overlay appears on plan page with last year's summary
- [x] All four navigation buttons open correct modals
- [x] "Start planning" dismisses overlay
- [x] X dismisses overlay
- [x] Overlay does not reappear after dismiss
- [x] Empty state shown when no past events
- [x] No internal names changed
- [x] Security suite 16/16 passing

### Evidence
- Nav rename confirmed: yes
- Overlay confirmed: yes
- Save for next year confirmed: yes
- Commit: 9f1ca68
