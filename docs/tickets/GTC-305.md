---
ticket: GTC-305
title: 'Asked but never chased — the wide hand-over has no line on the host''s list and no red on the board'
status: open
branch: TBD
commit:
moment: 4
type: build
severity:
severity_proposed: medium
depends_on:
  - '[[GTC-189]]'
blocks: []
related:
  - '[[GTC-189]]'
  - '[[GTC-192]]'
  - '[[GTC-251]]'
  - '[[GTC-296]]'
  - '[[GTC-300]]'
  - '[[GTC-301]]'
  - '[[GTC-310]]'
tags: [hinge, epic-i, the-press, chase, hand-over, host-list, glance, red-reason, recipient-model]
---

# GTC-305 — the hand-over the host is never shown

**Status:** Open — **filed, not scoped. Nothing is built by the ruling that filed it.**
Filed 2026-09-15 | **Branch:** TBD | **Commit:** [pending] | **Effort:** TBD

**Severity:** [ ] Critical [ ] High [x] Medium [ ] Low — **re-proposed 2026-09-15 from High, see
the ruling AG banner below**; the founder's to set.
The frontmatter carries it as `severity_proposed` and leaves `severity` empty until it is set, so
the proposal does not read as a ruling.
Escalated (brand/trust risk): [ ] No — nothing is sent wrongly. The host is not told something she
was ruled to be told.

**Filed on the founder's instruction**, from [[GTC-189]] *Founder rulings — fourth pass (Nigel,
2026-09-15)*, ruling Y, which settled decision 15 WIDE:

> DECISION 15 — WIDE. If the chase cannot text, it is mine, whatever the reason. Nobody is chased
> by email.
>
> My reason, and it answers one of the riders: a hand-over shows RED, at the moment of send. Red
> means it is the host's to do, and that is consistent with the earlier ruling that an unreachable
> person is red from the press rather than amber.

Tree read at `99cb852` on `feat/moment-one-redesign`. **Confirmed by reading the code, NOT
reproduced** — the press does not send yet, so there is nothing to reproduce until slice 5 exists.

---

> ⚠ **RESHAPED THE SAME DAY — RULING Y IS SUPERSEDED BY RULING AG, 2026-09-15.** This ticket was
> filed out of ruling Y and High because Y put ordinary, successfully-emailed guests into red on the
> board from the press. **Ruling AG removes exactly that population**, so the reason this was High
> is gone and the proposed severity is Medium.
>
> **What AG changes here:**
>
> - **The red shrinks to people who genuinely failed.** A person the host ticks *chase by email* is
>   chased, so he is amber like anyone else. A person she does not tick is hers **by her own
>   decision** — which is what makes ruling M's existing red true of him rather than merely applied
>   to him. ⚠ **So it is now an open question whether this ticket needs a new `RED_REASON` at all**,
>   where under ruling Y it plainly did. That is Unknown 1, re-pointed.
> - **The pre-flight half moves to [[GTC-311]].** Under Y the host's list needed a second population
>   shown to her. Under AG that list is a screen she acts on, with a per-person control and storage
>   behind it, and that is a different ticket.
> - **What stays here** is the board: `deriveItemState` in `src/lib/glance/state.ts` still gives an
>   unanswered row AMBER for everyone who is not the host, so a person the host has taken reads as
>   chased; and `whyLineFor` in `src/components/glance/strip.ts` still renders nothing for a red
>   whose reason is not in `WHY_PRECEDENCE`.
> - **And one thing AG adds:** the board must now distinguish *she took him* from *nobody could
>   reach him*, because under AG those are different facts arrived at differently. Under Y they were
>   one.
>
> **Re-scoped, not cancelled.** [[GTC-189]] decision 25 — whether AG's default is right at all — is
> open and would change this screen again if it is ruled the other way.

---

### Signal

**Expected**
Ruling Y creates a population the host has to know about: people whose ask goes out successfully
and who will never be chased, because the chase cannot text them. Ruling Y says they are hers from
the moment of send, and that they read red.

**Actual**
Nothing in the tree knows they exist.

- **The pre-flight does not show them.** `readAskPreview` in `src/lib/preflight/ask-preview.ts`
  routes every membership through `chooseAskRoute` and nothing else. `chooseChaseRoute` in
  `src/lib/eligibility/channel-chooser.ts` has **no caller anywhere in `src/`** — verified by grep
  at `99cb852`. So `HostListLine` is built purely from ask-routing, and a person who can be asked
  and cannot be chased produces no line at all.
- **The board does not show them.** `RED_REASONS` in `src/lib/glance/state.ts` holds three —
  `DECIDE_BY_EXPIRED`, `REVERSAL`, `EXHAUSTED_SILENCE` — and `deriveItemState` gives an unanswered
  row `AMBER` / `AWAITING_REPLY` for anyone who is not the host. A person nobody will ever chase
  therefore reads as chased, which is ruling J's *"the board lying to her"* in a second population.
- **A red without a reason has no why-line.** `whyLineFor` in `src/components/glance/strip.ts`
  renders only the reasons in `WHY_PRECEDENCE`, so a red carrying an unlisted reason renders with
  no why at all, against [[GTC-192]] Ruling 4. Ruling M already recorded this gap for the
  never-reachable; ruling Y widens who falls into it.

**Smallest failing case**
An adult with an email address and no phone number, holding one item. Open the pre-flight message
step: she is listed as an ordinary email recipient, and the host's list does not name her. Under
ruling Y she is the host's from the send.

---

### Why this is not already somebody's

- **Not slice 1's.** Slice 1 owns the chooser, and ruling Y's change there is one line —
  `chaseChannelOf`'s narrow-reading branch and its `ANCHOR(GTC-189)`, with the three `[D15-narrow]`
  assertions in `tests/channel-chooser-test.ts` moving with it. That is inside the module and
  anticipated at slice 1. It makes the chooser answer correctly; it shows nobody anything.
- **Not slice 3's, as built.** Slice 3 shipped the host's list from ask-routing and is committed.
  Consuming the chase chooser is new work on a built slice, which is why it has a ticket rather
  than an amendment.
- **Slice 7 is the nearest owner and does not cover it.** Ruling M filed *"the unreachable red,
  with a reason and why-line of its own"* against slice 7. That red is for a person who was never
  messaged. This is a person who **was** messaged, successfully. Whether they share one reason or
  need two is Unknown 1.

---

### Scope

**Fix here:**
- The pre-flight: the host's list gains its second population, each line named with the person's
  item and a why, from `chooseChaseRoute`.
- The board: whatever reason and why-line ruling Y's red needs, and the derivation that reaches it.

**Do not touch:**
- All Do-Not-Touch Zones in `GATHER-BUILD-CONSTANTS.md`. **Zone 7 is live here** — `SMS_OPTED_OUT`
  is one of the routes into this population, and nothing in this ticket may change what opt-out
  means or where it is stored ([[GTC-301]]).
- The ask. Ruling Y touches the chase only: an emailed adult is still emailed, and `chooseAskRoute`
  is unchanged.
- [[GTC-192]] Ruling 14's grey. A handed-over person who is also marked don't-chase stays grey, by
  ruling T's ground — she has already taken him over.

---

### Acceptance

- [ ] A person who can be asked and cannot be chased appears on the host's list at the pre-flight,
      named with their item and a why, before anything is sent.
- [ ] The same person reads red on the board from the send, with a why-line that is true of them —
      not *handed it back*, not *maybe timed out*, not *gone quiet*.
- [ ] A person marked don't-chase who is also handed over reads grey, not red.
- [ ] Every route into the population has words: no phone at all, a phone the send cannot use, an
      opted-out phone. No case renders a blank line.
- [ ] `npx tsc --noEmit` clean, build passes, security suite exits 0.

---

### Unknowns

1. **One reason, two, or none.** ⚠ **Re-pointed 2026-09-15 by ruling AG.** Ruling M's red is for a
   person never messaged. Under ruling Y there was a second population — messaged and never
   chaseable — and the question was one member or two. Under AG that population is a person the host
   chose to take, which may be ruling M's red exactly as it stands. **So the live question is now
   whether `RED_REASONS` needs anything at all**, and if it does, whether the board should say *she
   took him* differently from *nobody could reach him*.
2. **What the strip says while she is waiting.** A person can be red-because-unchaseable and also
   reply an hour later. What the board does at that moment — the red clears, or it was never about
   the reply — is not ruled.
3. ~~**Whether the pre-flight's two populations read as one list or two.**~~ **MOVED 2026-09-15 to
   [[GTC-311]]**, which owns the pre-flight under ruling AG. The finding stands and is recorded
   there: slice 3's heading is *"Yours to handle"* and its line is *"Gather will not message these
   people"*, and the second half is false for anyone who is asked but not chased.
4. **[[GTC-300]] changes who is in it.** `isValidNZNumber` in `src/lib/phone.ts` rejects `+61`, so
   every Australian number is in this population until that predicate is fixed.

---

### Stop Conditions

Standard eight from `BUG-TICKET-TEMPLATE.md`. Live notes:

- **5 (Do-Not-Touch zones):** fires on any change to opt-out storage or reading (Zone 7).
- **8 (unknowns):** Unknown 1 before adding anything to `RED_REASONS`.

---

### Executor Checklist

[ ] GATHER-BUILD-CONSTANTS.md read
[ ] Citations section read; every citation in this ticket is a symbol, not a line number
[ ] Preflight passed and results pasted
[ ] Bug reproduced — Observe confirmed
[ ] Plan documented (hypothesis, change set, test strategy, rollback risk)
[ ] Fix implemented within declared scope
[ ] No Do-Not-Touch zones modified
[ ] GREEN confirmed
[ ] Acceptance assertions checked
[ ] Adjacent flows verified
[ ] Evidence package assembled
[ ] Commit hash recorded
[ ] No stop conditions triggered without resolution

---

## Evidence (Executor-Completed)

> Filled in by the executor before committing.

**Root cause confirmed**

**Files changed**

**Test results**
- RED:
- GREEN:

**Assertions checked**

**Security suite:**

**Commit hash:**
