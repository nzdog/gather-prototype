# GTC-061 — Teams modal assigned/unassigned badge colours — CLOSED

**Status:** Complete
**Branch:** gtc-042-part1-nudge-schema
**Commit:** a57cb76

### What was found
The `ItemStatusBadges` component (`src/components/plan/ItemStatusBadges.tsx`) rendered the assignment badge using `bg-sage-100 text-sage-800` (assigned) and `bg-gray-100 text-gray-800` (unassigned). Both are muted grey-green tones with insufficient contrast — the host cannot scan assignment status at a glance.

No existing green or amber design tokens beyond Tailwind defaults. The design system defines a custom `sage` palette as the primary brand colour, plus Tailwind's built-in colour scales.

### What was changed

**`src/components/plan/ItemStatusBadges.tsx`**
- Assigned badge: changed from `bg-sage-100 text-sage-800` → `bg-green-100 text-green-800`
- Unassigned badge: changed from `bg-gray-100 text-gray-800` → `bg-amber-100 text-amber-800`
- No changes to size, position, structure, or any other badge (confirmation badge unchanged)

### Known limitations / follow-up flags
- None. The confirmation badge already uses green/yellow which remains visually distinct from the assignment badge colours.

### Assertions met
- [x] Assigned items show green badge
- [x] Unassigned items show amber/orange badge
- [x] Same position and size
- [x] No other badge styles affected
- [x] Security suite 16/16 passing

### Evidence
- Colours applied: `bg-green-100 text-green-800` (assigned), `bg-amber-100 text-amber-800` (unassigned)
- Commit: a57cb76
