---
ticket: GTC-246
title: "Dependabot triage: Prisma chain — watch item, no fix available"
status: deferred
branch: TBD
type: chore
severity: medium
depends_on: []
blocks: []
related:
  - '[[GTC-152]]'
  - '[[GTC-080]]'
tags: [chore, infra, security, dependencies, dependabot, prisma, deferred]
---

# GTC-246 — Dependabot triage: Prisma chain — DEFERRED, monitor only

**Status:** Deferred — no fix exists yet; this is a watch item, not actionable work
**Severity:** Medium — see Real-world exposure below (downgraded from the chain's raw "High"
npm-audit severity because the vulnerable path is dev/CI-only, not in the production runtime)
**Escalated (brand/trust risk):** No — not production-reachable (see below)

**Filed from:** Dependabot triage audit, 2026-08-23, run against `feat/moment-one-redesign`.
Sibling tickets: [[GTC-243]] (safe bundle), [[GTC-244]] (Next.js chain),
[[GTC-245]] (vendor chain).

---

## Why deferred

`npm audit` on `feat/moment-one-redesign` flags three packages in one chain — `prisma` (direct
devDependency, installed 6.19.3), `@prisma/config` (transitive, installed 6.19.3), and
`deepmerge-ts` (transitive, installed 7.1.5, GHSA-ggr8-5vv4-36mx — stack exhaustion / DoS when
merging recursive object graphs). For all three, `npm audit`'s own `fixAvailable` field reports
the only path is `prisma@6.12.0` with `isSemVerMajor: true` — that is not a forward upgrade, it
is a **downgrade** from the currently-installed 6.19.3, and a major-version move besides. There
is currently no released version of `prisma`/`@prisma/config` that both stays ahead of 6.19.3
and clears `deepmerge-ts`'s vulnerable range (`<8.0.0`).

Downgrading `prisma` is not acceptable given the schema work already built on 6.19.3-era
behavior: [[GTC-152]] shipped a reviewed migration
(`20260708081323_drop_generated_data_and_structure_change_request`) directly against this
branch's Prisma toolchain, and `GATHER-BUILD-CONSTANTS.md` Do-Not-Touch Zone 5 (Prisma
Migrations) already treats the migration history as fragile — the repo spent months on P3005
drift before baselining it clean on 2026-03-14. A Prisma downgrade is exactly the kind of change
that zone exists to prevent without explicit instruction and a dedicated ticket.

(Correction to the framing this ticket was filed under: [[GTC-241]] was checked and is a
seed-script cleanup ticket, not a migration — it does not touch `prisma/migrations/`. GTC-152 is
the ticket that actually carries recent migration risk; cited above instead.)

---

## Real-world exposure (why this is Medium, not the raw High severity)

`@prisma/client@6.19.3` — the package that actually ships into the production runtime — has
**zero dependencies** (`npm ls @prisma/client` confirms an empty dependency tree). The
`@prisma/config` → `deepmerge-ts` chain is pulled in exclusively by `prisma`, the CLI package,
which is a `devDependency` (`npm ls @prisma/config --all` shows its sole parent is `prisma`).
`deepmerge-ts`'s vulnerable code path is stack exhaustion when merging deeply recursive object
graphs — realistically triggered by a maliciously crafted Prisma config file processed by the
CLI, not by anything in this app's request-handling code. **This is a dev/CI-time exposure, not
a production-facing one.** Severity is recorded as Medium on that basis; re-escalate to High if
that assumption turns out to be wrong (e.g. if `@prisma/config` is ever loaded at runtime, not
just by the CLI).

---

## What would resolve this

Wait for one of:
1. A `prisma`/`@prisma/config` release that bumps its `deepmerge-ts` dependency to `>=8.0.0`
   while staying ahead of 6.19.3 (forward-compatible fix — the ideal outcome, requires no
   decision from us, just periodic re-checking).
2. Prisma's own advisory or release notes explicitly addressing GHSA-ggr8-5vv4-36mx.
3. If neither materializes and the CLI-only exposure is judged acceptable indefinitely, this
   ticket can be closed as accepted risk rather than fixed — that is a founder call, not an
   executor one.

**Check-in cadence:** re-run `npm audit` on this chain at the next scheduled Dependabot triage,
or ad hoc via `npm view prisma versions` / `npm view @prisma/config dependencies` to see if a
newer patch has moved `deepmerge-ts`.

Related, larger Prisma work already on file: [[GTC-080]] (Prisma v7 upgrade, also deferred) —
a v7 upgrade would very likely resolve this chain as a side effect (new major, new
`@prisma/config` dependency tree) but is its own large, separately-gated piece of work per
GTC-080's own scope (driver adapters, `prisma.config.ts`, env var migration, etc.). Do not use
this ticket to justify pulling GTC-080 forward — that remains a separate decision.

---

## Scope

**No action in this ticket.** This is a monitor entry, not a fix.

**Do not touch:**
- `prisma/migrations/` (Do-Not-Touch Zone 5) — never hand-edit, never reorder.
- Do not downgrade `prisma`/`@prisma/client` to "clear" this advisory. The downgrade path is
  explicitly rejected above.

---

## Unknowns

- Whether Prisma has a fix in progress upstream (not checked beyond the current npm registry
  state as of 2026-08-23).
- Whether `@prisma/config` is ever loaded outside the CLI in this codebase (assumed no, based on
  `@prisma/client` having zero dependencies, but not exhaustively grepped against every script
  in `package.json`).

---

## Stop Conditions

Standard 8 from `BUG-TICKET-TEMPLATE.md`, plus:
9. Do not act on this ticket by downgrading Prisma under any circumstance without explicit
   founder instruction overriding the reasoning above.

---

## Evidence (Executor-Completed)

> Not applicable — deferred, no fix attempted or expected until an upstream release exists.
