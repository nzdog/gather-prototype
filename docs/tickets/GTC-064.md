---
ticket: GTC-064
title: "FROZEN step labelled \"Freeze Event\" and clickable"
status: closed
branch: gtc-042-part1-nudge-schema
commit: 9eaed0e
moment: null
type: build
depends_on: []
blocks: []
tags:
  - build
  - wrap-up
  - frozen-screen
---

# [[GTC-064]] — FROZEN step labelled "Freeze Event" and clickable — CLOSED

**Status:** Complete
**Branch:** gtc-042-part1-nudge-schema
**Commit:** 9eaed0e

### What was found
- Progress bar component: `src/components/plan/EventStageProgress.tsx`
- Freeze transition trigger: `handleExpandSection('planstatus')` in `src/app/plan/[eventId]/page.tsx`, which opens the Plan Status expansion panel containing the `FreezeCheck` component

### What was changed

**`src/components/plan/EventStageProgress.tsx`**
- Added `onFreezeClick?: () => void` prop to `EventStageProgressProps`
- Added `getFrozenStepLabel()` helper that returns:
  - `'Freeze Event'` when `currentStatus === 'CONFIRMING'`
  - `'Frozen'` when `currentStatus === 'FROZEN'` or `'COMPLETE'`
  - `STATUS_LABELS.FROZEN` for DRAFT (fallback)
- Made the FROZEN step clickable when in CONFIRMING status: added `onClick`, `role="button"`, `tabIndex`, keyboard handler (Enter/Space)
- Added visual affordance for clickable state: blue ring, hover effects, cursor pointer, blue text

**`src/app/plan/[eventId]/page.tsx`**
- Passed `onFreezeClick={() => handleExpandSection('planstatus')}` to `EventStageProgress`, wiring the click to open the Plan Status panel (which contains Freeze Readiness / FreezeCheck)

### Known limitations / follow-up flags
None

### Assertions met
- [x] FROZEN step labelled "Freeze Event" on CONFIRMING
- [x] Click triggers freeze transition (opens Plan Status panel with FreezeCheck)
- [x] Past tense "Frozen" when already FROZEN or COMPLETE
- [x] Other steps unaffected
- [x] Security suite 16/16 passing

### Evidence
- Label and click confirmed: yes
- TypeScript: compiles clean (no errors)
- Security suite: 16/16 passing
- Commit: 9eaed0e
