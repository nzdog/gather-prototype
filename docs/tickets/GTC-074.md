# GTC-074 — Sweep remaining "Wrap Up" language — CLOSED

**Status:** Complete
**Branch:** gtc-042-part1-nudge-schema
**Commit:** [fill after commit]

### What was found
Full grep of `src/` for all "wrap up" variants (Wrap Up, wrap up, Wrap-Up, wrap-up, wrapup, WrapUp) found ~40 instances total. After categorisation:

- **User-visible UI text:** 0 remaining — GTC-065 already renamed all card headings, modal titles, and button labels to "Complete Event" / "Event Complete"
- **User-visible API error message:** 1 instance — `"Cannot wrap up event in ${status} status"` in `src/app/api/events/[id]/wrap-up/route.ts:52`
- **Internal (no change):** ~39 instances — variable names (`wrapUpLoading`, `wrapUpResult`, etc.), function names (`handleWrapUp`, `generateWrapUpLinks`), API route paths (`/api/events/[id]/wrap-up`), imports, console.error/warn messages, comments, type literals (`'wrapup'`), metadata keys

### What was changed

**`src/app/api/events/[id]/wrap-up/route.ts`**
- Line 52: Changed error message from `"Cannot wrap up event in ${status} status. Event must be FROZEN."` to `"Cannot complete event in ${status} status. Event must be FROZEN."` — this is returned in the API response body and could be displayed to the host.

### Known limitations / follow-up flags
- All internal names (variables, functions, routes, file names) intentionally left unchanged per ticket scope — "UI display text only"
- SMS/email templates checked — no "wrap up" in user-facing copy (messages use natural language like "Thanks for making X happen")

### Assertions met
- [x] Zero visible "Wrap Up" in UI text
- [x] Zero "Wrap-Up" in UI text
- [x] Replacements use correct language for context
- [x] No internal names changed
- [x] Security suite 16/16 passing

### Evidence
- Grep: zero visible "Wrap Up" instances remaining
- Instances replaced: 1 (API error message in wrap-up route)
- Commit: [fill after commit]
