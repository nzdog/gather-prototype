---
ticket: GTC-215
title: "Correct the stale P3005 drift warning in GATHER-BUILD-CONSTANTS.md"
status: closed
branch: feat/moment-one-redesign
commit:
moment: 0
type: docs
depends_on: []
tags: [docs, prisma, migrations, stale-doc, executor-preamble, audit-2026-08-04]
---

# GTC-215 — Correct the stale P3005 warning where it is actually read — CLOSED

**Status:** Complete | **Branch:** feat/moment-one-redesign | **Commit:** [pending backfill] | **Effort:** low

### Scope

`GATHER-BUILD-CONSTANTS.md:108-112` carried a "pre-existing known issue" claiming DB schema
drift (P3005) against `gather_dev` — confirmed stale. It sits in the ticket-template preamble,
step 1 of every GTC ticket, so every executor who followed the preamble read a false warning
before touching code.

**Fix here:** `GATHER-BUILD-CONSTANTS.md` only, replacing the stale P3005 entry with a
dated confirmation and bumping the file's "Last updated" stamp. Not the full sweep flagged in
the stub (`GATHER-KNOWN-BEHAVIOURS.md` and five skills also mention P3005) — those mentions are
correct as history (the drift did happen and was baselined 2026-03-14) and are left untouched
per the stub's own scoping note. Not `docs/04_roadmap/moment4-hinge-discovery-report.md` —
confirmed unrelated (zero hits for `p3005|drift|build-constants|migrate status` across all 181
lines).

**Ruling: correct, not remove.** A dated "verified clean" note is more useful to a future
reader than silent deletion — it records that the issue existed, was fixed, and when it was
last checked, rather than leaving a reader to wonder whether it was ever true.

### Evidence (Executor-Completed)

Re-verified live immediately before editing, not taken on trust from the stub:

```
$ npx prisma migrate status
37 migrations found in prisma/migrations
Database schema is up to date!
```

Corroborates the stub's own two prior clean runs (GTC-171 `e85ebfa`, GTC-172 `63764d1`, both
2026-08-04 at 32 migrations) and the original baselining fix (2026-03-14). Five migrations have
landed since with no drift.

- `GATHER-BUILD-CONSTANTS.md:108-112` — stale P3005 entry replaced with a dated confirmation
  pointing back to this ticket; "Pre-existing known issues" now correctly reads "None currently
  open."
- "Last updated" stamp bumped 2026-07-09 → 2026-08-06.
- Doc-only change; no code touched, no do-not-touch zone involved.
