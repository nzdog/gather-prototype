---
ticket: GTC-294
title: 'Coordinators who own an item get a PARTICIPANT token as well — the job must not cost them the ask'
status: open
branch: TBD
commit:
moment: 4
type: build
severity: medium
depends_on:
  - '[[GTC-262]]'
blocks:
  - '[[GTC-189]]'
related:
  - '[[GTC-189]]'
  - '[[GTC-248]]'
  - '[[GTC-172]]'
  - '[[GTC-256]]'
  - '[[GTC-191]]'
tags: [moment-4, tokens, coordinators, recipient-model, zone-3, the-press]
---

# GTC-294 — Coordinators get a PARTICIPANT token as well

**Status:** Open — filed 2026-09-13, not started | **Branch:** TBD
**Severity:** [ ] Critical [ ] High [x] Medium [ ] Low
Escalated (brand/trust risk): [ ] No — nothing sends yet, so nothing is wrong for a guest
today. It is a **blocker on [[GTC-189]] slice 5**, the press.

⚠ **DO NOT START BEFORE [[GTC-262]] CLOSES.** The ordering is the founder's and the reason is
below.

⚠ **DO-NOT-TOUCH ZONE 3** (AccessToken & scope). This ticket changes token *issuance*, which
Zone 3 names directly. Surface the shape and get explicit approval before writing code, in the
form [[GTC-256]] phase 3 used.

---

### Founder ruling (Nigel, 2026-09-13)

Option (a) of the two put to the founder in [[GTC-189]]'s audit:

> A coordinator who owns an item is a guest with an item and a job, and the job should not cost
> them the ask.

So a coordinator holds a PARTICIPANT token **as well as** their COORDINATOR one.

---

### Signal

**Expected**
An adult who owns an item can be sent a working ask link, whatever else they do on the event.

**Actual**
A coordinator has no `/p/` link to send. `ensureEventTokens` in `src/lib/tokens.ts` issues
PARTICIPANT tokens in its step 4 only where `pe.role === 'PARTICIPANT'` **and** the person is
not in `coordinatorIds`, which it builds from every team's `coordinatorId` and every
`role: 'COORDINATOR'` membership. The file says so in its own words: *"Coordinators do NOT get
PARTICIPANT tokens."* A coordinator holds only `/c/{token}`, a different page.

**Why it matters now.** [[GTC-189]]'s recipient and channel model (founder ruling, 2026-09-13)
messages every adult who owns an item directly, with a link to the guest tap page. For a
coordinator who owns an item, that link cannot be built. `GET` in
`src/app/api/events/[id]/pre-flight/message/route.ts` already shows this: it lists coordinators
and their link stays the stand-in string forever, because it reads only PARTICIPANT tokens.

**Reachable, not hypothetical.** `PeopleSection`, mounted on the plan page, offers the
Coordinator role and `AssignCoordinatorsModal`. In gather_dev (as of 2026-09-13), 7 coordinator
memberships hold items, all on "Henderson Family Christmas 2025".

---

### Why after [[GTC-262]] — the ordering is load-bearing

[[GTC-262]] is open and High: `GET /api/gather/[eventId]/directory` and
`POST /api/join/[token]/claim` are unauthenticated and hand out whatever token a person holds,
preferring PARTICIPANT and falling back to COORDINATOR.

Issuing coordinators a PARTICIPANT token **changes what those two surfaces leak** — they would
start returning the participant link rather than the coordinator one — which moves GTC-262's
blast radius while GTC-262 is still open. Closing GTC-262 first means this ticket lands against
a surface whose behaviour has already been decided.

---

### What issuing the token changes — check each, do not assume

- **The chase finders open to coordinators.** `findNudgeCandidates` in
  `src/lib/sms/nudge-eligibility.ts` and `findDecideByFollowupCandidates` in
  `src/lib/sms/decide-by-eligibility.ts` both gate on a PARTICIPANT token for this event. A
  coordinator becomes a live auto-nudge and decide-by recipient. **That is correct under the
  ruling** — they own an item and are chased like any other adult — but it is a behaviour change
  to [[GTC-178]]'s and [[GTC-175]]'s shipped machinery and must be asserted in a test, not
  inferred from this sentence.
- **Zone 1, the role-scoped session cookies.** GTC-001's fix (no ticket file in `docs/tickets/`) separates host and participant
  sessions. A person who can open both `/p/` and `/c/` will hold two scoped sessions. Read the
  cookie scoping and confirm they do not collide. Do not change it here.
- **The shared-link claim list.** `src/app/join/[token]/page.tsx` lists
  `people where role: 'PARTICIPANT'`, so a coordinator is still not a claimable name. Whether
  they should be is not decided here.
- **No constraint change.** `AccessToken`'s `@@unique([eventId, personId, scope, teamId])` already
  admits both rows: the PARTICIPANT token carries `teamId: null`, the COORDINATOR one carries the
  team.
- **The two existing sweeps stay exactly as they are.** The COORDINATOR prune at the top of
  `ensureEventTokens`, and the `role: 'HOST'` PARTICIPANT revocation [[GTC-256]] phase 3 added
  under narrow founder approval. Neither is widened by this ticket.

---

### Scope

**Fix here:**
- Step 4 of `ensureEventTokens`, and the note at the top of `src/lib/tokens.ts` that states the
  old rule.
- A regression test proving a coordinator who owns an item holds both tokens and that their ask
  link resolves.

**Do not touch:**
- All Do-Not-Touch Zones in `GATHER-BUILD-CONSTANTS.md` are binding; Zone 3 is entered under
  approval and nothing wider than step 4.
- The COORDINATOR prune and the HOST revocation in the same function.
- `validateChannelTarget` — that is [[GTC-248]]'s.
- The child rule in `src/lib/eligibility/child-exclusion.ts`.

---

### Acceptance

**Test gate:** Medium — regression test preferred, and the token assertions are cheap.

**Assertions**
- [ ] A `role: 'COORDINATOR'` membership on an event past `ensureEventTokens` holds a
      COORDINATOR token **and** a PARTICIPANT token.
- [ ] The pre-flight message route returns a real link for that person rather than the stand-in.
- [ ] A team's `coordinatorId` who has no `role: 'COORDINATOR'` membership is treated the same
      way, or is excluded deliberately with the reason recorded.
- [ ] The COORDINATOR prune still revokes a token whose team moved.
- [ ] [[GTC-256]]'s host revocation is unaffected — `test:host-never-messaged` green.
- [ ] The auto-nudge and decide-by finders now return a coordinator who owns an item, asserted
      directly.
- [ ] Adjacent flows: `test:security`, `test:nudge-cadence`, `test:decide-by-followup`,
      `test:host-household`.

---

### Unknowns

1. **A coordinator who owns nothing.** Tokens are issued at the transition to CONFIRMING and
   again at the press, before items are settled, so conditioning issuance on holding an item
   would churn. [[GTC-187]] decision 8 also asks itemless guests about attendance. Whether an
   itemless coordinator is asked at all is the founder's, and it decides whether the token is
   conditional or unconditional.
2. **Whether the `/c/` page should point at their own items**, or whether the ask link alone is
   the answer.
3. **Whether any event holds a coordinator token for someone who is no longer a coordinator** —
   the prune covers current state; a survey before shipping is cheap.

---

### Stop Conditions

Standard eight from `BUG-TICKET-TEMPLATE.md`. Live notes:

- **5 (Do-Not-Touch zones):** **pre-fired.** Zone 3. Approval before code, narrow as built.
- **8 (required unknown):** unknown 1 blocks the issuance condition. If the answer is needed to
  write the change, stop and ask.
- **7 (schema migration):** not expected — `AccessToken`, `TokenScope` and the constraint all
  exist.

---

### Executor Checklist

[ ] GATHER-BUILD-CONSTANTS.md read
[ ] Citations section read; every citation in this ticket is a symbol, not a line number
[ ] Preflight passed and results pasted
[ ] [[GTC-262]] confirmed closed
[ ] Zone 3 shape surfaced and approved before any code
[ ] Fix implemented within declared scope
[ ] GREEN confirmed
[ ] Acceptance assertions checked
[ ] Adjacent flows verified
[ ] Commit hash recorded
[ ] No stop conditions triggered without resolution

---

## Evidence (Executor-Completed)

> Filled in by the executor before committing.

**Root cause confirmed**

**Files changed**

**Test results**
- RED:
- GREEN:

**Assertions checked**

**Security suite:**

**Commit hash:**
