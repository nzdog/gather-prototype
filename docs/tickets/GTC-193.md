---
ticket: GTC-193
title: "J2 — Criticality badge in the grid"
status: closed
branch: feat/moment-one-redesign
commit: 2901ac7, 40d457c
closed_under: "[[GTC-192]]"
moment: 4
type: build
depends_on:
  - "[[GTC-192]]"
  - "[[GTC-170]]"
tags: [moment-4, epic-j, criticality]
---

# GTC-193 — J2: Criticality badge in the grid

**Status:** CLOSED | **Branch:** `feat/moment-one-redesign` |
**Commit:** `2901ac7`, `40d457c` (filed `e5f6e82`) | **Effort:** high

> **⚠ THIS TICKET'S WORK LANDED UNDER ANOTHER NUMBER. Closed 2026-09-12 by
> reconciliation, not by an executor.**
>
> Both Acceptance criteria hold in the tree and **no commit names GTC-193.** The work
> shipped inside [[GTC-192]] (J1), the grid this badge lives in:
>
> - `40d457c` *feat(GTC-192): phase 4 — tap-for-actions, and Rulings 17-19*
> - `2901ac7` *feat(GTC-192): phase 7 ruled and shipped — the reading panel, the door
>   treatment; GTC-192 CLOSED*
>
> `row.critical` renders inside the person's box at `PersonSurface.tsx:277`, carried
> there by `reading.ts:180`. Criticality drives no sort and no float-to-top anywhere in
> the grid — checked by grep for `sort`/`order` across `src/components/glance/` and
> `src/lib/glance/`, which is the exact contradiction B1 fixed once in the coordinator
> view and the thing this ticket was filed to keep out of J1.
>
> **The badge is a plain text marker, not a styled chip** — `<span>critical</span>` in
> the grid's own muted tone. That reads as satisfying "show a badge"; if a stronger
> treatment is wanted it is a new ticket, not a reopening of this one.

*Effort level is a proposal from the discovery-report ticket breakdown — run `/effort` manually
before execution.*

### Context

Moment 4 spec §8.2: critical items carry a badge (set at menu time, Moment 2 — B1, GTC-170)
visible in the grid. No float-to-top, no message-only. "Four seconds can tell the lamb from the
ice before anything goes wrong; a badged amber box still means rest." The assistant's one plain
message remains the escalation when a critical goes red (already built by J1, GTC-192).

*Quote updated 2026-08-30: the spec's "yellow" was renamed to "amber" by founder ruling ([[GTC-192]] Ruling 12). The quote above tracks the corrected spec text; the substance is unchanged.*

### Scope

**Fix here:**
- Render B1's criticality data as a badge within J1's person-primary grid, on the specific item
  inside a person's box.
- Confirm no float-to-top or reordering logic sneaks in (the exact contradiction B1 already fixed
  once, in the coordinator view — don't reintroduce it here).

**Do not touch:**
- All Do-Not-Touch Zones in `GATHER-BUILD-CONSTANTS.md`.
- B1's underlying criticality-setting logic — consumed, not rebuilt.

### Dependencies

J1 (GTC-192), B1 (GTC-170).

### Acceptance

- [ ] Critical items show a badge inside their person's box in the grid.
- [ ] No reordering/float-to-top behavior exists anywhere in the grid based on criticality.
- [ ] `npx tsc --noEmit` clean, build passes.

### Stop Conditions

Standard eight from `BUG-TICKET-TEMPLATE.md`.

### Closed ticket format (for the executor)

```
# GTC-193 — J2: Criticality badge in the grid — CLOSED
Status: Complete | Branch: [...] | Commit: [hash] | Effort: high
## What was built
## Test results
## Assertions met
## Evidence
```
