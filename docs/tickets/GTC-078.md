---
ticket: GTC-078
title: "Add demo link to logged-in home page"
status: closed
branch: gtc-042-part1-nudge-schema
commit: 8801aa4
moment: null
type: build
depends_on: []
blocks: []
tags:
  - build
  - home-demo
---

# [[GTC-078]] — Add demo link to logged-in home page — CLOSED

**Status:** Complete
**Branch:** gtc-042-part1-nudge-schema
**Commit:** 8801aa4

### What was found
- Single component `src/app/page.tsx` with `const user = await getUser()` — conditional rendering for logged-in vs logged-out
- "See it in action →" link (lines 35-42) was wrapped in `{!user && ...}`, hiding it from logged-in users
- Layout already used `flex-col sm:flex-row` with `gap-4` — both buttons render side by side on desktop
- Styling identical for both states — outlined/ghost secondary button with `border-2 border-accent`

### What was changed

**`src/app/page.tsx`**
- Removed `{!user && ...}` guard around the "See it in action →" link
- Link now renders for both logged-in and logged-out users
- No styling or layout changes needed — existing flex layout already handles two buttons side by side

### Known limitations / follow-up flags
- None

### Assertions met
- [x] "See it in action →" link visible on logged-in home page
- [x] Link goes to /demo
- [x] Styled as secondary/outlined button
- [x] Layout matches logged-out home page CTA area
- [x] Logged-out home page unaffected
- [x] Security suite 16/16 passing

### Evidence
- Demo link confirmed: yes
- Commit: 8801aa4
