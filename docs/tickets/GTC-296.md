---
ticket: GTC-296
title: 'STUB — every email carries a way out: Gather has no email opt-out'
status: open
branch: TBD
commit:
moment: 4
type: feature
depends_on: []
blocks:
  # FOUNDER RULING 2026-09-18: a precondition of GTC-189 SLICE 8, the chase, and
  # not only of the slices 5-7 release. Under ruling AH the scheduler's chase
  # email is the COMMON CASE, ruling Q says every email carries a way out, and
  # Gather has no email opt-out of any kind. See "Consequences recorded".
  - '[[GTC-189]]'
related:
  - '[[GTC-189]]'
  - '[[GTC-178]]'
  - '[[GTC-191]]'
  - '[[GTC-247]]'
  - '[[GTC-294]]'
tags: [email, opt-out, zone-7-adjacent, recipient-model, stub]
---

# GTC-296 — Every email carries a way out

**Status:** Open — filed from the founder rulings of 2026-09-14, not started, **not scoped** |
**Branch:** TBD
**Severity:** not set at filing — the founder's to set.

**Filed on the founder's instruction**, from ruling Q in [[GTC-189]] (*Founder rulings — third
pass*). This ticket records the ruling and what it meets. It proposes no shape.

> EVERY EMAIL CARRIES A WAY OUT — the invitation and both reminders. Gather has no email opt-out
> today, so this is new work.

> ⚠ **THIS TICKET CHANGED SPECIES ON 2026-09-15 AND THE BANNER BENEATH STILL READS CORRECTLY.
> CORRECTED 2026-09-18 UNDER [[GTC-189]] RULING AH.**
>
> The AG banner below says the chase's email leg is *"gated on a per-person tick"* and that
> *"a ticked person receives day-four and day-seven reminders by email"*. **Under ruling AH there is
> no tick.** The event-level default *"chase by email when there's no mobile"* is ON, and the
> per-person control is the **named exception**, meaning *hand this one to me*. So the population
> that receives automatic email reminders is **everyone with an address and no usable mobile, minus
> the people the host has named** — not a hand-picked few.
>
> **The scope, restated:** the invitation, **both scheduler reminders for the DEFAULT population**,
> the by-hand nudge, and the wrap-up thank-you. The AG banner's own citation of decision 25's 95.1%
> is the tell: that figure was always the size of this population, and under AG it was being read as
> the size of a set the host would have had to tick one by one.
>
> ⚠ **THE CONSEQUENCE — RULED 2026-09-18, AND IT MOVES THIS TICKET IN THE ORDERING.** Under AG an
> automatic chase email was the exception, so an unsubscribe could reasonably wait. Under AH it is
> **the common case**, and ruling Q says every email carries a way out.
>
> **THE RULING, in the founder's words:**
>
> > It is a precondition of slice 8, not only of the 5-7 release. Under AH the scheduler's chase
> > email is the common case, ruling Q says every email carries a way out, and Gather has no email
> > opt-out of any kind. Shipping the chase without one means most of a host's guests get two
> > unsubscribable emails from a service they never signed up to.
>
> **So this ticket now blocks [[GTC-189]] slice 8 as well as binding the slices 5–7 release** —
> recorded in the frontmatter, in *Consequences recorded* below, and in slice 8's own build-shape
> entry. It is the first precondition added to that shape by a ruling rather than left to the
> founder, and the shape's own preamble now says so.

⚠ **PARTLY RESTORED 2026-09-15 BY [[GTC-189]] RULING AG, WHICH SUPERSEDES RULING Y — read this
before the narrowing below.** *[⚠ IN AG'S POLARITY — read the AH correction directly above first.
Kept legible because the sequence is the record.]* Under AG the chase's email leg comes back, gated
on a per-person tick the host makes at the pre-flight ([[GTC-311]]). **A ticked person receives
day-four and day-seven
reminders by email, from the scheduler**, so ruling Q's *"both reminders"* has automatic email to
reach again for that population — which [[GTC-189]] decision 25 measures at 95.1% of recipients in
gather_dev. **So this ticket's scope is the invitation, both scheduler reminders for ticked people,
the by-hand nudge and the wrap-up thank-you.** The narrowing recorded below was true of ruling Y and
is kept legible; it is not current.

⚠ **NARROWED 2026-09-15 BY [[GTC-189]] RULING Y — "both reminders" no longer has anything to
reach.** *[SUPERSEDED — see directly above.]* Ruling Y settled decision 15 WIDE: a chase that cannot text hands over to the host rather
than falling to email, so **the scheduler never sends a chase email at all**. The principle is
unchanged and its population is smaller. What this ticket now covers:

- **the invitation**, which is the ask and is email-first — unchanged by ruling Y;
- **the by-hand nudge**, `sendNudgeEmail` from `POST` in
  `src/app/api/events/[id]/people/[personId]/nudge/route.ts`, which ruling AD keeps alive and which
  is now the **only** chase email in the product ([[GTC-310]]);
- **the wrap-up thank-you**, the other `sendNudgeEmail` caller, in `src/lib/wrap-up.ts`.

The automatic day-four and day-seven reminders are not on this list because under ruling Y they do
not exist by email. **Do not read the narrowing as dropping the by-hand nudge** — it is the one
survivor of the category the ruling was written for.

---

### What does not exist today — verified at `5aeaf0b`

- **Opt-out is SMS only.** `Person.smsOptedOut`, `Person.smsOptedOutAt` and the `SmsOptOut` model
  in `prisma/schema.prisma` are the whole of it, and `OPT_OUT_KEYWORDS` in
  `src/lib/sms/opt-out-keywords.ts` reads inbound texts. Do-Not-Touch Zone 7 names those and
  nothing about email.
- **No email opt-out state exists for any sender to read.** The senders are `sendNudgeEmail`,
  `sendWelcomeEmail` and `sendMagicLinkEmail` in `src/lib/email.ts`.

### The three emails the ruling names — none of them built

- **The invitation** — the ask by email, [[GTC-189]] slice 5.
- **Both reminders** — the chase's day-four and day-seven email legs, [[GTC-189]] slice 8. The
  chase sends SMS only today ([[GTC-178]]'s machinery).

### ⚠ Two guest-facing emails exist today, and the ruling does not name them

The ruling's headline is *"every email"*; its list names three, none of which is built. A guest can
receive two emails from the tree today:

- **The manual nudge** — `sendNudgeEmail`, from the route in
  `src/app/api/events/[id]/people/[personId]/nudge/route.ts`, when `chooseManualNudgeChannel`
  returns `email`.
- **The wrap-up thank-you** — `sendNudgeEmail`, from `src/lib/wrap-up.ts`.

Whether either carries the way out is the founder's. `sendWelcomeEmail` (to the host, at event
creation) and `sendMagicLinkEmail` (sign-in) are account mail rather than messages to guests, and
are named only so the question is visible.

---

### Consequences recorded — not scoped

- **An email no is a no to being chased.** Ruling O of the same pass: *"A no to one channel is
  treated as a no to being chased."* Read as written, a person who takes the email way out is not
  then chased by text. Confirm at scoping; recorded so it is met rather than discovered.
- **The invitation carries it from the first send.** [[GTC-189]] slices 5, 6 and 7 land together
  and send the invitation. If the ruling binds the first send, this ticket is a precondition of
  that release, as [[GTC-294]] and [[GTC-191]] are. **Not added to the build shape here** — the
  founder's call.
- ⚠ **AND IT IS A PRECONDITION OF [[GTC-189]] SLICE 8 — FOUNDER RULING, 2026-09-18, NOT A
  CONSEQUENCE LEFT RECORDED.** Raised as the executor's finding on the polarity sweep and ruled the
  same day.

  Under ruling AG the scheduler's chase email reached a hand-picked few, so an unsubscribe in it
  could reasonably follow. Under ruling AH the default is ON and the per-person control is the named
  exception — *hand this one to me* — so **an automatic chase email becomes the common case**, 95.1%
  of recipients on decision 25's measurement. Ruling Q says every email carries a way out.

  **The founder's reason, and it is the sentence to keep:**

  > Shipping the chase without one means most of a host's guests get two unsubscribable emails from
  > a service they never signed up to.

  **What moves:** this ticket now carries `blocks: [[GTC-189]]` in its frontmatter, and slice 8's
  build-shape entry names it as a precondition. It binds the slices 5–7 release through the bullet
  above AND slice 8 through this one — the invitation and the chase are two different sends, and
  ruling Q reaches both. **Severity is untouched; the founder has not set it and this ruling is
  about ordering, not weight.**
- **Where the gate sits.** The house order for SMS — opt-out first, then the don't-chase mark — is
  recorded in `src/lib/eligibility/nudge-mark.ts`. Where an email opt-out sits is scoping.
- **Zone 7 adjacency.** An email opt-out is the same kind of suppression as Zone 7's and is not
  inside it. Whether `GATHER-BUILD-CONSTANTS.md` gains a zone for it is a decision for when it
  exists.
- **The environment.** THE VOICE (2026-09-13) sends from Gather's verified domain. The dev
  environment has none and its Resend key does not authenticate ([[GTC-247]]), so a live test
  waits on that.

---

### Unknowns

1. **The mechanism** — the stored state, the link, and whether a header-level unsubscribe
   (`List-Unsubscribe`) is wanted beside a link in the body.
2. **The reach of a no** — per event, per host, or global. `Person` is shared across events.
3. **The manual nudge and the wrap-up thank-you** — above.
4. **Whether New Zealand's Unsolicited Electronic Messages Act 2007 applies** to a host's
   invitation sent from Gather's domain. Not established.

---

### Stop Conditions

Standard eight from `BUG-TICKET-TEMPLATE.md`. ⚠ **Zone 7 adjacent** — flag for explicit approval.

### Acceptance

Not written. Scoping writes it.
