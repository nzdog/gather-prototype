---
ticket: GTC-251
title: "E6 — Nudge exhaustion and the red door"
status: open
branch: TBD
commit:
moment: 4
type: build
related:
  - "[[GTC-179]]"
depends_on:
  - "[[GTC-178]]"
  - "[[GTC-180]]"
tags: [moment-4, hinge, epic-e, nudges, exhaustion, red-door]
---

# GTC-251 — E6: Nudge exhaustion and the red door

**Status:** Open | **Branch:** TBD | **Commit:** [pending] | **Effort:** high (proposed —
newly split from [[GTC-178]]'s original xhigh estimate per founder ruling 2026-08-23; run
`/effort` manually before execution)

### Context

Split out of [[GTC-178]] (E1) by founder ruling, 2026-08-23. GTC-178's own ticket text
originally assigned this scope to itself ("Wiring the expired state into the exhausted-silence
red is this ticket's, through whatever door you build") — that assignment is superseded; this
ticket is now the owner. Rationale for the split, recorded verbatim from the ruling: "cadence
has a testable definition of done today; exhaustion needs a counter, a rule, and the red state on
a Moment 4 screen that does not exist and is blocked on its own open decisions. Splitting delays
nothing real."

Moment 4 spec §4/§8.1:

> After the system's nudges are exhausted, the silence goes red — it comes to Kate. The door
> swings both ways at red: tapping it, Kate can (a) flick it back to orange for more system
> tries, with a retry count she specifies, or (b) keep it red and handle it herself.

§8.1 additionally: "There is a point where 'I'm still nudging' stops being true because there's
no time left for nudging to work — and that point is set by Kate... Red's exhaustion semantics
survive intact: at her line the system genuinely is out of moves — the calendar is a second way
to exhaust, not a new meaning for red." ~~That calendar-based path to red is already built and
closed — [[GTC-180]] (E3, host-set red-by-time line).~~ **CORRECTED 2026-08-27: [[GTC-180]] is
OPEN, not built.** Its frontmatter reads `status: open` with an empty `commit:`, and its Scope
still owns both the line itself and *"Truncate E1's ([[GTC-178]]) nudge schedule at this line."*
The original sentence was wrong when written; it is struck rather than deleted so the error stays
visible. Consequences: **neither** path to red is built today, and GTC-178 cannot implement Moment
4 §8.3's cadence truncation — the line it would truncate at does not exist yet. This ticket's
acceptance criterion "both paths terminate at the same red" therefore depends on GTC-180 landing,
which the Dependencies section below now records. **This ticket is the other path: exhaustion by
nudge count**, which is wholly unbuilt.

**No counter exists today.** `HouseholdMember.proxyNudgeCount` (plus `lastProxyNudgeAt`,
`claimedAt`, `escalatedAt`) was a real per-event incrementing counter and was deleted with no
replacement in commit `7606201` ("feat(GTC-101): add Household model and HouseholdRole enum for
Moment 1 redesign") — confirmed via `git log -S "proxyNudgeCount"`, not inferred from comments.
`Person.nudge24hSentAt`/`nudge48hSentAt` are two booleans, not a count, and are global on
`Person` (see [[GTC-178]] Ruling 2 — that leak is being fixed there, not here). The `NudgeLog`
Prisma model (`personEventId`, `nudgeType`, `scheduledFor`, `sentAt`, `status`, `errorMessage`) is
schema-present, per-event-keyed, and has an applied migration
(`20260407021304_add_nudge_log`) — but has **zero production writers**; every touch of it in the
current codebase is a cascade-delete test fixture (`tests/household-edit-*-test.ts`,
`tests/verify-personevent-team-setnull.ts`). ~~GTC-178's own ticket now records a recommendation
(not yet ruled) that its clock-fix write to per-send state should land in `NudgeLog` rather than
new columns, specifically so this ticket's count can be `COUNT(*)` over the same rows rather than
a second migration. Confirm that recommendation (or override it) before building the counter
here.~~ **SUPERSEDED 2026-08-27.** That recommendation was **overridden** by GTC-178's Ruling 4
(2026-08-23): sent-dedup state lands on `PersonEvent` as two fixed timestamp columns —
`firstNudgeSentAt`/`secondNudgeSentAt` per its Ruling 7 (2026-08-27) — and `NudgeLog` keeps its
zero writers. **So the `COUNT(*)`-over-shared-rows shortcut this paragraph hoped for does not
exist.** Ruling 4 says why in its own words: fixed sent-timestamp columns "cannot express" a
host-specified retry count "regardless of which table this ticket picks," so **this ticket needs
its own storage decision either way** — that was never avoidable through GTC-178's choice. Treat
Stop Condition 9 as satisfied on the *confirmation* limb only.

**The red state itself has nowhere to render yet.** [[GTC-192]] (J1, the person-primary grid) is
open and undecided — "Today: the Moment 4 screen doesn't exist in code at all." This ticket can
build the counter, the exhausted predicate, and the retry-count logic without GTC-192, but the
visible "door" Kate taps is not renderable until GTC-192 lands.

**`isDecideByExpired()` is waiting, unconsumed.** `src/lib/decide-by.ts`'s own docstring:

> GTC-175 RULING (a): D2 makes expiry a CORRECT, DERIVABLE STATE and pins it with tests. D2 does
> NOT surface it. The "standard door" Hinge §8 sends an expired maybe through — exhausted-silence
> red (GTC-178 / E1) and the person-grid colour encoding (GTC-192 / J1) — does not exist yet.
> Both tickets are open, both are told this predicate is here waiting, and neither is reached
> into from D2. The state is expressible and correct; the red is theirs to render when they
> build the door.

That comment names GTC-178 as the eventual owner. Per today's split, **that reference now points
here.** It is not corrected in the code comment itself — no code changes in this pass — but
whoever executes this ticket should update it as part of the work.


### ⚠ Added 2026-08-27 by [[GTC-179]] (E2) — don't-chase makes `nextNudgeAt` null from moment zero

**This ticket must distinguish NO cadence from a SPENT cadence.** GTC-179's Phase 0 gate ruled
that a person marked `DONT_CHASE` resolves to an empty offset array, and an empty cadence makes
`nextNudgeAt` in `src/lib/nudge-cadence.ts` return null **immediately** — not after two nudges,
but from the instant the mark is set. A `GENTLE` person reaches null after one nudge rather than
two, on a different day from everyone else.

`nextNudgeAt`'s own docstring already warns "NULL DOES NOT MEAN RED... whether that is
exhaustion, and what colour it earns, is GTC-251's (E6) to decide." That warning was written
against a world with one cadence. **An exhausted predicate written as "`nextNudgeAt === null`"
would turn every don't-chase person red the instant they are marked — inverting the control.**
Kate says *stop chasing my mother*, and the system escalates her mother to the host. That is the
exact opposite of what §10.3's off-switch is for.

Ordering: **GTC-179 lands first** (it is unblocked; this ticket additionally waits on
[[GTC-180]]), so the reconciliation is this ticket's to absorb, not GTC-179's to pre-empt.

Two further consequences worth checking when this ticket is scoped:
- A **retry count** (§4's "with a retry count she specifies") applied to a don't-chase person
  would re-open nudging for someone the host switched off. Retry must not override the mark.
- The **exhaustion count** this ticket derives is a count against a cadence whose length is now
  variable — 0, 1 or 2 legs depending on pace and mark. "Exhausted" cannot be hard-coded to two.

GTC-179 does not settle any of this; it records the collision so this ticket is not surprised by
it. See GTC-179's "Recorded from the Phase 1 audit (2026-08-27)", item (a), and its Ruling 1 on
gentle being a volume control and not a handback control.


### Scope

**Fix here:**
- A per-event nudge-send record sufficient to derive a count (storage location: see GTC-178's
  storage-question recommendation — confirm with the founder before building; do not assume it
  without checking GTC-178's current text).
- An exhausted predicate: pure, client-safe, testable — matching the shape of
  `isDecideByExpired()` in `src/lib/decide-by.ts` rather than inventing a new pattern.
- The host-specified retry-count mechanism: "flick it back to orange for more system tries, with
  a retry count she specifies" (§4). Data/logic layer first — the tap-to-act UI itself is
  GTC-192's (J1) surface, not this ticket's.
- Wire `isDecideByExpired()` into the same exhaustion semantic this ticket's count-based
  exhaustion produces — one red, two ways to reach it (count-exhausted, time-exhausted via
  GTC-180), per §8.1's "the calendar is a second way to exhaust, not a new meaning for red."
- Update the stale GTC-178 reference in `src/lib/decide-by.ts`'s docstring to point here.

**Do not touch:**
- All Do-Not-Touch Zones in `GATHER-BUILD-CONSTANTS.md`.
- [[GTC-178]]'s day-4/day-7 cadence timing logic itself — consume its output (and its per-send
  records, once that ticket lands), do not modify how/when nudges fire.
- [[GTC-192]]'s actual grid rendering, tap-surface, and assistant message — this ticket supplies
  the predicate and retry-count data GTC-192 consumes; building the visible screen is J1's job.
- The proxy-nudge path — out of scope here; its own frequency-limiting gap is [[GTC-252]], a
  separate problem from person-level nudge exhaustion.

### Dependencies

[[GTC-178]] (E1) — needs the day-4/day-7 cadence, and its storage-question recommendation
settled, before a per-event count can be built against real send records. **Settled 2026-08-23 by
GTC-178's Ruling 4** (`PersonEvent` columns, not `NudgeLog`) and **named 2026-08-27 by its Ruling
7** (`firstNudgeSentAt`/`secondNudgeSentAt`) — two fixed timestamp columns, which as GTC-178's own
Ruling 4 records **cannot express a retry count**, so this ticket still needs its own storage
decision. Stop Condition 9 below is satisfied as to *confirmation* (the recommendation was
overridden, on the record) but not as to *this ticket's own* count storage.

[[GTC-180]] (E3) — **added 2026-08-27.** Open, not built (see the correction in Context). The
acceptance criterion "`isDecideByExpired()` is wired into the same exhaustion-red semantic as
count-based exhaustion — both paths terminate at the same red" cannot be fully met until GTC-180
supplies the time-exhausted path. [[GTC-192]] (J1) is
downstream of this ticket (not a dependency of it) — GTC-192's own frontmatter already lists
GTC-178 as a dependency for "E1's nudge/colour state"; once this split is reflected there, that
dependency effectively becomes this ticket. Not corrected in GTC-192 itself in this pass — no
code/doc changes outside GTC-178, GTC-251, GTC-252 in this pass — flagged for whoever picks up
GTC-192 or this ticket next.

### Acceptance

- [ ] A per-event nudge-send record/count exists; storage location was confirmed (or explicitly
      overridden) against GTC-178's recorded recommendation before implementation began.
- [ ] An exhausted predicate exists, pure and testable, consistent in shape with
      `isDecideByExpired()`.
- [ ] A host-specified retry count exists at the data/logic layer and reopens nudging when set.
- [ ] `isDecideByExpired()` is wired into the same exhaustion-red semantic as count-based
      exhaustion — both paths terminate at the same red, per §8.1.
- [ ] The stale GTC-178 reference in `src/lib/decide-by.ts`'s docstring is updated to cite this
      ticket.
- [ ] `npx tsc --noEmit` clean, build passes, security suite exits 0.

### Stop Conditions

Standard eight from `BUG-TICKET-TEMPLATE.md`, plus:
9. GTC-178's storage-question recommendation has not been explicitly confirmed or overridden by
   the founder — stop and ask before choosing where send records live.
10. GTC-192 has not landed — this ticket may still build the counter/predicate/retry-count data
    layer, but must not attempt to build a visible red-door UI; if the scope seems to require
    touching a grid/screen component, that is scope expansion into GTC-192 — stop and report.

### Closed ticket format (for the executor)

```
# GTC-251 — E6: Nudge exhaustion and the red door — CLOSED
Status: Complete | Branch: [...] | Commit: [hash] | Effort: high
## What was built (incl. storage location chosen and why)
## Test results
## Assertions met
## Evidence
```
