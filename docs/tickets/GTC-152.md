# GTC-152 — Tier 1 safe deletes — CLOSED

**Status:** Complete | **Branch:** feat/moment-one-redesign | **Commits:** 82544b6 (code), 05bc621 (schema)

**Severity:** Medium (technical debt, complexity reduction)
**Surfaced by:** v1-v2 reconciliation review, Tier 1 prune list (`docs/v1-v2-reconciliation-review.md`, section 1). Audit cross-checked against the actual review list, re-verified with fresh greps at HEAD.

### Phase 1 audit (per-surface classification)

**SAFE DELETE — code:**
1. **Suggestion routes** — all five files under `src/app/api/events/[id]/suggestions/` (list, `[suggestionId]`, accept, dismiss, explain). Zero callers anywhere in src/; the conflict UI uses the separate `conflicts/[conflictId]/suggest-resolution` family.
2. **Chain-dependent on #1:** `generateExplanation` + `generateMockExplanation` + `AIExplanationResponse` in `src/lib/ai/generate.ts` (sole caller: the explain route) and `EXPLANATION_SYSTEM_PROMPT` + `buildExplanationPrompt` in `src/lib/ai/prompts.ts` (sole caller: `generateExplanation`), plus the two orphaned imports.
3. **501-stubbed members endpoint** — `households/[householdId]/members/route.ts` ("being redesigned for the new household model"); zero client callers.
4. **`tests/measure-moment2-prompts.ts`** (audit addition) — broken since GTC-146: imported six deleted symbols (`buildSectionPrompt`, `buildGapPrompt`, `buildDietaryCoveragePrompt`, `buildThingsToConsiderPrompt`, `MAX_TOKENS_SECTION_GENERATION`, `MAX_TOKENS_GAP_FILL`) and was the only remaining reader of `generatedData`. Not in package.json; only mentioned in GTC-127's closed doc (history, retained).

**SAFE DELETE — schema:**
5. **`EventSetup.generatedData`** — zero src readers/writers; 0 rows with data in dev.
6. **`StructureChangeRequest` model** + `Event.structureChanges` relation + private enums `StructureChangeType`/`StructureChangeStatus` (used by nothing else in schema or src). No API routes ever existed for it. 0 rows in dev.

**VERIFIED LIVE — excluded (with evidence):**
- **`/api/events/[id]/days`** — the review flagged this low-confidence; audit resolves it as **live**: fetched at `page.tsx:681`, `days` state passed as props at `:2582`/`:2592`. Kept.
- **`/api/events/[id]/generate`** — three live callers (`page.tsx:889`, `:1018` selective regenerate, `/demo/review:62`). Tier 2, blocked on the regenerate/demo decision. Kept.
- **GTC-146 per-section leftovers in src/** — already fully removed by GTC-146; zero hits outside the broken test script (#4).
- PlanRevision, HostMemory/HostPattern/HostDefault, QuantitiesProfile, DeletionReceipt — Tier 2/3 per the review. Kept.

Scope approved by Nigel in full (including the script deletion and the table+enum drops) before Phase 2.

### What was deleted (code)

10 files changed, **638 deletions, 0 insertions** (excluding the new migration):
- 5 suggestion route files, 1 members route file, 1 broken test script — deleted whole.
- `generate.ts` −65 lines (explanation function, mock, response type, 2 imports).
- `prompts.ts` −53 lines (EXPLANATION prompt const + builder).

### What was deleted (schema)

Migration `20260708081323_drop_generated_data_and_structure_change_request`, generated `--create-only` and reviewed before apply. Exactly five statements: drop `StructureChangeRequest_eventId_fkey`, `ALTER TABLE "EventSetup" DROP COLUMN "generatedData"`, `DROP TABLE "StructureChangeRequest"`, `DROP TYPE` × 2. No data movement (both objects empty). Reversible: re-add nullable Json column / recreate empty table+enums — nothing ever wrote to either (`generatedData` writes ceased at GTC-146; StructureChangeRequest had no writers, ever).

### Intentional non-deletions (with reasoning)

- `/api/events/[id]/generate` + `generate.ts`'s remaining pipeline + the legacy prompt surface — Tier 2, deletable only after the regenerate-for-V2 and `/demo/review` product decisions (review section 1, Tier 2).
- `/api/events/[id]/days` — live (evidence above); the review's low-confidence flag is now resolved as keep.
- `LEGACY_EVENT_TYPE_MAP`, PlanRevision and the other half-wired models — Tier 2/3 per the review.

### Browser verification result

Playwright, authenticated host, fixture V2 event with a pre-existing EventSetup row. 8/8 PASS:
- Landing page loads; V2 dashboard renders; GTC-149 hides intact (no V1 Generate button).
- Moment 2 flow walked to Step 1 modal; the pre-existing EventSetup row loads correctly post-column-drop (Christmas chip selected).
- Step 1 dietary autosave → **200** (EventSetup writes work against the migrated table).
- Deleted routes return **404** live (suggestions GET, members POST).
- No console errors beyond the deliberate 404 probes.

`npx tsc --noEmit` clean (post-build; the only transient errors were stale `.next/types` stubs for the deleted routes, regenerated by the build) · `npx next build` Compiled successfully · security suite 16/16 · preflight `console.log` = 0 · fixtures removed.

### Known limitations

- Live plan generation (finalize-plan → Claude) not exercised — placeholder API key in this environment, consistent with the verification boundary approved in GTC-150. The generation path's code was not touched by this ticket.
- The Tier 1 prune shrinks but does not close the reconciliation: Tier 2 (V1 generate/regenerate pipeline, `/demo/review`, PlanRevision) awaits its product decisions.

### Assertions met

1. ✅ Phase 1 audit with per-surface classification, surfaced and approved
2. ✅ All safe-delete surfaces removed (6 categories, 10 files)
3. ✅ `EventSetup.generatedData` dropped via reviewed migration
4. ✅ No orphan imports (tsc clean)
5. ✅ Live V2 flows verified functioning (browser, 8/8)
6. ✅ TypeScript clean
7. ✅ Build passes
8. ✅ Migration reversible (empty objects; single-statement re-adds)
9. ✅ Branch verified: `feat/moment-one-redesign`

### Evidence

- Phase 1 grep outputs with file:line references (audit above)
- `git diff --stat`: 10 files, 638 deletions + schema/migration
- Migration SQL quoted above; live-DB drops confirmed via `to_regclass` + `information_schema` + `db pull`
- Browser verification: 8/8 PASS
- Commits: 82544b6 (code), 05bc621 (schema)
