---
ticket: GTC-075
title: "Update wrap-up message templates"
status: closed
branch: gtc-042-part1-nudge-schema
commit: 80987fe
moment: null
type: build
depends_on: []
blocks: []
tags:
  - build
  - wrap-up
  - sms
  - language-copy
---

# [[GTC-075]] — Update wrap-up message templates — CLOSED

**Status:** Complete
**Branch:** gtc-042-part1-nudge-schema
**Commit:** 80987fe

### What was found

**Current SMS template** (`src/lib/sms/wrap-up-templates.ts`):
```
Hope {eventName} was a great one! Thanks for bringing {guestTaskItem} — it made a difference. If you've ever got an event to organise, this might help: {newEventLink} — {hostFirstName}
```

**Current email subject:** `Thanks for making {eventName} happen`

**Current email body:**
```
Hey {guestFirstName},

Hope {eventName} was everything it should have been. Thanks for bringing {guestTaskItem} — having people you can count on makes all the difference.

If you've ever got an event to pull together — birthday, Christmas, end of season — Gather makes it easy. Your details are already filled in:
{newEventLink}

— {hostFirstName}
```

**`newEventLink` construction** (`src/lib/wrap-up.ts:40-43`):
`buildStartLink()` returned `${baseUrl}/start/${token}` — pointed into the `/start/[token]` → `/plan/new` flow.

### What was changed

**`src/lib/sms/wrap-up-templates.ts`:**
- SMS with-item: `Hi {guestFirstName}, {hostFirstName} asked me (Gather — the app they used to organise {eventName}) to pass on a thanks for bringing {guestTaskItem}. Much appreciated.`
- SMS fallback: `Hi {guestFirstName}, {hostFirstName} asked me (Gather — the app they used to organise {eventName}) to pass on a thanks for being part of it. Much appreciated.`
- Email subject: `Thanks from {hostFirstName}`
- Email body with-item: same as SMS with-item
- Email body fallback: same as SMS fallback
- Fallback detection: `guestTaskItem === 'what you brought'` (the sentinel value from `resolveGuestTaskItem`)

**`src/lib/wrap-up.ts`:**
- `buildStartLink()` changed from `${baseUrl}/start/${token}` to `${baseUrl}/?token=${encodeURIComponent(token)}`

**`tests/wrap-up-dispatch-test.ts`:**
- Updated all template assertions to exact-match approved copy (with-item and fallback variants)
- Updated `buildStartLink` assertions to verify home page destination with token query param
- Added fallback template test params and assertions
- Total: 31 passing

### Known limitations / follow-up flags
- `/start/[token]` route still exists — existing links in the wild will still work. No changes made to that route per Do-Not-Touch instructions.
- The `newEventLink` field remains in `WrapUpTemplateParams` interface and is still constructed/passed, but is no longer interpolated into message copy. This preserves the data flow for future use if the link is re-added to templates.

### Assertions met
- [x] SMS/WhatsApp with-item template correct
- [x] SMS/WhatsApp fallback template correct
- [x] Email subject correct
- [x] Email body with-item correct
- [x] Email body fallback correct
- [x] newEventLink points to home page
- [x] Security suite 16/16 passing

### Evidence
- Templates updated: confirmed — exact copy assertions pass (31/31)
- Link destination: confirmed — `/?token=` instead of `/start/`
- Commit: 80987fe
