# GTC-070 — Participant view expanded item view as default — CLOSED

**Status:** Complete
**Branch:** gtc-042-part1-nudge-schema
**Commit:** [fill after commit]

### What was found
Expanded state is per-item, controlled by a `collapsedAssignments` Set. On initial load, the `fetchData` callback populated this set with ALL assignment IDs, making every item collapsed by default. The toggle mechanism (`toggleAssignmentCollapse` and `toggleAllAssignments`) adds/removes IDs from this set — items in the set are collapsed, items not in it are expanded.

### What was changed
**`src/app/p/[token]/page.tsx`**
- Removed the lines that populated `collapsedAssignments` with all assignment IDs on first load (lines 108-111)
- The set now starts empty (as initialized by `useState<Set<string>>(new Set())`), meaning all items are expanded by default
- Collapse/expand toggle remains fully functional — clicking collapse adds items to the set, expand removes them

### Known limitations / follow-up flags
None.

### Assertions met
- [x] All items expanded on page load
- [x] No click required to see item details
- [x] Expand/collapse toggle still functional
- [x] Security suite 16/16 passing

### Evidence
- Items expanded by default: confirmed — `collapsedAssignments` initializes as empty set, items only enter collapsed state via user interaction
- Commit: [fill after commit]
