---
ticket: GTC-192
title: "J1 — The screen: person-primary grid"
status: open
branch: feat/moment-one-redesign
commit: 307c5cc, c49fe02, 8792080, c43672e, 40d457c
moment: 4
type: build
depends_on:
  - "[[GTC-170]]"
  - "[[GTC-174]]"
  - "[[GTC-178]]"
  - "[[GTC-251]]"
tags: [moment-4, epic-j, the-screen]
---

# GTC-192 — J1: The screen — person-primary grid

**Status:** Open — **phases 1, 2, 3 and 4 built** (phases 1–3 2026-08-31, phase 4 2026-09-01);
phases 5–7 remain | **Branch:**
`feat/moment-one-redesign` | **Commit:** phase 1 `307c5cc`, phase 2 `c49fe02`, phase 3 `8792080`, phase 3b `c43672e`,
phase 4 `40d457c` (filed `e5f6e82`) |
**Effort:** xhigh

*Effort level is a proposal from the discovery-report ticket breakdown — run `/effort` manually
before execution.*

### Correction (added 2026-08-23)

This ticket's text below still describes GTC-178 (E1) as owning "nudge/colour state" and the
exhausted-silence red. Per founder ruling on GTC-178, 2026-08-23: **GTC-178 owns cadence only.
[[GTC-251]] (E6) owns exhaustion and the red door** — the counter, the exhausted predicate, and
the host-specified retry count were split out of GTC-178 into GTC-251. Frontmatter `depends_on`
above has been updated to add GTC-251; the references below to GTC-178 owning colour/exhaustion
are corrected in place rather than left to mislead a future reader.

### Context

Moment 4 spec §3: the four-second contract — closing it, Kate must know (a) is anything mine to
do, (b) is it moving. Green/orange/red as **ownership, not status**. Behind a tap: all actions
(remind, reassign, take over a silence) — looking never becomes operating. The assistant's one
plain message when a critical item is red. Explicitly refused: messaging/chat, countdown, activity
feed, the plan content itself, analytics/response-rate reporting, progress bars or percentages.

§10.8: **the grid is person-primary.** People are the boxes; items live inside the person. A
person holding items in different states shows the **worst colour**, with all items visible on
tap. "Is the lamb covered?" is answered by the badge (J2, GTC-193) and the assistant's message,
not by inverting the grid.

Today: the Moment 4 screen doesn't exist in code at all — confirmed across every territory in the
discovery report. This ticket is the first thing that builds it, so §10.8 fixes the target data
shape before any code is written: a person-keyed read API, not an item-keyed one.

### Scope

> **Redirect (added 2026-08-30).** The grid description below is superseded by the chosen
> design direction — see **Design direction — chosen 2026-08-30** and **Superseded spec text**
> at the foot of this ticket. The screen is a grid of **neutral household cards with one thin
> colour strip per member**, not a flat grid of per-person boxes. Person-primary,
> worst-colour-wins per person, and tap-for-actions all stand. The section is left unedited
> for the audit trail.

**Fix here:**
- A person-keyed read API assembling per-person state from B1's criticality (GTC-170), D1's
  response model (GTC-174), E1's cadence timing (GTC-178 — e.g. the "nudge in 2 days" display),
  and E6's exhaustion/colour state (GTC-251 — corrected 2026-08-23, was misattributed to GTC-178;
  see Correction note above).
- The grid itself: person-primary boxes, worst-colour-wins when a person holds mixed-state items,
  all items visible on tap.
- Tap-for-actions: remind, reassign, take over a silence — behind the tap, not on the surface.
- The assistant's one plain critical-red message (banner-class, the only one the surface has).
- Confirm the explicit refusal list is honored: no chat, no countdown, no activity feed, no plan
  content, no analytics, no progress bars/percentages.

**Do not touch:**
- All Do-Not-Touch Zones in `GATHER-BUILD-CONSTANTS.md`.
- The runbook (J3, GTC-194) and the criticality badge's specific rendering (J2, GTC-193) — this
  ticket is the grid shell and colour/data mechanics; J2 layers the badge on top.

### Inherited from D2 (GTC-175) — an expired maybe has no colour yet

D2 landed the maybe's decide-by clock. Per founder ruling (a) of 2026-08-05 it stopped at the
state: **this ticket owns the colour encoding, so D2 did not reach into it.**

Two maybe-related states reach your grid, and they are different colours:

- A **live** maybe is yellow. Hinge §8: "the system can work a maybe — just not with the silence
  machinery", and the item is held softly. It is not a gap, and D1 pinned that.
- An **expired** maybe is red — the clock ran out, which is the same exhaustion semantic red
  already carries. `isDecideByExpired(assignment, item, event, now?)` in `src/lib/decide-by.ts`
  is the predicate; it is pure, client-safe and already tested, so the grid and the server read
  the identical definition rather than two that drift.

Under §10.8's worst-colour-wins rule a person holding one live maybe and one expired maybe shows
red. Coordinate with E6 (GTC-251 — corrected 2026-08-23, was GTC-178), which owns the
exhausted-silence red the expired maybe joins — one door, not two.

Also available for a person's row: the decide-by instant itself, via `decideBy(item, event)`. It
is derived, never stored; do not add a column for it.

### Dependencies

B1 (GTC-170), D1 (GTC-174), E1 (GTC-178 — cadence timing), E6 (GTC-251 — exhaustion/colour
state; added 2026-08-23, corrected from GTC-178).

### Acceptance

- [ ] The read API is person-keyed; items are nested under their owning person, not the reverse.
- [ ] A person holding items in mixed states shows the worst colour; all items are visible on tap.
- [ ] Tapping surfaces remind/reassign/take-over actions; the closed surface shows state only.
- [ ] The critical-red assistant message appears exactly once regardless of how many critical reds
      it covers, and disappears when resolved.
- [ ] None of the explicitly refused elements (chat, countdown, activity feed, plan content,
      analytics, progress bars) appear anywhere on the surface — checked in review, not assumed.
- [ ] `npx tsc --noEmit` clean, build passes.
- [ ] Browser walk on a seeded event with a mix of green/orange/red states.

### Stop Conditions

Standard eight from `BUG-TICKET-TEMPLATE.md`.

---

## Design direction — chosen 2026-08-30

**Reference of record:** `docs/design/moment4-glance-reference.md`, with a standalone viewable
mockup at `docs/design/moment4-glance-mockup.html` beside it. Chosen with the founder on
2026-08-30 after eight mockup iterations, which the reference records along with the trade each
one made. **This ticket now aims at that reference.** It is cited here, not restated — read it
before building.

The screen in one paragraph: a grid of **neutral household cards**. Each card carries the
household name and one thin, full-width **colour strip per member**; the strip's tint is that
person's state, and the card itself stays quiet. **Red = yours. Amber = with Gather. Green =
settled. Neutral = not chased.** Above the grid, a **summary sentence** is the four-second
answer — *"3 need you. Gather is on 9. 28 settled."* **Unassigned critical items** render as an
**alert strip above the grid**, since they have no person and person-primary gives them no home.
*The alert strip is the reference's proposal and is **unruled** — see new open decision 6.*

**§10.8 survives; its geometry does not.** Person-primary is intact — states belong to people,
and a person holding items in different states still shows the worst colour. What changes is the
unit of layout: the box is the **household card**, the person is a **strip inside it**. The card
deliberately carries no colour of its own, so no household-level merge rule is needed. The
reference's own framing draws the data model: **card = channel, strip = person** — messages go to
households, states belong to people.

---

## Reconciliation against "GTC-192's twelve open decisions" — the list does not exist

**Finding, recorded so a future session does not hunt for it again.** The reference twice refers
to *"GTC-192's original twelve open decisions"* (`moment4-glance-reference.md`, open-decisions
item 6, and the heading "What this resolves from GTC-192's twelve"). **There is no such list.**
Verified 2026-08-30:

- This ticket has **never** carried an audit section or a numbered decisions list. Every commit
  that has ever touched `docs/tickets/GTC-192.md` — `e5f6e82`, `898e729`, `59a4afb`, `3c8f29d` —
  was checked at its own revision; none contains the strings "audit", "decision", or "twelve".
- No twelve-item list for J1 exists anywhere in the repo. `moment4-hinge-discovery-report.md`
  covers the screen in a single table row (§7) and one ticket-breakdown line;
  `moment4-hinge-report-accuracy-audit-2026-08-04.md`'s Cluster J pass (rows J-1…J-7) audits the
  *discovery report's* claims, not this ticket's decisions.
- The sibling tickets that do carry Phase 1 audits with numbered open decisions are
  [[GTC-184]] (five — 1, 2, 4, 5 ruled in-ticket, 3 split out as [[GTC-249]]) and [[GTC-187]]
  (eight, all ruled 2026-08-23). GTC-192 is not among them and has no audit of its own.

**Consequence: the twelve cannot be reconciled, because there is nothing to reconcile against.**
Nothing has been invented here to stand in for the missing list. What the reference actually
names, and what the record actually corroborates, is below. If the twelve exist outside the repo,
they need to be supplied — this ticket should not proceed to a ruling pass without them.

| # | What the reference claims about "the twelve" | Verdict | Verified against the tree |
|---|---|---|---|
| 1 | *"The itemless guest mostly dissolves"* | **DISSOLVED — but it was never this ticket's decision** | The itemless guest is **GTC-187's** Phase 1 audit **open decision 8**, and the founder **already ruled it on 2026-08-23** ("Founder ruling — the itemless guest", in [[GTC-187]], which closes all eight). The layout claim itself still holds and is worth keeping: children and attendance-only people sit as ordinary strips in their household card, so the itemless guest needs no special grid treatment. Misattribution corrected here, not in the reference. |
| 2 | *"The 'where does the grid live' question is unaffected — still open"* | **NO REFERENT** | The phrase occurs only in the reference itself; a sweep of `docs/` finds no other instance, and no ticket poses the question. It is genuinely open as a **build** question — the Moment 4 screen does not exist in code at all — but it was never a recorded decision, so there is nothing for the layout to leave unaffected. |
| 3 | *"The colour vocabulary… the spec's orange/yellow inconsistency should be ruled once, here"* | **REAL, STILL OPEN, corroborated** | [[GTC-175]] records twice that *"J1 (GTC-192) owns the person-grid colour encoding"* — so this ticket does own it. The reference **proposes** red/amber/green plus a neutral fourth; that is a proposal, not a ruling. The exact spec conflict is tabled under Superseded spec text below. |
| 4 | *"Unassigned critical items… one of GTC-192's original twelve"* | **REAL, STILL OPEN, corroborated** | [[GTC-187]]'s founder ruling on the guest response flow flags it downstream: *"Where unassigned items surface in the grid is an open GTC-192 decision."* The reference's alert-strip-above-the-grid is explicitly **a proposal, not a ruling** — carried forward as new open decision 6. |

**Corroborated total: two.** Rows 3 and 4 are genuine, externally-attested GTC-192-owned open
decisions. Row 1 belongs to another ticket and is already ruled; row 2 has no referent. [[GTC-178]]
refers generically to *"[[GTC-192]]'s (J1) own open decisions"* without enumerating any. Two is
what the record supports — not twelve.

---

## Open decisions the reference surfaced — ALL EIGHT RULED 2026-08-30

*Recorded 2026-08-30 as open; ruled the same evening. The questions are left standing as the
audit trail and each carries its answer inline — see **Founder rulings — the glance screen**
below for the full text of each ruling.*

1. **Sort order.** Do red households float to the top-left, or do houses hold fixed positions?
   The reference leans fixed — strips are loud enough that red finds you anyway, and fixed
   preserves the learned geography that iteration 4 lost — but explicitly does not rule it.
   **→ RULED (Ruling 3): fixed positions, host's household first.** The board is a map, not a queue.
2. **Red terseness.** Bare red strip, or red strip with a why-line (*"Amelia — quiet after 2
   nudges"*)? And its amber counterpart, clock-lines (*"Sarah — deciding Fri"*). Green stays bare
   either way. The two variants below are the test; this decision is its outcome.
   **→ RULED (Ruling 4): reds carry their why; amber and green stay bare.** The amber clock-line
   is preserved as a variant to test, by explicit intent.
3. **Green fold at large events.** At large headcounts, does the settled field fold to a count or
   stay as strips — warmth against noise? Unruled. Note iteration 2's recorded trade: folding
   buys constant height at any headcount but buries family members.
   **→ RULED (Ruling 5): green never folds, at any event size.** The green wall of actual names
   *is* the shoulder-drop; folding turns people into arithmetic.
4. **The "was in, now out" red (Uncle Ray's fade).** §8.6 rules that green can un-happen and goes
   straight to Kate; it does not say how long that red persists. Does it stay red until Kate
   acknowledges it, or fade to settled on its own? The spec supplies the red-source, not the
   behaviour.
   **→ RULED (Ruling 6): red with its why, sticky until seen; plays last in the replay, no
   sparks; settles once played.** "Seen" means the replay played — no acknowledge button.
5. **Declined guests.** *"Out — can't make it"* reads as green/settled in the reference's mocked
   state table. Whether a **no** is rest, or something Kate should see differently, is unruled —
   and the state table itself marks that row "pending decision 5".
   **→ RULED (Ruling 7): OUT fades entirely, text included; NOT-CHASED keeps border and
   full-strength text.** OUT is **not** green — which supersedes that state-table row.
6. **The critical-items strip placement.** The reference renders unassigned critical items as an
   alert strip above the grid. **Proposal, not a ruling** — this is the decision [[GTC-187]]
   flagged downstream (reconciliation row 4). Both the placement and whether an alert strip is the
   right form at all are open.
   **→ RULED (Ruling 8): ruled IN — above the grid, criticals only, with a quiet "and N more
   unassigned" door.** Ordinary unassigned items stay the plan's and pre-flight's business.
7. **The arrival replay's last-seen record.** The replay needs a per-host "last seen" marker — the
   reference suggests a `lastViewedAt` on the host's relationship to the event — plus a diff of
   state changes since. **This is schema, and it is ruled rather than discovered: it belongs on
   this ticket's migration list.** **Stop Condition 7 ("DB schema migration required") fires at
   build time** — the executor stops and reports before writing the migration rather than
   proceeding. When it is subsequently authorised, rehearse it against a scratch database first,
   per the migration caveat in `GATHER-BUILD-CONSTANTS.md`. Field name, owning model, and whether
   the diff is stored or recomputed are all open.
   **→ RULED (Ruling 9): the record exists — when the host last viewed, on her relationship to
   the event.** Shape decided at build time under SC7 with founder sign-off. **Nothing about
   guests is tracked.** SC7 still fires; the shape is deliberately still open.
8. **Live updates while on screen.** The reference's *"live changes get the same flourish"* — a
   reply landing while Kate is watching — requires the screen to learn about changes while open.
   **Polling or push, undecided.** The reference hands this to GTC-192 explicitly as a build
   decision.
   **→ RULED (Ruling 10): polling, roughly every 20 seconds.** Push is a later upgrade that
   changes nothing structural. **Do not build websocket infrastructure for this screen.**

---

## Founder rulings — the glance screen (Nigel, 2026-08-30)

Sixteen rulings. Thirteen on 2026-08-30 — ten from the evening rulings session, plus **Rulings 11,
12 and 13** recorded the same day. Together they settle **all eight** open decisions this ticket
recorded above, **both** refusal-list tensions flagged under *Superseded spec text*, and the colour
vocabulary.

**Rulings 14, 15 and 16 were added on 2026-08-31**, answering the three questions the phase 1 build
raised. They are recorded here rather than in the build's own notes because two of them are colour
encoding, which this ticket owns ([[GTC-175]] records it twice). **Ruling 14 overturns a position
phase 1 had taken**; that is noted at the ruling rather than quietly corrected in the code.

What survives them is listed under *Still open after these sixteen*.

**Ruling 1 — the arrival replay stays, and the shoulder-drop becomes the screen's general test.**
The replay is distinct from the activity feed §3 refuses, and **the distinction is the point**: a
feed makes the host lean in and keep watching; the replay exists to deliver the **shoulder-drop**
— proof she didn't need to watch. The fence, verbatim:

> The replay may only ever show state changes (resolutions), never behaviour. No opens, no views,
> no hesitations, ever.

**And the general test: anything that would make the host lean in does not belong on this
screen.** This generalises §3's refusal list rather than restating it — it is the test to apply
to any later addition, including ones §3 never enumerated.

*Settles the activity-feed tension flagged under Superseded spec text.*

**Ruling 2 — the summary sentence stays: whole numbers of people, never rates, never
proportions.** *"3 need you. Gather is on 9. 28 settled."* answers whose-move-is-it, counted. A
percentage would **grade her family**, and crosses into §3's refused analytics.

*Settles the summary-sentence flag under Superseded spec text.*

**Ruling 3 — sort order: fixed positions.** Households hold their places; **the host's own
household anchors first.** The board is a **map, not a queue** — strips were chosen over dots
precisely because colour with area finds her eye across a stable grid. *Settles open decision 1.*

⚠ **Buildability check, run 2026-08-30:** the host having a household of her own is not assumed —
[[GTC-256]] is **closed**, and its Ruling 1 ("Moment 1 opens with the host's own household") plus
Ruling 2 ("I'm hosting alone" as a household of one) are what make this anchor real. The
degenerate case is covered: a host hosting alone anchors a household of one.

**Ruling 4 — reds carry their why.** *"Amelia — quiet after 2 nudges"*, *"Ray — was in, now
out"*. A red is her move, and **a move needs a direction**. **Amber and green stay bare.** The
amber clock-line **remains a variant to test, not ruled**. *Settles open decision 2 on the red
half; the amber half stays a variant by explicit intent, not by omission.*

**Ruling 5 — green never folds.** Every settled person stays a strip at any event size. **The
green wall of actual names IS the shoulder-drop**; folding it turns people into arithmetic.
Revisit only with a real oversized event in hand. *Settles open decision 3 — and overturns
iteration 2's fold, whose recorded trade was exactly this.*

**Ruling 6 — the reversal lifecycle.** "Was in, now out" is **red with its why, sticky until
seen.** It plays **LAST** in the arrival replay, quietly, **no sparks** — the replay ends on the
truth. **Once played, it settles.** Items the person held **fall loose**; criticals among them
**surface in the alert strip** (Ruling 8). **"Seen" means the replay played** — no acknowledge
button, no inbox mechanics. *Settles open decision 4.*

**Ruling 7 — declined guests fade.** Full grey **including the text**, receding: absence fades to
a ghost. The fence that makes it work: **NOT-CHASED keeps its border and full-strength text**
(expected, just unbothered); **OUT fades entirely** (absent). The **"— out" text carries the
meaning** for anyone the fade confuses. *Settles open decision 5.*

⚠ **This supersedes the reference's own states table**, which rendered *"Settled — out"* as a
green success tint marked "(pending decision 5)". **OUT is not green.** ✅ **FIXED at source by
Ruling 11** — the table now reads *"Out — declined (faded) | fully faded, text included; no
border | name + '— out'"*, and NOT-CHASED's row is made explicit about keeping its border and
full-strength text.

**Ruling 8 — the critical-items strip is ruled IN.** Above the grid, **criticals only**, with a
**quiet door** — *"and N more unassigned"* — opening the non-critical list. Ordinary unassigned
items stay **the plan's and pre-flight's business**; the glance does not nag about what can wait.
*Settles open decision 6, and with it the decision [[GTC-187]] flagged downstream.*

**Ruling 9 — the replay's memory exists.** A record of **when the host last viewed**, on **her
relationship to the event**. The exact shape is decided **at build time, as a migration under
Stop Condition 7, with founder sign-off**. **Nothing about guests is tracked — only her own
glance.** *Settles open decision 7's existence question. The shape stays deliberately open, SC7
still fires, and the guest-tracking fence is itself a ruling — it is the same fence as Ruling 1's,
applied to storage rather than to display.*

**Ruling 10 — live updates by polling, roughly every 20 seconds.** A strip flipping within 20
seconds of the truth **still feels live**; the spark does the work. Push is a **later upgrade that
changes nothing structural**. **Do not build websocket infrastructure for this screen.** *Settles
open decision 8.*

**Ruling 11 — fix the reference's states table.** *"Settled — out"* is **not green**; it renders
per Ruling 7: **fully faded including the text**, with **"— out" in the text**. Corrected at
source in `docs/design/moment4-glance-reference.md` under a dated correction note, with the
NOT-CHASED row made explicit about keeping its border and full-strength text — the two greys are
a deliberate pair and the table has to show that. *Closes the ⚠ raised under Ruling 7.*

**Ruling 12 — the middle colour is named AMBER everywhere.** An **explicit founder ruling to edit
the spec**, not merely to record a supersession. Applied in place in
`docs/03_specs/gather-moment-4-spec-v1.md`, each edit site carrying an inline marker plus one
dated correction note at the head of the document. *Closes the colour vocabulary formally,
not just in usage — and Ruling 13 then carries it into the Hinge spec.*

⚠ **Five sites, not the three named.** The ruling named §3's "orange" and §8.2/§8.5's "yellow".
The Moment 4 spec carried **two more of the same single state** that the earlier survey in this
ticket missed — **§4** (*"flick it back to orange for more system tries"*) and **§10.8**
(*"Jake's box is yellow with the pavlova in it"*). All five were corrected together: leaving two
behind would have left the document self-contradictory in exactly the way Ruling 12 exists to
end. **The two unnamed sites are flagged here for confirmation** — they are inside the ruling's
"everywhere", but outside its enumeration.

⚠ **The Hinge spec was held back from Ruling 12, and Ruling 13 resolves why.** Ruling 12 left
`gather-hinge-spec-v1.md`'s six **yellow** sites untouched, because its **§7 defined the word on
purpose** — *"yellow = the Moment 4 walk's orange; same axis, calmer opening shade; one palette,
not a new state"* — a deliberate shade distinction that a rename would have flattened into
*"amber = the Moment 4 walk's amber."* **Ruling 13 keeps the idea and drops the second word.**

**Ruling 13 — keep the Hinge's calm-opening idea under the amber name.** **One state, one name —
amber, everywhere.** The Hinge's design idea **survives the rename**: the send may open at a
**lighter tint of amber** that **deepens as waiting becomes real**. Same axis, same state, **a
volume knob on the shade — not a second colour and not a second state.**

Applied 2026-08-30, each site carrying an inline marker plus a dated correction note at the head
of the document:

- **`gather-hinge-spec-v1.md`** — all six yellow sites (§4's owner-and-channel line, §7's screen
  in the gap, §7's nudge-clock line, §7's colours-know-the-difference line, and §8's two maybe
  lines) are now amber. **§7's definition is rewritten** to say what it now means: the grid opens
  at a lighter tint of amber that deepens as waiting becomes real — same axis and the same single
  state as Moment 4's amber, a volume knob on the shade.
- ✅ **The three quoting tickets are corrected, not left to drift.** Each carries a supersession
  note rather than a silent edit:

| Ticket | Quoted | Now |
|---|---|---|
| [[GTC-251]] | §4's *"flick it back to orange"*, in a blockquote and in the scope list | Both read **amber**, each with a note naming Ruling 12 and stating the mechanism is unchanged |
| [[GTC-193]] | §8.2's *"a badged yellow box still means rest"* | Reads **amber**, with the same note form |
| [[GTC-190]] | The Hinge's yellow-grid language, at three sites | Follows the Hinge's **new** wording — the lighter opening tint — and its acceptance criterion now names that tint explicitly (Ruling 13) |

*Ruling 13 closes the only question Ruling 12 left open. The colour vocabulary is now settled
across both specs and every ticket that quotes them.*

---

## Founder rulings — the phase 1 questions (Nigel, 2026-08-31)

**Ruling 14 — DONT_CHASE beats every red source, always.** Verbatim:

> Kate marked her mother don't-chase because Kate is handling her personally; a bounced number for
> someone Kate is handling is not the system's to escalate, and the fix-it action a red would
> offer is the exact thing the mark forbids. Grey wins over bounce, over reversal, over
> exhaustion, over expired maybe. No exceptions; if one is ever wanted, it gets its own ruling.

⚠ **This OVERTURNS the position phase 1 shipped to the founder for confirmation** — that the mark
displaced amber only, on the reasoning that it "suppresses chasing, never telling". That reasoning
is superseded: the ruling's ground is that a red's whole content is *a fix-it action Kate should
take*, and offering it for someone she has explicitly taken off the system's hands is the
inversion, not the service.

Implemented as `worst !== 'GREEN'` in `derivePersonState` (`src/lib/glance/state.ts`) —
deliberately **not** a list of red reasons, because a reason list would need extending by every
future red source and the ruling says there are none. That is what makes **"over bounce"** already
satisfied: no bounce-red source exists in the tree today (delivery receipts are §10.9 / the press),
and when one arrives it is displaced by construction rather than by remembering to add it.

**Two states the ruling does not reach, because neither is a red source**, both asserted:
**GREEN** stays green (a settled person is not being chased, and folding her away would empty the
wall of names Ruling 5 keeps) and **OUT** stays out (they answered). **The mark greys the STRIP,
not the rows** — items keep their own colours and stay visible on tap, per §10.8.

**Ruling 15 — the expired maybe turns red at end of the decide-by day, as built
(`isDecideByExpired`).** *"By Friday" means Friday is still hers.* No code was added; `deriveItemState`
reads D2's predicate and applies no offset of its own.

⚠ **Ruling 15 — measured divergence, recorded rather than transcribed.** The ruling's operative
clause ("as built") and its rationale ("end of the decide-by day") are **not the same instant**, and
the code follows the operative clause. `isDecideByExpired` in `src/lib/decide-by.ts` compares
against the decide-by **instant**, which `decideBy` derives as `neededBy() − offsetHours` — an
arbitrary time of day inherited from `Item.dropOffAt` or `Event.endDate`, not midnight. Measured
2026-08-31 on a 6pm NZ event date at the 120-hour default:

| Probe, on the decide-by day (NZ) | `isDecideByExpired` |
|---|---|
| 00:01 | false |
| 12:00 | false |
| 17:59 | false |
| **18:01** | **true** |
| 23:59 | true |

So between 18:01 and midnight the person is already red while the rationale says the day is still
theirs. **Nothing is blocked** — the ruling's instruction was "as built", and that is what shipped.
The divergence is recorded here, cited from `state.ts`, and pinned by the `Ruling 15` assertions in
`tests/glance-read-test.ts`, so the next reader is not misled by the rationale. **If end-of-day was
meant literally, it is a change to `decide-by.ts` and belongs to D2's owner, not to this ticket** —
one derivation, two clocks (§10.2), and E3 ([[GTC-180]]) shares it.

**Ruling 16 — the itemless undecided person is AMBER, with Gather.** Verbatim:

> The ask is real even when the hands are empty: they have been asked whether they are coming, and
> Gather will nudge them. Attendance-only is a state of the ask, not an absence from the board.

Confirms what phase 1 built, and the confirmation is load-bearing rather than cosmetic: it settles
that "no items" is not a fourth grey. The one exception stands and is unaffected — the **host** is
never amber by absence of a reply, because [[GTC-256]] Ruling 5 means no ask is ever made to her.

---

## Founder rulings — the phase 4 proposals (Nigel, 2026-09-01)

**Three rulings, closing all three proposals phase 4 recorded.** Two of them are builds and were
built the same day — see *"Rulings 18 and 19 — built"* under the phase 4 evidence. The third is a
refusal and needed no code.

**Ruling 17 — OUT and NOT-CHASED strips stay sealed. No read-only view.** Verbatim:

> A door onto "nothing here for you" invites a look that changes nothing, which fails Ruling 1's
> general test. The don't-chase mark is revisited where it is set (the pre-flight), not here. Amber
> and green stay sealed too, as built.

*Settles phase 4's read-only-view proposal, in the negative.* **Only a red is a door**, at every
state, which is what phase 4 shipped — so nothing changed in the tree and the phase-4 assertion
"amber, green and the two greys are NOT doors" becomes a ruled invariant rather than a held-back
guard.

⚠ **The second sentence is load-bearing beyond this screen.** "The mark is revisited where it is
set" is what makes sealing the grey defensible rather than merely quiet: there IS a place to change
it, and it is `/plan/[eventId]/pre-flight`. Ruling 19's refusal message names that place for the same
reason.

**Ruling 18 — an OUT person is not offered as a reassign target.** Verbatim:

> Filter the picker. Handing an item to someone who declined is a mistake the surface should not
> offer; Ruling 7's absence-recedes argues the same way. Record the boundary precisely: this is a
> SURFACE filter on this picker, not a route change — the route stays permissive, because a host
> reassigning to an out person deliberately (they are dropping it off anyway) remains possible
> through the plan's own assignment UI. If that split ever feels wrong in practice, it gets its own
> ruling.

*Settles phase 4's picker proposal.* **Built** in `reassignCandidates`
(`src/lib/glance/actions.ts`), one line, above `mayHoldRow` and deliberately not inside it.

⚠ **THE BOUNDARY IS ASSERTED IN BOTH DIRECTIONS, because a split looks like an inconsistency
without them.** `tests/glance-actions-test.ts` proves the picker refuses him AND that the route
still accepts him (200, row moved), and pins that **no attendance gate exists anywhere in the
assignment path** — not in the route, not in `same-team.ts`. The mutation that pushes the filter
down into the shared assignment rule fails three assertions, which is the ruling's own warning made
mechanical.

**It catches OUT and nothing else.** A don't-chase person is still offered: the mark suppresses
CHASING, never HOLDING, and Ruling 14's grey says nothing about whether someone can be given a job.
Asserted.

**Ruling 19 — the manual-nudge route enforces Ruling 14 itself.** Verbatim:

> The DONT_CHASE refusal goes into resolveManualNudgeRecipient beside the child and host gates.
> Changing V1's behaviour is the point, not a side effect: no surface, old or new, can nudge a person
> the host said to leave alone. Test RED first at the route; assert the V1 button path refuses too.

*Settles phase 4's boundary proposal, and moves a boundary phase 4 recorded as fixed.* **Built** in
`src/lib/sms/manual-nudge-recipient.ts`, third of three gates: **CHILD, then HOST, then the MARK**,
each a 403 that says which.

⚠ **THIS PROMOTES RULING 14 FROM A COLOUR RULE TO A SYSTEM RULE.** Until today the mark suppressed
the two AUTOMATED paths (`nudge-eligibility.ts`, `proxy-nudge-eligibility.ts`) and had never gated a
button; V1's `PersonInviteDetailModal` → `NudgeComposer` could nudge a marked person, and did.
`tests/nudge-cadence-controls-test.ts` — the file that already owns "the mark reaches the sweep, on
both paths" — now covers three paths.

⚠ **THE GLANCE STILL REFUSES AT ITS OWN SURFACE, AND THAT IS NOT A SECOND DEFINITION.** Both ask
`isChaseable` and both show `DONT_CHASE_NOT_ADDRESSABLE_MESSAGE`; the surface's refusal earns its
place by firing before any request is made. Belt-and-braces, the treatment the child rule already
gets where the SQL excludes and the JS re-checks — if they disagree they fail refused, not sent.

⚠ **THE CHILD RULE STILL LEADS.** A person who is both a CHILD and marked reports the CHILD reason,
matching the ordering `tests/nudge-cadence-controls-test.ts` already asserts on the proxy path;
§10.6 must not become reachable-through by a gate added later. The mutation that gates the mark
first fails.

⚠ **ORDERING AGAINST OPT-OUT (Do-Not-Touch Zone 7), checked rather than assumed.** `nudge-mark.ts`
requires opt-out be checked FIRST wherever both apply. That ordering is not at risk here: this
route's opt-out check is **not a refusal at all** — it is a per-host, per-number channel switch that
falls through to email — so the two never compete to explain the same "no", and both still apply.

⚠ **V1's UI is not changed, only its outcome.** `PersonInviteDetailModal` does not read `nudgeMark`,
so the Nudge button still renders for a marked person and the refusal arrives on press, in the
route's words. That matches how the child gate has always behaved there and is not a defect; hiding
the control is a UX nicety, unscoped and unfiled.

---

## Still open after these nineteen — do not decide

*The list below was written against sixteen rulings. Rulings 17, 18 and 19 (2026-09-01) settle three
phase-4 proposals and touch item 2 only, which is noted at that item. Nothing else moved.*

1. ✅ **CLOSED — the colour vocabulary, formally and completely.** *Was, in two stages: the ten
   rulings used amber throughout while §3 said orange and §8.2/§8.5 said yellow (closed by Ruling
   12, five Moment 4 sites); then whether the Hinge's deliberate yellow shade collapsed too
   (closed by Ruling 13 — it does, as a lighter tint of the one amber state).* **One state, one
   name, across both specs and all three quoting tickets.** Nothing about the colour vocabulary
   remains open. The **tint ramp itself** — where lighter becomes deeper, and on what clock — is
   a rendering detail for build time, not an open ruling.
2. **The two variants** — amber clock-lines, and the strip-as-button shape. See below; Ruling 4
   preserves the first as a variant by name.
   ⚠ **Narrowed, not settled, by phase 4 and Ruling 17.** Phase 4 built the second layer's
   EXISTENCE without its LOOK, and **Ruling 17 fixes which strips can ever have one**: only reds are
   doors, at every state, so the strip-as-button variant is now a question about red strips alone.
   The amber clock-line is untouched.
3. **The household merge rule.** Still not-used-by-construction: the card is neutral, so no merge
   rule is needed. If a household-level colour ever returns (e.g. a collapsed view),
   worst-state-wins must be ruled explicitly — it is not inherited from §10.8.
4. **Where the screen lives** — its route and entry point. A real build question (the Moment 4
   screen does not exist in code at all), untouched by all twelve.
   ⚠ **Still open after phases 1–4.** Phase 1 added `GET /api/events/[id]/glance` — a data
   endpoint, not the screen's route. Phase 2 added a page at **`/plan/[eventId]/glance`**, URL-only,
   with nothing linking to it and the V1 dashboard untouched. **That address is a place to look at
   the screen, not a ruling about where it belongs.** The real question — whether the glance is a
   tab on the plan, the thing that replaces the V1 dashboard, or its own destination — is
   untouched, and moving the page later costs one directory rename because nothing links in.
   **Phases 3 and 4 did not move it either**, and phase 4 deliberately added no route of its own:
   its three actions are requests to routes that already existed, so the API surface is exactly
   where phase 1 left it (80 routes, inventory gate).

---

## Variants to test, not scope

Both are from the reference's "Variants to test before building". They are **test material, not
build scope.** Shipping either as the default settles an unruled decision by stealth.
**Unchanged by all twelve rulings — Ruling 4 preserves the amber clock-line as a variant by name**,
and no ruling touches the strip-as-button shape.

- **Why-lines on red, clock-lines on amber.** The strips have the room. §8.3 wants the nudge
  promise visible, and the amber clock is where trust is earned; the counter-risk is crowded
  strips, and reds becoming the tersest entries on the screen — the exact trade iteration 7 hit.
  Test with and without. Green stays bare in both.
- **Strip as button — the second layer.** Each strip is the natural door to the person's story and
  the fix-it actions: tap Amelia, get her state and remind / reassign / take over. Note both the
  fit and the gap: **tap-for-actions is already this ticket's scope** (§3, *"behind a tap: all
  actions"*), so the existence of the second layer is not in question — but **the spec only ever
  described the first layer**, and this layout is what gives the second one an obvious shape. The
  *shape* is the variant; the *existence* is scope.
  ⚠ **Phase 4 built the existence and left the shape alone, which narrows this variant rather than
  settling it.** A red strip is now a `<button>` element carrying its unchanged tint, text and
  padding; no border, no chevron, no hover state, no cursor class was added, and a mutation adding
  them fails `tests/glance-grid-test.tsx`. What phase 7 still has to test is whether the door
  should LOOK like one — and, if it should, whether that treatment belongs on reds only or on every
  strip.

---

## Superseded spec text — the colour rows CORRECTED, the geometry rows still flagged

*Originally "FLAGGED, NOT CORRECTED", per house style ([[GTC-163]]'s "Known-superseded
references"). **Rulings 12 and 13 changed that for the colour rows only** — those were edited in
place in both specs, so their "Superseded by" column now records a completed edit rather than a
pending one. The **geometry** rows (§10.8's per-person box, the worst-colour rule's unit, and this
ticket's own Scope bullet) remain **flagged and uncorrected**: no ruling has authorised editing
§10.8's layout language, and it should not be inferred from the colour rulings.*

Recorded so a future session reading Moment 4 §3/§8/§10.8 does not re-derive the spec's version of
this screen and build it.

| Location | What it says | Superseded by |
|---|---|---|
| ✅ **CORRECTED** — `gather-moment-4-spec-v1.md` §3, the colour encoding list | Named the middle state **orange** — *"Orange — the system is on it"* | The chosen direction's **amber** ("with Gather"). One word; the meaning is unchanged. Ruled once, here — see reconciliation row 3, still open |
| ✅ **CORRECTED** — `gather-moment-4-spec-v1.md` §8.2, §8.5 (and §4, §10.8, found in the same pass) | Named that same middle state **yellow** / **orange** | Same amber. Note the spec is **internally inconsistent** — orange in §3, yellow in §8.2 and §8.5 — which is precisely why the reference asks for one ruling rather than a patch |
| `gather-moment-4-spec-v1.md` §3, the colour encoding list | The surface has **three** colour states (green / orange / red) | The reference adds a **fourth**: neutral outline = **not chased**. This is an **addition with no spec source**, not a correction of one — it needs its own ruling, not just a vocabulary note |
| `gather-moment-4-spec-v1.md` §10.8 (*"People are the boxes… Jake's box is yellow with the pavlova in it"*) | The grid's unit of layout is a **per-person box** in a flat grid | The **household card containing one strip per member**. §10.8's *substance* — person-primary, items live inside the person, worst-colour-wins per person — is **preserved**; only the geometry is superseded. Do not read "box" as the render unit |
| `gather-moment-4-spec-v1.md` §10.8, the worst-colour rule as applied to "the boxes" | Worst-colour-wins on the grid's box | Worst-colour-wins applies to the **person/strip**, and is **deliberately not applied at household level** — the card stays neutral precisely so no merge rule is needed. If a household-level colour ever returns (e.g. a collapsed view), worst-state-wins must be **ruled explicitly**, not inherited from §10.8 |
| **This ticket**, `### Scope` → *"The grid itself: person-primary **boxes**"* | Restates §10.8's box geometry | The household-card layout above. The bullet's other clauses — worst-colour-wins, all items visible on tap — stand unchanged |

**RULED 2026-08-30 by Ruling 1 — was: tension to resolve.** *Left in place as the reasoning the
ruling answers. The replay **stays**, fenced to state changes only, and the shoulder-drop becomes
the screen's general test.* §3
explicitly refuses an **activity feed** on this surface. The reference's arrival replay opens on
the state as of Kate's last visit and **replays the changes since, in order**. These are not
obviously the same thing — the replay is a bounded ~3-second animation that resolves to the
current state, not a scrollable history — but it is close enough that it must be **ruled, not
assumed**, before it is built. The reference's own guardrails (never hold the answer hostage; the
summary line legible throughout; no fake fireworks when nothing changed) are the argument that it
serves the four-second contract rather than obstructing it. Recorded here because this ticket's
acceptance requires the refusal list be *"checked in review, not assumed"*.

**Not conflicts — checked, recorded so they are not re-litigated.** §3's red and green meanings
survive verbatim: *"Red — only you can do this"* = "Red = yours"; *"Green — nothing is yours"* =
"Green = settled". §8.2's badge ruling is untouched by this layout — J2 ([[GTC-193]]) still layers
the badge on top. The reference adds no chat, no countdown, no plan content, and no progress bar.
**RULED 2026-08-30 by Ruling 2 — was: the one item for the refusal-list review to look at
closely.** The summary sentence **stays**: whole numbers of people, never rates, never
proportions. A percentage would grade her family and would cross into the refused analytics. The
counts do not.

---

## Build order — GRID FIRST (founder direction, 2026-08-30)

**Direction:** the build proceeds **grid first**. **Phases 1–4 below are unblocked and are next.**
Phase 5's migration and the press ([[GTC-189]], I2) **follow**, with **credentials and the TNZ
reply gathered in parallel** so neither is the thing everyone waits on.

**Not started.** Recorded 2026-08-30 as direction, not as a start. No code was written for this
ticket in this pass.

**What "unblocked" means precisely, so phase 1 is not begun on a false premise.** [[GTC-170]]
(B1), [[GTC-174]] (D1) and [[GTC-178]] (E1) are **closed**, which is what phases 1–4 read from.
**[[GTC-251]] (E6) is still open**, and it owns two things phases 1–4 touch but do not depend on:

- **The exhaustion colour path** (phase 1). Build the seam; do not stub a predicate that lies.
- **The red door's two-way swing** (phase 4) — §4's *"flick it back to amber for more system
  tries, with a retry count she specifies"*. Phase 4 ships remind / reassign / take-over; the
  swing wires up when GTC-251 lands.

Everything else in phases 1–4 — the person-keyed shape, the household cards and strips, the
summary sentence, the alert strip, the assistant's message, tap-for-actions — is assemblable
today. **Grid first is a real path, not a hopeful one.**

**Running in parallel, owned outside this ticket:**

- **Credentials** for the press — `TNZ_AUTH_TOKEN` and the rest of the SMS/send environment per
  `GATHER-BUILD-CONSTANTS.md`. Gathering them does not block the grid, and the grid does not
  block them.
- **The TNZ reply** — the delivery-receipt question. Moment 4 **§10.9** makes this a build
  instruction, not a ruling: *"Its ticket's first task: investigate TNZ delivery-receipt
  capability. If webhooks exist, wire them."* ⚠ §10.9 also fixes the answer if TNZ says no: the
  principle **degrades gracefully** — an invisible bounce travels the nudge path and reaches Kate
  at exhaustion or her time line. **The fallback is documented in the ticket, never silently
  substituted for the target.** So the reply shapes the press; it does not gate the grid.

**Sequencing note.** Phase 6 (the arrival replay and polling) depends on phase 5's migration, so
under grid-first the replay is **not** in the first run of work. Phases 1–4 stand on their own and
deliver a working screen without it — Rulings 1, 6 and 10 wait for phase 5 to clear, and none of
phases 1–4 should reach for a last-seen record to fake it.

---

## Execution phasing — PROPOSED against the rulings, NOT agreed (2026-08-30)

*Proposal only. Nothing here is started. Phases are sequential unless stated; each names what
would prove it works, and **every phase declares its migration status** per house convention —
the one phase that carries schema carries nothing else.*

⚠ **Dependency reality, checked 2026-08-30, not assumed:** [[GTC-170]] (B1, criticality),
[[GTC-174]] (D1, response model) and [[GTC-178]] (E1, cadence) are **closed**. **[[GTC-251]]
(E6) is still OPEN** — it owns the exhaustion counter, the exhausted predicate, and the
host-specified retry count. Red-by-exhaustion is therefore **not assemblable today**. Phase 1
below is scoped so this blocks one colour path, not the whole phase; phase 2's reds are
demonstrable via the already-closed sources (expired maybe, reversal) without GTC-251.

**Phase 1 — the person-keyed read API. ✅ BUILT 2026-08-31.** *See "Evidence — phase 1" at the
foot of this ticket. Everything the proposal named was delivered; two additions are recorded
there (the HTTP route, and `unhoused`).* No UI. The shape §10.8 fixes: person-keyed, items nested
under their owning person, worst-colour-wins per person, assembled from GTC-170's criticality,
GTC-174's response model, GTC-178's cadence timing, and — **when GTC-251 lands** — its exhaustion
state. Household grouping is carried in the payload (card = channel, strip = person) without a
household-level colour, per Ruling 3 and the reference's item 7.
**No migration.**
*Proved by:* a fixture over a seeded event with a deliberate mix of states asserting (a) the
payload is person-keyed, not item-keyed; (b) a person holding a live maybe and an expired maybe
resolves **red**, reading `isDecideByExpired` in `src/lib/decide-by.ts` rather than a second
definition; (c) a mixed household returns per-person states and **no** household colour; (d) the
host's household is identifiable for Ruling 3's anchor.
*Blocked in part:* the exhaustion colour path, on GTC-251. Ship the seam, not a stub that lies.

**Phase 2 — the static grid. ✅ BUILT 2026-08-31.** *See "Evidence — phase 2" at the foot of this
ticket.* Household cards, per-member strips, summary sentence. Rulings 2,
3, 4, 5, 7. **No replay, no polling, no alert strip, no tap.**
**No migration.**
*Proved by:* a browser walk on a seeded event, plus a deliberately oversized one. Fixed positions
with the host's household first, stable across reloads (Ruling 3). Reds carry a why; amber and
green are bare (Ruling 4). At the oversized event **every** settled person is still a strip —
nothing folds (Ruling 5). OUT renders fully grey **including text**, carrying "— out", while
NOT-CHASED keeps border and full-strength text — the two greys must be distinguishable in a
screenshot (Ruling 7). The summary line is whole numbers, and a grep of the rendered output finds
**no `%`** (Ruling 2).

**Phase 3 — the alert strip and the assistant's message. ✅ BUILT 2026-08-31**, in two passes —
the strip first, the message second. *See "Evidence — phase 3" and "Evidence — phase 3b".*
Ruling 8's criticals-only strip above
the grid with the quiet "and N more unassigned" door, plus the existing scope's one plain
critical-red assistant message.
**No migration.**
*Proved by:* unassigned criticals appear above the grid and non-criticals do **not**; the door
reports the right N and opens the non-critical list; the assistant message appears **exactly
once** regardless of how many critical reds it covers, and disappears when resolved.

**Phase 4 — tap-for-actions, the second layer. ✅ BUILT 2026-09-01.** *See "Evidence — phase 4" at
the foot of this ticket.* Remind, reassign, take over a silence. Already
this ticket's scope and §3's *"behind a tap: all actions"* — the **shape** is the open variant,
not the existence.
**No migration.**
*Proved by:* the closed surface shows state only — no action affordance renders until tapped;
each of the three actions is reachable and round-trips. The red door's two-way swing (§8.1: back
to amber with a retry count, or keep it and handle it herself) is **GTC-251's data** — wire it
only once GTC-251 has landed, or leave the seam and say so.
⚠ **The two-way swing is NOT wired and no seam was faked for it.** [[GTC-251]] is still open, so
there is no retry count to specify and no exhaustion fact to flick back to amber. `RED_REASONS`
already carries `EXHAUSTED_SILENCE` from phase 1 and `derivePersonState` already applies it; when
E6 lands, the swing is an action on this surface against a fact that exists, not a new mechanism.
Nothing in phase 4 claims otherwise.

**Phase 5 — MIGRATION, ISOLATED. Ruling 9's last-seen record.** Nothing else ships in this phase.
**Stop Condition 7 fires by design**: stop, report the proposed shape, obtain founder sign-off,
rehearse against a scratch database, and prove `gather_dev` untouched afterwards before applying.
**Nothing about guests is stored — only the host's own glance**, and the migration should be
reviewed against that fence explicitly.
*Proved by:* the rehearsal transcript on the scratch DB; `prisma migrate status` clean afterwards;
a review line confirming no guest-behaviour field was added.

**Phase 6 — the arrival replay and live polling.** Rulings 1, 6, 10. Depends on phase 5.
**No migration** (phase 5 carried it).
*Proved by:* **the fence first** — a test asserting the replay's input contains only state
changes, so behaviour fields are not merely unrendered but **absent from the payload** (Ruling 1;
a "we don't render it" assertion is not the fence). Then: amber→green flips spark; anything that
went red does **not** spark; the reversal plays **last**, quietly, and **settles once played**,
with no acknowledge control anywhere (Ruling 6). Whole replay under ~3s with the summary legible
throughout. **Nothing changed → nothing plays.** Polling lands a flip within ~20s, and the
dependency tree gains **no websocket package** (Ruling 10).

**Phase 7 — the two variants, tested and then ruled.** Amber clock-lines, and the strip-as-button
shape. **Test material — shipping either as the default settles an unruled decision by stealth.**
*Proved by:* a side-by-side on the same seeded event put in front of the founder, then a ruling.

**Closing gate, not a phase:** the refusal-list check the Acceptance section already requires —
"checked in review, not assumed" — run against **§3's list plus Ruling 1's general test**: does
anything here make the host lean in? That test is broader than the enumerated list and is the one
that catches additions §3 never named.

---

## Evidence — phase 4 (Executor-Completed, 2026-09-01) — tap-for-actions

**Commit:** `40d457c` · **Branch:** `feat/moment-one-redesign`

### What was built

| Path | What |
|---|---|
| `src/lib/glance/actions.ts` | **NEW.** The action layer. Refusal, request builders, candidate filter, and the three performers. Pure plus an injectable `fetch`; no provider, no Prisma, no session. |
| `src/components/glance/PersonSurface.tsx` | **NEW.** The modal behind a red strip, and the strip's `<button>` itself. The only `'use client'` file in the glance. |
| `src/components/glance/GlanceBoard.tsx` | `Strip` splits into `StripBody` (the words) and the element: a `<button>` for RED, a `<div>` for everything else. `assignablePool` added. |
| `src/lib/glance/state.ts` | `GlancePerson` gains `role`, `teamId`, `nudgeMark`; `GlanceItem` gains `kind`, `teamId`; `EventGlance` gains `hostPersonId`. |
| `src/lib/glance/read.ts` | Two fields added to each existing `select`; `hostPersonId` from the FK. **Still one read, still no `include:`.** |
| `src/app/plan/[eventId]/glance/page.tsx` | Passes `actorRole` (from the guard it already ran) and the formatted `eventDate`. |
| `tests/glance-actions-test.ts` | **NEW**, 52 assertions in four layers. |
| `tests/glance-grid-test.tsx` · `tests/glance-read-test.ts` | 3 + 4 new assertions; the two "phase 4 held back" guards retired; the behaviour fence extended to both new sources. |
| `scripts/seed-gtc192-glance.ts` | A second team, and one red with a phone number. Both are needed to see the guards work — see below. |
| `package.json` | `test:glance-actions`. |

**No migration. Stop Condition 7 did not fire.** `npm run build`: *"No pending migrations to apply."*
`prisma migrate status`: 41 migrations, *"Database schema is up to date!"*; `git status prisma/` empty.

**AND NO NEW ROUTE.** Phase 4 adds zero API surface — `route-classifications.json` is untouched
and the inventory gate still reports 80 routes. Every action is a request to a route that already
existed.

### The surface — a modal, and the reason is Ruling 3

`PersonInviteDetailModal` is this codebase's person-level detail surface and it is a fixed overlay
with a scrim; `HostPersonModal`, `EditPersonModal` and `AcknowledgeModal` are the same shape.
Matching it is the obvious half of the reason.

**The other half is Ruling 3.** The board is *"a map, not a queue"*, and its whole value is that
households hold their places. An in-card expansion would push every card below and to the right of
the tapped one — opening Amelia would move the Nguyens — so a card that changed shape when touched
would cost exactly the learned geography fixed positions were chosen to buy. The overlay leaves the
map underneath untouched, and closing returns her to the same four-second screen she left.

**Only a red is a door.** Amber, green and both greys carry no button, no handler and no link;
asserted on the rendered markup, not assumed. On the oversized board — 64 settled people — there
are **0 doors and 0 buttons**, so nothing on it hydrates at all.

⚠ **The red strip is a button ELEMENT, not a button LOOK.** Tint, text and padding are unchanged;
`block w-full text-left` is the entire difference, undoing the element's own inline centring. A
mutation adding `cursor-pointer` and a hover state fails the suite. The strip-as-button *shape* is
phase 7's variant and is not settled here. (The browser's own pointer cursor on a `<button>` comes
from Tailwind's preflight, not from a class added by this phase.)

**An island per red, not a client board.** `GlanceBoard` keeps the property phase 2 built it for —
server component, no hooks, `renderToStaticMarkup`-testable — and only the reds hydrate. The cost is
that each island is handed the same candidate pool, so three reds serialise it three times; that is
a few hundred bytes against hydrating sixty-four strips to make three of them tappable.

### The whole surface, as it renders

> **Amelia Turner**
> *maybe timed out*
> ● The trifle  [ Move to… ▾ ] [ Move ] [ I'll do it ]
> ● The pavlova *critical*  [ Move to… ▾ ] [ Move ] [ I'll do it ]
> ──────
> [ Remind them ]
> *Yours now. This board still shows the state from before — reload to see it.*

Person, why, what they hold, actions. Nothing else. §10.8's *"all items visible on tap"* includes
the settled ones — worst-colour-wins means a red person can hold three things and only one of them
is the trouble.

### ONE refusal lives here. Every other guard is the route's.

**Ruling 14, at the action layer.** *"The fix-it action a red would offer is the exact thing the
mark forbids."* A remind IS that fix-it action, so `remindRefusal` refuses a marked person **before
the request is built** — the spy records **zero** calls, against **one** for the same function on an
unmarked person. Differential, so it cannot pass vacuously.

It keys on **the mark, not the tint**, through **`isChaseable`** — the same predicate
`nudge-eligibility.ts` and `proxy-nudge-eligibility.ts` ask. The string `DONT_CHASE` does not appear
in `actions.ts`; a literal there would be a second definition of §10.3's off-switch. Both are
asserted, and the mutation that re-implements it as a literal fails.

**Colour would have been the wrong key regardless.** Ruling 14 greys only a person whose worst row
is not green, so a *settled* marked person reads GREEN and the mark would be invisible to a check
that read the strip. That is why `nudgeMark` is on the wire.

⚠ **RULING 19 (2026-09-01) MOVED THIS BOUNDARY, SAME DAY.** As phase 4 shipped, the manual-nudge
route enforced no mark: the mark suppressed the automated sweeps and had never gated a host's own
press, so V1's `NudgeComposer` could nudge a marked person and did. Widening a V1 host control on
the strength of a Moment 4 colour ruling was a founder decision, carried below as a proposal — and
**the founder took it**. The route now refuses too; see *"Rulings 18 and 19 — built"* below. The
surface's refusal survives as the near half of a pair, firing before any request is made.

**Everything else is deferred, and the surface may only ever be stricter than the routes, never
looser.** The child rule, the host exclusion, the 24-hour cooldown, `smsOptedOut`, the same-team rule
— each answers for itself and its refusal is shown **in its own words**, because those are already
written for a host and a second wording would be a second place to keep them true.

### The three actions, and the routes they are doors to

| Action | Route | Guards it meets, unchanged |
|---|---|---|
| REMIND | `POST /api/events/[id]/people/[personId]/nudge` | HOST-only; child rule (§10.6); host exclusion (GTC-256 R5); 24h cooldown; opt-out; channel choice |
| REASSIGN | `POST /api/events/[id]/items/[itemId]/assign` | HOST/COHOST/COORDINATOR; same-team (GTC-171 B2); event scope; ledger + transaction |
| TAKE OVER | **the same route**, host as assignee | GTC-256 Ruling 9's self-pick, **already** the second exception in `mayHoldRow` |

**TAKE OVER is not a second mechanism.** `takeOverRequest` returns `reassignRequest`'s object
byte-for-byte, and a test asserts it — so a future divergence has to be deliberate. It has its own
control rather than being a name in the picker because one thing deserves one door; that is why the
host is excluded from the dropdown.

**The picker asks the same function the route gates on.** `reassignCandidates` calls `mayHoldRow`,
so it cannot offer a name the route will refuse or hide one it would accept. No team comparison is
written in `actions.ts` — asserted on the source, and the mutation that hand-rolls the rule fails
twice (the structural assertion, and Ruling 9's host case, which a hand-rolled `teamId ===` silently
drops).

**No `reason` is sent with a reassign.** GTC-196 makes the why optional and records its absence as a
gap rather than rejecting it. A reason box would be a second thing to do before the action lands,
and a fabricated constant would be worse than the gap — it would put words in her mouth in the
ledger. The walk's ledger row shows `MOVE_ASSIGNMENT`, actor `Kate Whittaker` (HOST), `reason: null`.

### REMIND composes, it does not converse

§3 refuses messaging on this surface and Ruling 1's general test refuses anything that makes the
host lean in. A four-tone picker and an editable textarea is the V1 composer, which is where a host
who wants her own words already goes. **The glance presses once**, and the words are
`getHostNudgeMessage('warm', …)` — the existing shared template, so the glance and V1 send the same
sentence. A test asserts the message equals that function's output and that none of the four
template strings appears in `actions.ts`.

### Staleness is stated, never hidden

No polling this phase — Ruling 10's ~20 seconds is phase 6. So an action that changes what the board
shows **says the board is behind**:

| Action | What it says |
|---|---|
| REASSIGN | `Moved to Minh Nguyen. This board still shows the state from before — reload to see it.` |
| TAKE OVER | `Yours now. This board still shows the state from before — reload to see it.` |
| REMIND | `Reminded. Nothing here changes until they reply.` |

**A remind deliberately makes no staleness claim, because it changes no state.** The strip stays
exactly as red as it was until the person answers, and "reload to see it" would promise a change
that has not happened. A test asserts the word *reload* is present on the two that move the board and
absent on the one that does not.

**Nothing refreshes itself.** No `router.refresh()`, no interval, no websocket package.

### Test results

| Run | Result |
|---|---|
| RED — `glance-actions`, before the action layer or the surface existed | **16 passed, 36 failed**, exit 1 |
| RED — `glance-grid`, with the phase 4 assertions added | **80 passed, 5 failed**, exit 1 |
| RED — `glance-read`, with the payload assertions and the extended fence | **85 passed, 30 failed**, exit 1 |
| GREEN — `test:glance-actions` | **52 passed, 0 failed** |
| GREEN — `test:glance-grid` | **85 passed, 0 failed** |
| GREEN — `test:glance-read` | **115 passed, 0 failed** |

**All 16 greens in the first RED are guards on code that already existed** — the auth gate on both
routes, the same-team refusal, Ruling 9's self-pick exemption, the child rule, the host rule. That is
the phase in one number: the mechanisms were already there, and this phase wires doors to them.

**Two rounds of RED were taken, and only the second was recorded.** The first had four vacuous
greens — "no request was issued" is trivially true of a module that does not exist, and three
"nothing was logged" assertions passed because the dev server was answering 500 to everything. Each
is now gated on its own positive case: the marked person is asserted *against* the unmarked one, and
every absence is gated on the refusal that should have caused it. The health probe was also
rewritten: it asks the glance route for a **401**, because a stale `.next` answers too.

**Mutation-tested, each reverted:**

| Mutation | Assertions that failed |
|---|---|
| drop Ruling 14's refusal | 5 (+1 collateral) |
| re-implement the mark as a `'DONT_CHASE'` literal instead of `isChaseable` | 2 |
| hand-roll the same-team rule instead of calling `mayHoldRow` | 2 — including Ruling 9's host case, dropped silently |
| report a route's refusal as a success | 1 |
| give TAKE OVER its own request body | 2 |
| make amber a door too | 3 |
| ship phase 7's `cursor-pointer` + hover treatment | 1 |
| render a behaviour field behind the tap | 1 (the fence) |
| import `sendSms` into the action layer | 1 |

### Browser walk — the mixed board, actions round-tripped

Seeded boards, `npm run dev`, screenshots taken. **The seed changed twice, and both changes were
needed to see the guards at all:**

1. **Two teams, and everyone on one.** Every `PersonEvent.teamId` was null in the phase 2/3 seed, and
   the same-team rule refuses a null-team candidate for an ITEM row — so the picker would have been
   empty on every red and a bug would have been indistinguishable from the rule working.
2. **One red with a phone number.** The two remind legs do not fail the same way; see the finding
   below.

| Step | Result |
|---|---|
| Tap Amelia (red, expired maybe) | Surface opens; board underneath unmoved |
| Her picker, on a **Mains** row | Rob Whittaker · Charlotte Turner · **James Turner** · Ray Dalton · Sarah Dalton · Bob Unhoused |
| Chloe's picker, on a **Desserts** row | Minh Nguyen · Grace Nguyen · Connor OBrien · Aoife OBrien |
| TAKE OVER the pavlova | `Yours now…`; DB: held by Kate Whittaker; ledger `MOVE_ASSIGNMENT`, reason null |
| Reload | Amelia RED → **AMBER**; summary `2 need you. Gather is on 3. 6 settled.` → `1 needs you. Gather is on 4. 6 settled.`; **the assistant's message is gone** — the critical red it named is hers now, held silently |
| REASSIGN the trifle bowl → Minh | `Moved to Minh Nguyen…`; DB agrees |
| Reload | Chloe RED → AMBER, Minh GREEN → AMBER, `1 needs you. Gather is on 5. 5 settled.` |
| REMIND Chloe (**SMS leg**) | **`Failed to send nudge`**, in red — the route's own words. **No InviteEvent logged.** |
| Oversized board (64 settled) | **0 doors, 0 buttons**, `Nothing needs you. 64 settled.` — Ruling 5 intact, nothing hydrates |

**Two things worth naming in the picker output.** `James Turner` is CHILD-role and **is offered** —
GTC-207's kid with a job, and the assertion that the §10.6 message exclusion has not leaked into an
assignment path. `Aoife OBrien` is don't-chase and **is offered** — correctly: the mark suppresses
*chasing*, not *holding*.

⚠ **`Ray Dalton` is OUT and was offered too. Ruling 18 removed him the same day** — the walk above
records the board as phase 4 shipped it, and the re-walk under *"Rulings 18 and 19 — built"* records
the Mains picker without him.

### ⚠ FINDING — the email leg reports a success that did not happen

**Not introduced by this phase, and not fixed by it. It affects V1 identically.**

The walk's first remind went to an email-only person and the surface said **`Reminded.`** Nothing was
sent. Measured the same evening:

```
resend.emails.send(…) →
  {"data":null,"error":{"statusCode":401,"name":"validation_error","message":"API key is invalid"}}
```

The Resend SDK **returns** its error rather than throwing. `sendNudgeEmail`
(`src/lib/email.ts:38-46`) `await`s the call inside a `try` and returns `{ success: true }` without
ever inspecting the result, so the route answers **200**, logs **`NUDGE_SENT_HOST`** with
`contactMethod: email`, and **starts a 24-hour cooldown for a message that never left**. Measured on
the walk:

| Leg | Provider result | Route | InviteEvent |
|---|---|---|---|
| SMS (`+64…`, TNZ unconfigured) | `{success:false, blocked:'SMS_DISABLED'}` | **502** | **none** — honest |
| Email (Resend key invalid) | `{data:null, error:{401,'API key is invalid'}}` | **200** | **`NUDGE_SENT_HOST` logged** — false |

**This phase stubs nothing.** The surface reports exactly what the route reports, which is the
property under test and the one the mutation suite pins. On the email leg the route is reporting a
lie told one layer further down.

**It is a sharper thing than [[GTC-247]].** 247 says no send path authenticates in dev; this says one
of them **cannot tell**, in any environment. A production Resend outage, a revoked key or a bounced
domain would all read as *sent* to the host, and the cooldown would suppress her retry.

**Not fixed here, deliberately.** `sendNudgeEmail` is a shared send path used by V1's nudge composer
and it sits next to the magic-link delivery this app's only auth mechanism depends on. Changing what
it returns changes V1 behaviour under a Moment 4 ticket. **Proposed as its own defect ticket** — see
below. No test in this phase pins the defective behaviour, because such a test would have to be
deleted the day it is fixed.

### Refusal-list check — §3's list plus Ruling 1's general test, run on the OPEN surface

Measured against the rendered dialog's text and markup, not assumed:

| Refused | Present? |
|---|---|
| chat / messaging | **no** — one button, no composer, no thread, no history |
| countdown | **no** |
| activity feed | **no** — no opens, no views, no "last seen", no log |
| the plan content itself | **no** — the row's name and its criticality; no quantity, team, drop-off or notes |
| analytics / response rates | **no** |
| progress bars, percentages | **no** — no `%` anywhere in the dialog |

**Ruling 1's general test — does this make the host lean in?** The surface has nothing to watch: no
number that moves, no history to scroll, no timer. It is reached by choice, does one thing, and
closes back to the same four-second board. It passes.

### Amber and green stay non-interactive — and ONE proposal, not built

> ✅ **RULED THE SAME DAY — Ruling 17: no read-only view. OUT, NOT-CHASED, amber and green all stay
> sealed.** The proposal is left standing below as the audit trail; the ruling took the argument
> against, and added the sentence that makes sealing the greys defensible rather than merely quiet —
> *"the don't-chase mark is revisited where it is set (the pre-flight), not here."* **No code
> changed**: only reds are doors, which is what phase 4 shipped.

**PROPOSAL (not built): should OUT and NOT-CHASED strips open a read-only view?**

The argument for: both greys are states Kate *decided*, and a decision she cannot see the contents of
is a decision she cannot revisit. Tapping Ray would show what he was holding when he pulled out;
tapping Aoife would show what she still holds while the system leaves her alone. Neither needs an
action — Ruling 14 forbids the remind on Aoife anyway, and Ray's rows are Ruling 6's fall-loose
clause, which is phase 5/6's.

The argument against: Ruling 1's general test. A grey is *"nothing here for you"*, and a door on it
invites a look that changes nothing. Ruling 7's OUT is *"absence receding to a ghost"* — giving
absence a door is arguably ungreying it.

**Amber and green are not proposed.** An amber is Gather's move and §8.3's promise is that she does
not have to take it; a green is nobody's.

### Two more proposals, recorded rather than taken — BOTH RULED, BOTH BUILT (2026-09-01)

> ✅ **Ruling 18** took proposal 1 (filter the picker, leave the route alone) and ✅ **Ruling 19**
> took proposal 2 (the route enforces the mark). Both were built the same day — see *"Rulings 18 and
> 19 — built"* below. Proposal 3 is filed as [[GTC-265]]. The three are left standing here as the
> audit trail.

1. **Should an OUT person be offered as a reassign target?** The picker offers Ray Dalton today,
   because the route allows it — attendance gates no assignment anywhere in the tree — and filtering
   him here would be this surface inventing a rule nobody has made. Ruling 7 calls OUT *"absence"*,
   which is an argument for the filter. **Visible on the mixed board** so it can be ruled on what is
   seen rather than in the abstract. One line in `reassignCandidates` if it is ruled in.
2. **Should the manual-nudge route itself enforce Ruling 14?** See the boundary note above. If ruled
   in, it belongs in `resolveManualNudgeRecipient` beside the child and host gates — the same shape,
   the same 403 — and it would change V1's composer too. That reach is the reason it is a founder
   call.
3. **`sendNudgeEmail` must inspect Resend's returned `error`.** Its own defect ticket, per the
   finding above. ✅ **Filed as [[GTC-265]]** (2026-09-01), with [[GTC-264]] named as its sibling on
   the SMS side: together they are *"Gather never checks what actually happened"*, provider by
   provider.

### Rulings 18 and 19 — built (2026-09-01)

*Same commit as phase 4. Both are small; neither is a new mechanism.*

| Path | What |
|---|---|
| `src/lib/eligibility/nudge-mark.ts` | `DONT_CHASE_NOT_ADDRESSABLE_MESSAGE` — the refusal shown to Kate, beside the skip reason it is deliberately not. |
| `src/lib/sms/manual-nudge-recipient.ts` | **Ruling 19.** The third gate, after CHILD and HOST. `nudgeMark` added to the membership select. |
| `src/lib/glance/actions.ts` | **Ruling 18.** `GlanceAssignable` gains `state`; `reassignCandidates` drops OUT. And `remindRefusal` now returns the shared message rather than its own wording. |
| `src/components/glance/GlanceBoard.tsx` | The pool carries `state`. |
| `tests/nudge-cadence-controls-test.ts` | **Layer 8**, 11 assertions — the mark's own suite grows from two paths to three. |
| `tests/glance-actions-test.ts` | 10 assertions: Ruling 18's filter and its two-way boundary, Ruling 19 over HTTP. |

**No migration. Stop Condition 7 did not fire.** No new route; the inventory gate still reports 80.

#### Ruling 18 — one line, and deliberately in the outer layer

```
!exclude.includes(candidate.personId) &&
candidate.state !== 'OUT' &&          // Ruling 18 — above mayHoldRow, not inside it
mayHoldRow(…)
```

**Above `mayHoldRow`, not inside it, and that placement IS the ruling.** `same-team.ts` answers "may
this person hold this row" for every caller in the tree, and an out person still may. This is one
picker declining to propose it.

**Both halves of the boundary are asserted**, because the split looks like an inconsistency without
them:

| | |
|---|---|
| the picker | refuses him — three assertions, including on a TASK row so the same-team exception cannot reopen the door |
| the route | **still accepts him — 200, and the row really moves** |
| the whole assignment path | carries **no** attendance gate — asserted on the route source and on `same-team.ts` |

The mutation that pushes the filter down into `mayHoldRow` — the mistake the ruling names — fails
**three** boundary assertions.

#### Ruling 19 — the third gate, and the child rule still leads

`resolveManualNudgeRecipient` now runs **CHILD → HOST → MARK**, each a 403 that says which. The
select was widened to fetch `nudgeMark`: a column left out of a narrow select reads as `undefined`,
which `isChaseable` would treat as chaseable, silently — the exact hazard
`tests/nudge-cadence-controls-test.ts` was written for, so the assertion that turns the column into a
403 is also the select-proof.

**The refusal, as the host reads it:**

> This person is marked don't-chase and will not be nudged. Change the mark in the pre-flight first.

It names where the mark is changed because **Ruling 17 sealed the glance's grey strips on exactly
that ground.** The two rulings are one argument: the grey has no door here *because* it has one
there.

⚠ **Ordering against opt-out (Do-Not-Touch Zone 7), checked rather than assumed.** `nudge-mark.ts`
requires opt-out be checked FIRST wherever both apply, so clearing a host-set mark can never resume
messaging someone who opted out. Not at risk here: this route's opt-out check is **not a refusal** —
it is a per-host, per-number channel switch that falls through to email — so the two never compete
to explain the same "no". Both still apply.

⚠ **One assertion had to be loosened, and it is worth saying which.** Phase 4 asserted the action
layer "never spells `DONT_CHASE`". Ruling 19 gave the refusal a shared constant whose *name* contains
the mark, and a bare-substring test cannot tell an import of the shared definition from a copy of it.
Both suites now test for the **quoted literal** (`'DONT_CHASE'`), which is what a second definition
actually looks like — and the mutation that re-implements the check as a literal still fails.

#### Test results

| Run | Result |
|---|---|
| RED — `nudge-cadence-controls`, Ruling 19 before the gate | **59 passed, 3 failed**, exit 1 |
| RED — `glance-actions`, Rulings 18 + 19 before either | **56 passed, 6 failed**, exit 1 |
| GREEN — `test:nudge-controls` | **62 passed, 0 failed** |
| GREEN — `test:glance-actions` | **62 passed, 0 failed** |

Both REDs left the *boundary* assertions green, correctly: "the route still accepts an OUT person",
"the child rule leads", "both doors post to the same URL" are guards on behaviour that must **not**
change, and a ruling that broke them would be the wrong build.

**Mutation-tested, each reverted:**

| Mutation | Assertions that failed |
|---|---|
| drop Ruling 18's OUT filter | 3 |
| push the OUT filter down into `mayHoldRow` (the ruling's named mistake) | 3 — all boundary |
| drop Ruling 19's route gate | 2 |
| gate the mark BEFORE the child rule | 1 — §10.6's ordering proof |

#### Walk

**Ruling 18, on the mixed board.** Amelia's Mains picker: `Rob Whittaker · Charlotte Turner · James
Turner · Sarah Dalton · Bob Unhoused`. **Ray Dalton is gone** — and he is still a strip on the board,
faded, carrying "— out". Removed from the picker, not from the screen.

**Ruling 19, through V1's own request.** `POST /api/events/[id]/people/[personId]/nudge` with
`{template:'warm', message}` — byte-for-byte what `NudgeComposer` sends — at Aoife O'Brien, marked
don't-chase:

```
403  {"error":"This person is marked don't-chase and will not be nudged. Change the mark in the pre-flight first."}
```

**That is V1's button refusing.** There is no second route to gate: V1's composer and the Moment 4
glance build the same URL, and a test asserts they still do.

### Gates

`tsc` 0 · `build` 0 (*"No pending migrations to apply"*) · `format:check` 0 · `lint` **99 before,
99 after, 0 in glance** (baseline measured by stashing this phase's work) · `test:security` 51/51 ·
inventory gate **80 routes, 0 critical, 0 weak** · `test:glance-actions` 52/52 ·
`test:glance-grid` 85/85 · `test:glance-read` 115/115.

Fourteen adjacent suites green: `nudge-cadence` 54, `nudge-controls` 52, `host-holds-items` 22,
`host-never-messaged` 20, `child-assignment-eligibility` 17, `child-message-exclusion` 19,
`host-nudge` 40, `message-composition` 54, `nudge-provider-gate` 37, `decide-by-clock` 46,
`attendance` 51, `guest-response`, `unassigned-critical-items`, `url-no-double-slash`.

⚠ **The dev server had to be restarted before this phase could be tested at all** — it was answering
500 on every route from phase 3's own recorded trap (`npm run build` rewrites `.next` under a live
Turbopack server). The trap fired again at this phase's build gate and the restart was repeated
after it. Still worth the line in `GATHER-KNOWN-BEHAVIOURS.md` that phase 3 flagged and did not take.

⚠ **The mobile walk was measured indirectly.** The browser pane's viewport emulation does not reach
`position: fixed`, so the scrim sized to the real window and the numbers were meaningless. Measured
instead by constraining the scrim to a 375-wide containing block: **panel 343px at x=16, height 338,
select 152px, both row buttons on one line, no internal scroll.** The board itself was walked at
375px in phase 2 and is unchanged.

---

## Evidence — phase 3b (Executor-Completed, 2026-08-31) — the assistant's message

**Commit:** `c43672e` · **Branch:** `feat/moment-one-redesign`

### What was built

| Path | What |
|---|---|
| `src/components/glance/assistant.ts` | `findCriticalRedHits`, `assistantMessage`. Pure; reads only what the board already carries. |
| `src/components/glance/GlanceBoard.tsx` | The message, between the strip and the grid. |
| `tests/glance-grid-test.tsx` | 18 new assertions; the module imports were also isolated (see below). |
| `tests/glance-read-test.ts` | Ruling 1's behaviour fence extended to `assistant.ts`. |
| `scripts/seed-gtc192-glance.ts` | Aoife's expired maybe made critical, so Ruling 14's silence is visible and not merely asserted. |

**No new data.** Person name and state, item name, criticality and state — all already in the
payload. No new field, no second query, no fence relaxed. **No migration; SC7 did not fire.**

### The condition — two clauses, both earning their place

**The item must be critical AND red; the person holding it must be red.**

The item clause stops the message naming a critical that is perfectly fine: a person can be red
for one row while holding a settled critical, and pointing at the settled one is worse than
silence. §8.2 — non-critical reds sit in the grid without a message.

The person clause looks redundant, because worst-colour-wins usually makes a red row's holder
red. It is not redundant in exactly one case, and that case is the point: **under Ruling 14 the
don't-chase mark greys the strip while the rows underneath stay red.** So a person the host has
taken off the system's hands can hold a red critical and read neutral — and the message must not
fire for them.

⚠ **That exclusion is BY CONSTRUCTION.** Nothing in `assistant.ts` names the mark; it reads the
person's state, and Ruling 14 was already applied upstream in `derivePersonState`. A
`nudgeMark !== …` check here would be a second definition of one rule, free to drift. A test
asserts on this file's source that the mark is never named — and the mutation that re-implements
the exclusion as an explicit check **fails that assertion**, which is the point of having it.

### The wording

| Case | Message |
|---|---|
| One | `Gather is out of moves — the glazed ham, with Sarah Dalton. It’s yours now.` |
| Several | `Gather is out of moves on 2 critical items — The pavlova with Amelia Turner · the glazed ham with Sarah Dalton. They’re yours now.` |
| None | *(nothing renders)* |

§3's three beats, in order: **admitting what the system couldn't do**, **naming the thing**,
**handing it over**.

**Why the admission is generic rather than per-reason.** "Gone quiet after two nudges" would read
better, but the three red sources fail differently and only one is about reach. §8.6 supplies the
sentence true of all of them — a withdrawn claim *"isn't a silence to chase, it's a fact the
system can't fix"*. Out of moves is that fact, honest for an expired maybe, a handed-back row and
an exhausted silence alike. The per-reason why still shows, on the person's own strip.

**Why it avoids "needs you", which §3's own example uses** (*"two things need you"*). The summary
directly above already says *"2 need you"* and is counting **people** (Ruling 2). Two lines a
centimetre apart both saying "N need you" about different nouns is the one thing a four-second
screen cannot afford. The handover is carried by *"yours"* instead — §3's own gloss on red,
*"only you can do this"*.

⚠ **THE ITEM NAME NEVER SITS AT A SENTENCE BOUNDARY, and that is a finding, not a preference.**
Names are host- and AI-authored and arrive in **both** cases — the design reference writes *"the
glazed ham"*, this repo's fixtures write *"The pavlova"*. The browser walk showed both failures in
turn: mid-sentence a capital stumbles (*"out of moves on The pavlova"*), and at the head of a
sentence a lower-case name reads as a typo. **The fix is position, not transformation** — every
name follows an em-dash, exactly where the alert strip already puts them, and where either case
reads as a label. Lower-casing to fit would mangle a proper noun the first time one appeared. A
test now pins both cases.

### Placement — between the strip and the grid, and NOT a second filled band

Two objects, not one. The strip is about criticals with **no** owner; this is about a critical
with the **wrong** one. Folding them into a single red block would make one band say two
different things.

**It is deliberately not a second filled danger band.** Two stacked fills dilute the strip exactly
as a second alarm dilutes the first, so this carries the same red as **text and a left rule**, and
the strip stays the only filled band above the grid. A test counts them, and the mutation that
gives the message its own fill fails it.

⚠ The Scope line calling this *"banner-class, the only one the surface has"* **predates Ruling 8**,
which put the alert strip above it. "The only one" is no longer literally available; the weighting
above is what replaces it.

### Test results

| Run | Result |
|---|---|
| RED — first attempt | **36 passed, 45 failed** — see the note below |
| RED — after isolating the imports | **65 passed, 16 failed**, every failure in `[§3 assistant]` |
| RED — the reworded singular, after the walk | **80 passed, 2 failed**, then **81 / 1** |
| GREEN | **82 passed, 0 failed** |

**The first RED was 45 failures that were really one.** A shared `try{}` around four dynamic
imports meant the missing `assistant.ts` aborted the other three, so `GlanceBoard` and `read`
never loaded and every phase-1/2/3 assertion failed as collateral. The imports are now isolated
per module, and the RED that was recorded is the legible one. A RED that cannot be read is not
evidence.

**Mutation-tested, each reverted:**

| Mutation | Failed |
|---|---|
| drop the person clause — the don't-chase holder starts triggering it | 1 |
| re-implement the exclusion as an explicit mark check | 1 (the by-construction fence) |
| one message per hit — a stack instead of one covering all | 1 |
| give the message its own filled danger band | 1 (the dilution fence) |
| an exclamation mark in the copy | 2 |

### Browser walk

Mixed board, after a dev-server restart following the build. **Exactly one message element in the
DOM**; the strip is the only filled band above the grid; no buttons in `main`; no console errors.

> **2 critical items have no owner** — the glazed ham · the marquee
> *and 4 more unassigned*
> │ Gather is out of moves — The pavlova, with Amelia Turner. It’s yours now.

**Ruling 14 is visible on the same screen.** Aoife O'Brien holds a **critical** expired maybe and
is marked don't-chase: her strip is the neutral grey, and **she is not named in the message**. The
seed was changed to make that contrast real rather than only asserted.

⚠ **A raw-HTML count of `data-assistant-message` returns 2, and that is not a duplicate.** The
second occurrence is Next's RSC flight payload, not a rendered element —
`document.querySelectorAll` returns **1**. Recorded because the raw count is the obvious thing to
measure and it lies.

### Gates

`tsc` 0 · `build` 0 · `format:check` 0 · lint 102 = baseline, 0 in glance · `test:security` 51/51
· inventory gate 80 routes, 0 critical · `test:glance-grid` 82/82 · `test:glance-read` 111/111 ·
nine adjacent suites green.

---

## Evidence — phase 3 (Executor-Completed, 2026-08-31)

**Commit:** `8792080` · **Branch:** `feat/moment-one-redesign`

### What was built

| Path | What |
|---|---|
| `src/lib/glance/state.ts` | `GlanceUnassignedItem`; `EventGlance` gains `unassignedCritical` and `unassignedOrdinaryCount`. |
| `src/lib/glance/read.ts` | Two queries inside the existing `Promise.all` — criticals named, ordinary counted. |
| `src/components/glance/strip.ts` | `criticalStripClauses`, `criticalStripText`, `unassignedDoorText`, `unassignedDoorHref`. |
| `src/components/glance/GlanceBoard.tsx` | The strip, above the grid and below the summary. |
| `tests/glance-grid-test.tsx` · `tests/glance-read-test.ts` | 17 new assertions. |
| `scripts/seed-gtc192-glance.ts` | Ownerless items on the mixed board. |

**No migration. Stop Condition 7 did not fire.**

### One fetch, not two

The page still makes **one** read. Both queries live inside `readEventGlance`'s existing
`Promise.all`, so the strip and the grid come from one call. Criticals are fetched with
`select: { id, name }`; ordinary unassigned items are a `count()`, not a `findMany` — a
`findMany` would haul every name across for a number, and those names would then be one
careless render away from a surface Ruling 8 says must not carry them.

**"Unassigned" is `assignment: null`** — the house predicate, shared with the pre-flight route,
`/api/c/[token]`, `workflow.ts` and `detectUnassignedItems`. `Item.status` is deliberately not
read (architecture-contract §6), and a mutation swapping the predicate for
`status: 'UNASSIGNED'` **changed the answer**, not just the source: the cache and the truth
already disagree on this fixture. That is the §6 mistake demonstrated live, and it is why the
assertion is on the predicate rather than on the result.

⚠ **NARROWER THAN THE PRE-FLIGHT'S "critical gaps".** This strip is *critical AND has no
Assignment row*. "Critical without an ACCEPTED assignment" is a wider set and a different fact,
which the pre-flight route already shows and already documents as different. A critical held by
someone who declined it **has** a person, reads RED on their strip, and is not ownerless.

### The door

**It links, and it links to `/plan/[eventId]?expand=teams`** — the destination the pre-flight
already uses for this exact errand, on a query contract the plan page already honours. No V1
change, no new surface, and no second place to fix coverage.

**Why link rather than expand in place:** Ruling 8's own sentence — *"Ordinary unassigned items
stay the plan's and pre-flight's business; the glance does not nag about what can wait."*
Expanding the list on the glance is the glance taking that business back, and Ruling 1's general
test (does this make the host lean in?) refuses it. The door hands off.

**The door hangs off the strip.** With no ownerless critical there is no strip and no door, even
when ordinary unassigned items exist — *"and N more unassigned"* is a continuation, and a glance
that volunteers "and 9 more unassigned" against no criticals is nagging about what can wait. ⚠
**This is a reading, not a ruled point** — the direction specified only the N=0 case. One
boolean if it should be overturned.

### Test results

| Run | Result |
|---|---|
| RED — grid, before any of phase 3 | **53 passed, 12 failed**, exit 1 |
| RED — read, before any of phase 3 | **106 passed, 5 failed**, exit 1 |
| GREEN — `test:glance-grid` | **64 passed, 0 failed** |
| GREEN — `test:glance-read` | **111 passed, 0 failed** |

Three negative assertions passed vacuously in the first RED (nothing rendered, so nothing could
be wrong); each was gated on the positive case before the RED was recorded.

**Mutation-tested, each reverted:**

| Mutation | Failed |
|---|---|
| an "All criticals have an owner" all-clear banner | 3 |
| ordinary unassigned items named in the strip | 2 |
| `Item.status` read instead of `assignment: null` | 4 |
| the door rendered without a strip above it | 1 |

**One phase-2 guard was retired, not deleted quietly**: *"no alert strip — unassigned criticals
are phase 3"* held its line until phase 3 arrived. Its replacement is named at the site.

### The strip on the mixed fixture

> **2 critical items have no owner** — the glazed ham · the marquee
> *and 4 more unassigned*

Danger tint, above the grid, below an **unchanged** summary (`2 need you. Gather is on 3. 6
settled.`). The door renders as an `<a>` to `/plan/<id>?expand=teams` — no button anywhere on the
surface. On the oversized board (no ownerless items): **no strip, no door, no all-clear text**,
`Nothing needs you. 64 settled.` Verified live in the DOM, not only in the test.

### CONSIDERED AND DECLINED — the summary sentence stays people-only (founder, 2026-08-31)

**Recorded as settled so it is not re-derived.** Phase 3 raised it, the founder considered it the
same evening, and the answer is **no change**. The sentence counts people; an ownerless item is
not a person; Ruling 2 stands exactly as written.

**The question, stated fairly** — because the tension is real and the next reader will find it.
With zero red **people** and one ownerless **critical**, the surface reads:

> Nothing needs you.
> **1 critical item has no owner — the glazed ham**

The summary is right by its own rule and the strip is right by Ruling 8, and yet the pair can be
read as a contradiction — on a screen whose entire purpose is a four-second answer that is not
contradicted.

**Why it is declined anyway — the strip already gets there first.** Not first in the document: the
summary is above it, in the reference's own order. First **in the eye**. The strip is
danger-tinted and has area, and that is the exact property the chosen design was chosen for —
*"Colour has area, so red pulls the eye from across the screen without any sorting or
sectioning."* A host who has an ownerless critical sees the red band before she parses the
sentence above it, so "Nothing needs you" is never the last thing she reads. The contradiction is
available to a careful reader and not to a glancing one, and this screen is built for the glancing
one.

**Two consequences of the decline, so the reasoning stays load-bearing:**

- **The strip must keep its tint and its area.** If a later phase quiets it — a smaller line, a
  neutral tone, a collapse into text — the argument for this decline goes with it, and the
  sentence question reopens. Any change to `STRIP_TONE`-scale prominence on the alert strip should
  come back here first.
- **It does not generalise to a strip that is scrolled away.** On a very long board the summary and
  the strip are adjacent at the top, which is what makes them read as one unit. Nothing about this
  decline licenses putting a future ownerless-critical signal somewhere the summary cannot be seen
  with it.

**The two alternatives, recorded and rejected**, so neither is re-proposed from scratch: letting
the lead clause yield (*"1 critical item needs you."*) makes one sentence count two different kinds
of thing; suppressing the "Nothing" wording while the strip is up loses the four-second answer at
exactly the moment it is least true. Both were weighed; neither is better than silence plus a red
band.

### Flagged — Ruling 6's fall-loose clause is not in this strip

Ruling 6: *"Items the person held fall loose; criticals among them surface in the alert strip
(Ruling 8)."* **Not built here.** A critical held by an OUT person still shows under that person,
not in the strip, because the strip's test is "no Assignment row" and theirs still has one. That
clause belongs to the reversal lifecycle, which is phase 5/6 (it needs the last-seen record), and
building half of it now would put a second definition of "loose" in the tree. Recorded so phase 6
inherits it rather than rediscovers it.

### Gates

`tsc` 0 · `build` 0 · `format:check` 0 · lint 102 = baseline, 0 in glance · `test:security` 51/51
· inventory gate 80 routes, 0 critical · eleven adjacent suites green, including
`test:unassigned-critical-items`.

⚠ **`npm run build` breaks a running `npm run dev`.** Measured this pass: the production build
rewrites `.next` under the live Turbopack server, and every subsequent page 500s with `ENOENT` on
`.next/server/.../app-build-manifest.json`. It looked exactly like a defect in the new page and
was not — the fix is to restart the dev server. Worth a line in
`GATHER-KNOWN-BEHAVIOURS.md`; not done here, as that file is outside this ticket's scope.

---

## Evidence — phase 2 (Executor-Completed, 2026-08-31)

**Commit:** `c49fe02` · **Branch:** `feat/moment-one-redesign`

### What was built

| Path | What |
|---|---|
| `src/app/plan/[eventId]/glance/page.tsx` | The page. Server component, `requireEventRole(['HOST','COHOST'])` reused unmodified, `readEventGlance`, `notFound()` on refusal. |
| `src/components/glance/GlanceBoard.tsx` | The grid. Presentational — no client hooks, no data access, no handlers. |
| `src/components/glance/strip.ts` | The tones and the copy, pure: `STRIP_TONE`, `stripLabel`, `whyLineFor`, `summaryClauses`, `summarySentence`. |
| `tests/glance-grid-test.tsx` | 51 assertions in four layers — pure, rendered markup, DB order anchor, structural. |
| `tests/tsconfig.json` | `jsx: react-jsx` for the test run only. `tsx` otherwise falls back to the classic transform and cannot render a component that has no `React` import; the project's own `jsx: preserve` is untouched. |
| `scripts/seed-gtc192-glance.ts` | The two boards the browser walk needs, plus the session token. Re-runnable. |
| `package.json` | `test:glance-grid`. |
| `tests/glance-read-test.ts` | Phase 1's behaviour fence extended to the page, the board and the strip module. |

**No migration. Stop Condition 7 did not fire.** `npm run build` reported *"No pending migrations to
apply."*

### The route-vs-module question, closed

Phase 1 built both doors and declined to choose. **Phase 2 chooses the module, read from a server
component**, and the reason is the four-second contract itself: a client fetch paints an empty frame
first, so the four seconds start again when the data lands. A server component has the board in the
first paint.

**The HTTP route is not thereby vestigial.** Ruling 10's ~20-second polling is a client job, and it
is what the route will refresh from in phase 6. First paint from the module, refreshes from the
route, **one assembly behind both** — `readEventGlance`. Neither door re-derives anything, which the
test asserts on the page's source.

### Ruling 3's anchor, stated

**Household creation order (`Household.createdAt`), with the host's household lifted to the front by
a stable sort.** Members within a card: `householdRole` rank, then name, then id. Unhoused: name,
then id.

**Not one comparator reads `state`** — asserted structurally, and behaviourally by reading one event
twice with every response permuted in between (accepted / declined / maybe) and requiring
byte-identical household and member ordering. The fixture creates the host's household **last**, so
"host first" cannot pass by accident of capture order.

### The zero case, chosen

| Counts | Sentence |
|---|---|
| 3 / 9 / 28 | `3 need you. Gather is on 9. 28 settled.` |
| 0 / 9 / 28 | `Nothing needs you. Gather is on 9. 28 settled.` |
| 1 / 0 / 0 | `1 needs you.` |
| 0 / 0 / 0 | `Nothing needs you.` |

**The lead clause is always present and is worded, never counted** — it is the four-second answer,
and at zero it should read as relief rather than as a tally. **The other two clauses are dropped at
zero rather than reworded**: "Gather is on 0" and "0 settled" are noise. The singular is handled —
`1 needs you`, not `1 need you`.

### Test results

| Run | Result |
|---|---|
| RED — grid test before any of phase 2 existed | **2 passed, 48 failed**, exit 1 |
| RED — phase 1's fence, with the page expected in the tree | **80 passed, 26 failed**, exit 1 |
| RED — the shortened why-line, after the browser walk | **48 passed, 3 failed**, exit 1 |
| GREEN — `test:glance-grid` | **51 passed, 0 failed**, exit 0 |
| GREEN — `test:glance-read` | **106 passed, 0 failed**, exit 0 |

The first RED left two assertions green, and both are guards on code that already existed (the
reader's comparators, and the V1 god file being unmodified). One genuinely vacuous green — a
why-line check that an empty string trivially satisfied — was found and closed before the RED run
was taken.

**Mutation-tested, each reverted:**

| Mutation | Assertions that failed |
|---|---|
| reds sorted to the top-left (the queue Ruling 3 refuses) | 3 |
| green folded to a count above 12 members | 2 |
| the two greys collapsed into one | 3 |
| `0 need you` instead of the worded zero | 3 |
| the mockup's `12 days to go` countdown inherited | 1 |

### Browser walk

Two seeded boards, `npm run dev`, screenshots taken, no console errors.

- **Mixed board** — 5 households, 13 people, every state at once. `2 need you. Gather is on 3. 6
  settled.` Reds carry their whys; amber and green are bare. **Ray Dalton — out** renders faded with
  no border directly beside **Aoife OBrien**, bordered and full-strength: the two greys read as a
  deliberate pair in the screenshot, which is Ruling 7's own test. Aoife holds an expired maybe and
  is still grey — Ruling 14 on screen.
- **Oversized board** — 16 households, 64 people, all settled. `Nothing needs you. 64 settled.`
  Every one of the 64 is a strip; nothing folds (Ruling 5).
- **Mobile (375px)** — one column; the why-lines fit on one line.

### Refusal-list check (§3 plus Ruling 1's general test)

No chat, no countdown, no activity feed, no plan content, no analytics, no progress bar, no `%` —
asserted against the rendered markup, not assumed. **⚠ The mockup's own meta line carries a
countdown (`12 days to go`) and this does not.** §3 refuses countdowns; the mockup is the picture,
not the specification, on that line. A mutation that inherits it fails the suite.

### Decisions taken in the build

1. **The why-line copy.** `handed it back` / `handed N back` (reversal), `maybe timed out` (expired
   maybe), `gone quiet` (exhaustion, waiting on [[GTC-251]] for the count the reference's *"quiet
   after 2 nudges"* wants). ⚠ **Neither of the reference's two example whys can be rendered today** —
   one needs E6's count, the other is Ruling 6's attendance reversal behind phase 5. The copy above
   is what the two live red sources can honestly say. **The browser walk changed it**: the first
   wording wrapped a red strip to three rows at the reference's `minmax(160px, 1fr)`, so the lines
   are now capped at 16 characters by a test rather than by the writer's ear.
2. **Card labels are the primary contact's full name**, as `HouseholdCardList` already titles
   households. The mockup's surnames ("Turner", "Aunt June") are a naming derivation no column
   supports.
3. **Household-less people get a final card labelled "Not in a household"** — visually identical,
   marked `data-unhoused-card` so the neutrality assertions still count exactly the real households.
   **Not a ruling**; the least-committing way to keep everyone on the board.
4. **The palette lives beside the screen**, not in `tailwind.config.js` or `globals.css`, because
   those are shared with V1 and V1 is out of scope this phase.

### ⚠ Flagged — the host is GREEN, not NOT_CHASED

The phase 2 direction said *"The host renders like anyone else — grey not-chased strip in her own
card, per phase 1's payload."* **The operative half was built exactly: there is no host special-case
anywhere in the view**, and her strip is whatever the payload gives her. **But phase 1's payload
gives her GREEN, not NOT_CHASED**, and so does the mockup — `moment4-glance-mockup.html` renders
`<div class="strip g">Kate</div>`, green, and its only neutral strip is Aoife's.

Phase 1's rule, which the founder confirmed alongside Ruling 16, is that the host is never AMBER by
absence of a reply because [[GTC-256]] Ruling 5 means no ask is ever made to her — so an unanswered
row of hers reads settled. Nothing was changed under a phase 2 ticket to match the recollection.

**If she should be grey, it is a one-line change in `derivePersonState` and needs no change to the
view at all** — which is the argument for leaving it to a ruling rather than to a render.

---

## Evidence — phase 1 (Executor-Completed, 2026-08-31)

**Commit:** `307c5cc` · **Branch:** `feat/moment-one-redesign`

### What was built

| Path | What |
|---|---|
| `src/lib/glance/state.ts` | The colour encoding, pure and client-safe. `deriveItemState`, `derivePersonState`, `worstItemState`, `summarisePeople`, `nextNudgeFor`, `decideByAtFor`, and the `EventGlance` / `GlanceHousehold` / `GlancePerson` / `GlanceItem` types. |
| `src/lib/glance/read.ts` | `readEventGlance(db, eventId, now)` — three explicit `select`s, no `include`, `now` injected. |
| `src/app/api/events/[id]/glance/route.ts` | `GET`, host-scoped via `requireEventRole(['HOST', 'COHOST'])`, the same guard `/api/events/[id]/pre-flight` uses. Assembles nothing of its own. |
| `tests/glance-read-test.ts` | 106 assertions in four layers — pure, DB, runtime fence, structural fence. |
| `package.json` | `test:glance-read`. |
| `route-classifications.json` | The new route, `SESSION` / `requireEventRole`. Held true by the `route auth` assertions rather than asserted only in the data file. |

**No migration. Stop Condition 7 did not fire** — nothing in phase 1 needs schema.
`prisma migrate status`: *"Database schema is up to date"*, 41 migrations; `git status prisma/`
empty; `npm run build` reported *"No pending migrations to apply."*

### Payload shape

```
EventGlance { eventId, asOf, summary, households[], unhoused[] }
  summary            { needYou, withGather, settled }   three whole numbers of PEOPLE
  GlanceHousehold    { householdId, primaryContactName, isHostHousehold, members[] }
  GlancePerson       { personEventId, personId, name, isHost, householdRole,
                       state, reasons[], nextNudgeAt, items[] }
  GlanceItem         { itemId, assignmentId, name, critical, state, reason, decideByAt }
```

`state` is `RED | AMBER | GREEN | NOT_CHASED | OUT`. **No household carries a state**, in the type
or on the wire.

### Test results

| Run | Result |
|---|---|
| RED — before any module existed | **0 passed, 96 failed**, exit 1 |
| GREEN — phase 1 as first built | **96 passed, 0 failed**, exit 0 |
| RED — Rulings 14/15/16 + the route, tests first | **68 passed, 38 failed**, exit 1 |
| GREEN — after Ruling 14 and the route | **106 passed, 0 failed**, exit 0 |

Every fence is gated on the module under test actually existing; a first pass in which 23
source-fences passed vacuously against a missing file was corrected before the RED run was taken.

**Mutation-tested, each reverted** — a guard that cannot fail is not a guard:

| Mutation | Assertions that failed |
|---|---|
| `firstNudgeSentAt: true` added to the select | 1 |
| a `state` field on `GlanceHousehold` + a `worstHouseholdState` export | 3 |
| the summary divided by headcount | 5 |
| the seam replaced by `exhaustion: { exhausted: false }` | 2 |

### Assertions met

- **Person-keyed, not item-keyed** (§10.8) — no top-level item collection; every item reached
  through a member. Asserted on the payload, not assumed.
- **Worst-colour-wins per person** — a live maybe plus an expired maybe reads RED, through
  `isDecideByExpired` in `src/lib/decide-by.ts` rather than a second definition of the clock.
- **No household colour at any layer** (Ruling 7 of the design reference; this ticket's still-open
  item 3) — asserted on the emitted objects, on the `GlanceHousehold` declaration, and on the
  absence of a merge export.
- **Ruling 3's anchor** — the host's own household holds first position, and it is the only one so
  marked. Two reads of the same event give the same order.
- **Ruling 2** — three counts of people; no division or modulo in `summarisePeople`, asserted on
  the function body.
- **Ruling 14** — grey beats the expired maybe, the reversal, exhaustion, and a red mixed among
  greens, in the pure layer and against real rows; GREEN and OUT unaffected.
- **Ruling 1's fence, from birth** — 23 behaviour-bearing names denied on the payload keys at any
  depth AND on the source of all three modules with comments stripped; `include:` forbidden, so no
  whole row can spread in.
- **The [[GTC-251]] seam** — `EXHAUSTED_SILENCE` is in the red vocabulary from birth (one door,
  §8.1); the derivation consumes an `ExhaustionFact | null` where **null means "no signal", not
  "not exhausted"**; the reader supplies `null` at `ANCHOR(GTC-251)`. Supplying `{ exhausted: true }`
  turns a PENDING row red — the door carries weight without a predicate that lies. E6 cannot hand
  this module raw telemetry: `firstNudgeSentAt` / `secondNudgeSentAt` are inside the fence.

### Gates

`npx tsc --noEmit` 0 · `npm run build` 0 · `npm run format:check` 0 · `npm run test:security` 0
(51/51) · `tests/security-inventory-gate.ts` 0 (80 routes, 0 critical, 0 weak) ·
`test:glance-read` 0 (106/106) · `test:nudge-cadence` 0 (54/54) · `test:nudge-controls` 0 (52/52) ·
`test:host-holds-items` 0 (22/22) · `test:host-never-messaged` 0 (20/20) · `test:decide-by-clock`,
`test:guest-response`, `test:attendance` 0.

`npm run lint`: 102 findings before and after, none in the new files — pre-existing baseline
(`next.config.js` carries `eslint.ignoreDuringBuilds: true`, ended by [[GTC-221]]).

### Decisions taken in the build, beyond the rulings

Colour encoding is this ticket's ([[GTC-175]] records it twice). Recorded so they are visible
rather than buried:

1. **A `DECLINED` row is RED, reason `REVERSAL`.** §8.6: declining leaves the row on the person, so
   it is held by someone who will not do it and only Kate can move it.
2. **The host's `PENDING` row is GREEN, not AMBER.** Amber means Gather has a next move;
   [[GTC-256]] Ruling 5 says it never has one for her, and its Ruling 4 has her holding items
   silently. The pick is the decision.
3. **Household-less people are returned in `unhoused[]`, not filed into an invented card.** 61 of
   93 `PersonEvent` rows in `gather_dev` have no household. Where they render is a layout question
   no ruling answers, and phase 2 should not inherit an answer from phase 1's data shape.
4. **Member order inside a card** is `householdRole` rank, then name, then id — a deterministic
   default so Ruling 3's fixed positions hold all the way down. **Not a ruling**; `PersonEvent`
   carries no `createdAt` to sort capture order on.

### Flagged, not fixed

**On events created before [[GTC-256]], no `PersonEvent` carries `role: HOST`** — one such row
exists across all 93 in `gather_dev`. Ruling 3's anchor therefore has nothing to anchor to on
legacy events (`isHostHousehold` is false on every card, and the host is absent from her own
board). This is the direct consequence of GTC-256's **Ruling 12, no backfill (conditional)**, not a
defect in this read. New events are correct. If the glance is to be demonstrated on a legacy event,
that event needs reseeding rather than this module needing a special case.

---

### Closed ticket format (for the executor)

```
# GTC-192 — J1: The screen — CLOSED
Status: Complete | Branch: [...] | Commit: [hash] | Effort: xhigh
## What was built
## Refusal-list verification (explicit checklist)
## Test results
## Assertions met
## Evidence
```
