---
ticket: GTC-086
title: "Demo Page: Update Step 3 to reflect contact import"
status: closed
branch: master
commit: 0dde373
moment: null
type: build
depends_on: []
blocks: []
tags:
  - build
  - contacts-import
  - home-demo
  - seed-data
---

# [[GTC-086]] — Demo Page: Update Step 3 to reflect contact import — CLOSED
**Status:** Complete | **Branch:** master | **Commit:** 0dde373

### What was found
Step 3 ("Added her people") in `src/app/demo/page.tsx` predated the
VCF/CSV contact-import feature shipped in [[GTC-082]]/083 and made no
mention of Sarah importing contacts from her phone. The narrative
jumped straight into "She added 43 family members with their:"
followed by the bullet list.

### What was changed
Added a single sentence above the existing "She added 43 family
members with their:" paragraph in Step 3:

> "She imported her contacts directly from her phone."

No other content on the demo page was modified. No other files were
touched.

### Known limitations
None. Copy-only change inside one StepBlock.

### Assertions met
1. ✅ Step 3 contains "She imported her contacts directly from her
   phone." appearing before the bullet list.
2. ✅ The bullet list in Step 3 is otherwise unchanged (Names and
   contact details / Dietary requirements / Which team they're
   helping with).
3. ✅ No other content on the demo page has been modified — diff
   is a single added `<p>` line.
4. ✅ No console.log statements in `src/` after changes
   (preflight count = 0, no new ones added).

### Evidence
- Preflight: `console.log` count in `src/` = **0** ✓
- TypeScript: `npx tsc --noEmit` exits clean ✓
- Diff (single-line addition):

```diff
diff --git a/src/app/demo/page.tsx b/src/app/demo/page.tsx
--- a/src/app/demo/page.tsx
+++ b/src/app/demo/page.tsx
@@ -185,6 +185,7 @@ export default function DemoLandingPage() {
               isOpen={openStep === 3}
               onToggle={() => toggleStep(3)}
             >
+              <p className="text-gray-600 mb-2">She imported her contacts directly from her phone.</p>
               <p className="text-gray-600 mb-2">She added 43 family members with their:</p>
               <ul className="text-gray-600 space-y-1 pl-4 mb-3">
                 <li className="before:content-['•'] before:mr-2">Names and contact details</li>
```
