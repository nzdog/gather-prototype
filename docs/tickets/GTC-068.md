# GTC-068 — Wrap-Up Status modal countdown timer and rename — CLOSED

**Status:** Complete
**Branch:** gtc-042-part1-nudge-schema
**Commit:** 8e3c920

### What was found
- Modal title was at `src/app/plan/[eventId]/page.tsx:2808` — conditionally rendered as `'Completion Status'` (COMPLETE) or `'Complete Event'` (FROZEN). No "Wrap-Up Status" text existed; the ticket signal was slightly stale.
- Dispatch status API (`getDispatchSummary` in `src/lib/wrap-up.ts`) did NOT return `createdAt` — only aggregate counts and per-guest status. Added `earliestCreatedAt` (earliest undispatched link) to the summary response.
- The dispatch delay constant `DISPATCH_DELAY_MINUTES = 10` was already defined in `src/lib/wrap-up.ts`.

### What was changed

**`src/lib/wrap-up.ts`**
- Added `earliestCreatedAt: string | null` to `DispatchSummary` interface
- In `getDispatchSummary`, computed `earliestCreatedAt` from the earliest `createdAt` among undispatched links (returns null when no pending)

**`src/app/plan/[eventId]/page.tsx`**
- Changed modal title from conditional `'Completion Status' : 'Complete Event'` to static `"Event Complete"`
- Added `earliestCreatedAt` to `wrapUpDispatch` state type
- Added `countdownMinutes` state and a `useEffect` that computes remaining minutes until the 10-minute dispatch window, updating every 30 seconds
- Replaced queued copy "they will be sent shortly" with "messages will be sent within 10–20 minutes"
- Added countdown display: "Sending in X minute(s)" while within window, "Sending now — refresh to check status" after window passes
- Countdown hidden when no pending messages

### Known limitations / follow-up flags
- Timer accuracy depends on clock sync between client and server (standard for web apps)
- If host opens the modal before loading dispatch status, countdown won't show until they click "Load dispatch status" or "Refresh" (existing UX pattern, not changed)

### Assertions met
- [x] Modal title reads "Event Complete"
- [x] No visible "Wrap Up" text in modal
- [x] Timeline copy updated ("messages will be sent within 10–20 minutes")
- [x] Countdown shows "Sending in X minutes" while within 10-minute window
- [x] Countdown shows "Sending now — refresh to check status" after window passes
- [x] Timer updates every 30 seconds
- [x] Timer hidden when all sent (countdownMinutes set to null when pending === 0)
- [x] Security suite 16/16 passing

### Evidence
- Title confirmed: yes
- Countdown confirmed: yes
- TypeScript: clean compile
- Security: 16/16
- Commit: 8e3c920
