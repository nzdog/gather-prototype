# GTC-042 — Nudge System: Production Wiring

## Status: All Parts Complete

---

## Part 1 — Schema

### Root Cause
NudgeLog model was missing from `prisma/schema.prisma`. The nudge system had no
database table to write scheduling and delivery records to.

### Files Changed
- `prisma/schema.prisma` — Added `NudgeLog` model + `nudgeLogs` back-reference on `PersonEvent`
- `prisma/migrations/20260407021304_add_nudge_log/migration.sql` — Auto-generated migration

### Evidence
- Migration: Applied cleanly (`20260407021304_add_nudge_log`)
- `npm run typecheck`: Passes (exit 0)
- `npm run build`: Passes (exit 0)
- `npm run test:security`: 16/16 passed
- No existing nudge logic files modified

### Do-Not-Touch Compliance
- [x] `src/lib/sms/` — not modified
- [x] `src/app/api/cron/nudges/route.ts` — not modified
- [x] No existing nudge scheduling or eligibility logic modified

---

## Part 2 — Credentials Audit

### Audit Results (2026-04-07)

| Variable | Present in Railway production? |
|----------|-------------------------------|
| `TWILIO_ACCOUNT_SID` | No → Added by Nigel |
| `TWILIO_AUTH_TOKEN` | No → Added by Nigel |
| `TWILIO_PHONE_NUMBER` | No → Added by Nigel |
| `CRON_SECRET` | No → Added by Nigel |

No code changes. Credentials added manually via Railway dashboard.

---

## Part 3 — Cron Verification

### Step 1 — Manual endpoint test
Triggered: `GET /api/cron/nudges?secret=$CRON_SECRET`
Response: `200` — `{"success":true, "smsEnabled":true, candidates: 0, sent: 0}`

### Step 2 — NudgeLog table verification
Production migration `20260407021304_add_nudge_log` applied via `prisma migrate deploy`.
Query `SELECT status, COUNT(*) FROM "NudgeLog" GROUP BY status` returned 0 rows (table active, no nudges pending).

### Step 3 — cron-job.org configuration

| Field | Value |
|-------|-------|
| URL | `https://gather-prototype-production.up.railway.app/api/cron/nudges?secret=YOUR_CRON_SECRET` |
| Schedule | Every 15 minutes (`*/15 * * * *`) |
| Method | GET |
| Expected response code | 200 |

---

## Acceptance Assertions

- [x] NudgeLog table exists in production database
- [x] Cron endpoint responds 200 when triggered manually
- [x] NudgeLog records will be created when eligible candidates exist (0 currently — correct)
- [x] Twilio credentials confirmed present (`smsEnabled: true`)
- [x] cron-job.org setup documented and ready for Nigel to action

## Commit Hashes
- Part 1: `4dd05a2` (schema + migration)
