---
ticket: GTC-188
title: "I1 — The pre-flight screen"
status: open
branch: TBD
commit:
moment: 4
type: build
depends_on:
  - "[[GTC-185]]"
  - "[[GTC-172]]"
  - "[[GTC-179]]"
tags: [hinge, epic-i, pre-flight]
---

# GTC-188 — I1: The pre-flight screen

**Status:** Open | **Branch:** TBD | **Commit:** [pending] | **Effort:** xhigh

*Effort level is a proposal from the discovery-report ticket breakdown — run `/effort` manually
before execution.*

### Context

Hinge spec §1: the threshold is not a summary screen before a button — it is a guided check
Gather does with Kate, and as she moves through it, the weight lifts. The pre-flight explicitly
includes: **coverage** (everyone/everything accounted for, nothing unassigned she doesn't already
know about), **dietaries** (re-verified by name — G2, GTC-185), **the message, shown** (she reads
exactly what each person will receive before it goes), **recipients confirmed** (per household,
per the Moment 1 toggles — C1, GTC-172). Now also the per-event cadence-pace selector (§10.3 —
E2, GTC-179).

Today: no coverage sweep, no message-preview, no recipient-confirmation screen anywhere. The
closest existing artifact is `checkFreezeReadiness`'s warnings (`UNASSIGNED_ITEMS`,
`LOW_COMPLIANCE`, `CRITICAL_GAPS`), which A1's plan (GTC-167) may or may not repurpose for the
coverage-sweep piece — check A1's decision before assuming this ticket rebuilds coverage-checking
from scratch.

### Scope

**Fix here:**
- The pre-flight screen assembling: coverage-sweep, G2's dietary re-verify, a message preview
  (what each recipient will actually receive), C1's recipient confirmation, and E2's per-event
  pace selector — into one guided, has-an-end flow.

**Do not touch:**
- All Do-Not-Touch Zones in `GATHER-BUILD-CONSTANTS.md`.
- The press itself (I2's territory, GTC-189) — this is Kate's side of the threshold only, before
  anything is sent.

### Dependencies

G2 (GTC-185), C1 (GTC-172), E2 (GTC-179). Check A1's (GTC-167) plan for whether coverage-sweep
reuses or replaces `checkFreezeReadiness`.

### Acceptance

- [ ] All four pre-flight elements (coverage, dietary, message-shown, recipients-confirmed) plus
      the pace selector are present in one guided flow with a clear end.
- [ ] "The message, shown" displays exactly what the guest will receive — not a summary or
      approximation.
- [ ] `npx tsc --noEmit` clean, build passes.
- [ ] Browser walk through the full pre-flight on a seeded event.

### Stop Conditions

Standard eight from `BUG-TICKET-TEMPLATE.md`.

### Closed ticket format (for the executor)

```
# GTC-188 — I1: The pre-flight screen — CLOSED
Status: Complete | Branch: [...] | Commit: [hash] | Effort: xhigh
## What was built
## Test results
## Assertions met
## Evidence
```
