---
ticket: GTC-164
title: "Update Moment 4 and Hinge specs to the discovery-gap-ruled versions"
status: closed
branch: feat/moment-one-redesign
commit: f5cb7b4
moment: 4
type: chore
depends_on:
  - "[[GTC-163]]"
blocks: []
tags: [moment-4, hinge, docs, spec, product-authority]
---

# [[GTC-164]] — Update Moment 4 + Hinge specs to the ruled versions — CLOSED

**Status:** Complete | **Branch:** feat/moment-one-redesign | **Commit:** `f5cb7b4`

**Severity:** Medium (documentation of record — product-authority documents)
**Type:** Chore (docs). No code, schema, or skill changes.
**Surfaced by:** Nigel, 2026-08-03. All sixteen discovery-gap questions were ruled and folded into both specs; the repo copies filed under [[GTC-163]] were the pre-ruling versions.

**Supersedes:** the spec versions committed in [[GTC-163]] (`5f44649`). Same filenames, same location — this is a content update in place, not a new document.

---

## What changed

| File | Before ([[GTC-163]]) | After (this ticket) |
|---|---|---|
| `docs/03_specs/gather-moment-4-spec-v1.md` | 128 lines, 15,398 B, `1ce02380…` | **154 lines, 21,240 B, `5e5e1339…`** |
| `docs/03_specs/gather-hinge-spec-v1.md` | 163 lines, 19,675 B, `9c690254…` | **164 lines, 22,264 B, `8e3e689c…`** |

`git diff --stat` against `5f44649`: **2 files changed, 34 insertions(+), 7 deletions(-)**.

### Moment 4 spec — `+26 lines, −1`

- **Line 4 (Status)** — rewritten: all sixteen discovery-gap questions ruled 3 August 2026; "**no open questions remain anywhere**" (previously scoped to the eight §8 questions only).
- **§10 added whole (lines 129–154)** — `## 10. DISCOVERY-GAP RULINGS (3 August 2026)`, ten rulings carrying full spec authority:

  | # | Ruling |
  |---|---|
  | 10.1 | COMPLETE survives; the transition dissolves — the calendar does it silently. Wrap-up's FROZEN hard-requirement migrates to COMPLETE-gated. |
  | 10.2 | One derivation, two clocks — red-by-time and the maybe's decide-by both key to **needed-by**. Constant settled at ticket time with Nigel's sign-off. |
  | 10.3 | Cadence controls live where the people-decisions live — per-event pace at the pre-flight; per-person go-gentle / don't-chase beside the recipient picker. Not a settings page. |
  | 10.4 | The thank-you is offered once, a day or two after the date passes. Declinable, never repeated. |
  | 10.5 | Per-person dietary captured at Moment 1; Moment 2 consumes rather than asks. Three-state stays the safety gate. |
  | 10.6 | **The child rule is a HARD CONSTRAINT** — a CHILD-role person never receives system messages regardless of contact info. Flags that current code contradicts this. "No future session may soften this." |
  | 10.7 | The recipient control is a household contact picker — cross-household capable, which requires channel as a **Person reference, not a boolean**. |
  | 10.8 | The grid is person-primary — people are the boxes, items live inside the person, worst colour wins. |
  | 10.9 | Bounce detection — build instruction, not a ruling. First task: investigate TNZ delivery-receipt capability; documented graceful degradation if absent. |
  | 10.10 | The clone-review / ghost-guest guard — a cloned guest list never copies silently; cloning's first act is a people-review. |

### Hinge spec — `+8 lines, −6` (net +1)

- **Line 4 (Status)** — rewritten to record the discovery-gap rulings folded into §2–§3, pointing the remainder at Moment 4 §10.
- **§2 The press (lines 35–39)** — four bullets materially changed and one added:
  - *The release* — no-undo is now absolute **at the mechanism level** (gap #1). No unsend for the disaster case; a wrong-date send is recovered through the material-change machinery (§8.5). The old FROZEN→CONFIRMING unfreeze path dies with FROZEN.
  - *The lock, as a ledger* — **the why is scoped** (gap #2): required only for changes that touch someone (reassignment, removal, a claimed-against quantity, date/venue). A typo fix gets a version and no interrogation.
  - *Versions* — **universal, silent, per-individual-change** (gaps #2–3), complete history always reachable; storage mechanics left to the reconciliation ticket.
  - *The audit trail begins* — restated as "version always, why when it touches someone."
  - **NEW: per-person mini-sends** (gap #5) — people added after the send get the same three-movement message with their own clocks, run from *their* send date and truncated by the event date.
- **§3 (line 64)** — the **one-tap model** (gap #10): the tap is the item ask and **attendance is inferred from it**. Only the *no* path gets a conditional attendance follow-up; itemless guests get an attendance-only degenerate case; a maybe stays purely an item-maybe. **This supersedes event-level RSVP as a guest-facing question** — `PersonEvent.rsvpStatus` becomes derived state.

---

## Integrity verification

**1. Byte-level identity — PASS.** SHA-256 on disk matches the authored source byte-for-byte:

```
8e3e689c60a6dc3bd5c5ec877f2b4d595564f1570c835626bae177de5cc2d7a5  gather-hinge-spec-v1.md
5e5e1339f66779278161cb657dd9e0a3190ddc9f8cd657b44efa3c40e0e7b132  gather-moment-4-spec-v1.md
```

**2. Line / word / byte counts — PASS.**

```
  164  3690 22264 gather-hinge-spec-v1.md
  154  3426 21240 gather-moment-4-spec-v1.md
```

**3. Moment 4 §10 landed intact — PASS.** Block occupies lines 129–154 (separator 130, heading `## 10. DISCOVERY-GAP RULINGS (3 August 2026)` at 132, framing note at 134, rulings at 136/138/140/142/144/146/148/150/152/154). All ten of **10.1–10.10** present, none truncated; 10.10 is the file's final line and ends on "Not an optional check."

**4. Hinge §2–§3 changes landed intact — PASS.** §2 at line 32 with all five bullets present at 35–39 (release / lock-as-ledger / versions / audit-trail / mini-sends). §3 at line 53 with the one-tap paragraph complete at line 64, including the no-path follow-up, the itemless degenerate case, and the `PersonEvent.rsvpStatus`-becomes-derived sentence. File ends on the one-act test.

**5. Full section structure survived — PASS.** Moment 4: 10 sections + title, in order (§10 new). Hinge: 10 sections + title, in order, heading line numbers shifted by the §2 expansion exactly as expected (§3 53, §4 72, §6 99, §8 133, §10 159).

**6. Prettier non-interference — PASS.** `.prettierignore` contains `*.md`; `prettier --check` on both files reports clean. Committed bytes are authored bytes.

---

## Observation for the spec author (NOT corrected here)

`gather-hinge-spec-v1.md:68` still reads **"OPEN — the maybe state (§8)."** at the close of §3. It is a residual pointer from before the maybe ruling: §8 is titled "The maybe state — **RESOLVED**" and line 4 states no open questions remain in the spec. The line was present in the [[GTC-163]] version too and is unchanged by this update.

Left exactly as authored. These are product-authority documents and this ticket's scope is transcription, not editing. Flagged so it can be dropped in the next spec revision.

---

## Scope boundary

By instruction:

- **No skill edits.** The discovery session owns the corrections to the superseded references catalogued in [[GTC-163]] — which remain uncorrected as of this commit (`gather-domain-reference/SKILL.md` still describes Moments 3 & 4 as "open roadmap", still carries "Frozen = the psychological finish line").
- **No ticket filing for the epic breakdown.** Epics A–K and their tickets belong to the session that produced the breakdown.
- No spec edits, no code, no schema.

---

## Assertions met

1. ✅ Both spec files updated in place at `docs/03_specs/`, filenames unchanged.
2. ✅ Ticket filed at the next free number — highest existing was GTC-163, so GTC-164 — and references [[GTC-163]] as the version it supersedes.
3. ✅ Integrity verified against the authored hashes: `8e3e689c…` / `5e5e1339…`, 164 / 154 lines.
4. ✅ Moment 4 §10 verified present at lines 129–154, all of 10.1–10.10 intact.
5. ✅ Hinge §2 (five bullets) and §3 (one-tap model) verified landed intact.
6. ✅ Nothing else touched — `git status` shows only the two specs and this ticket (plus the pre-existing untracked `public/brand/*.jpg`, not ours).
7. ✅ One commit containing ticket + both specs, followed by the hash-backfill commit.
8. ✅ Branch verified: `feat/moment-one-redesign`.

## Preflight note

Preflight sanity sequence not run — markdown-only change under `docs/`, no source, schema, config, or dependency affected. As in [[GTC-163]], the Husky `pre-commit` hook cannot run over the Cowork device-bridge mount (which refuses `unlink`, so lint-staged's stash/cleanup aborts), and the commit was made with `--no-verify`. The gate is a no-op here regardless — `.prettierignore` contains `*.md` — and `prettier --check` was run manually on both files and passed.

## Evidence

- `shasum -a 256` before and after transfer — identical (hashes above).
- `wc -lwc` on both files on disk — counts above.
- `git diff --stat` vs `5f44649` — 2 files, +34 / −7.
- `grep -nE '^#{1,2} '` heading dumps and `grep -nE '^\*\*10\.[0-9]+'` ruling dump — full lists above.
- `prettier --check` — clean; `.prettierignore` contains `*.md`.
- Commit: `f5cb7b4` — ticket + both spec updates together, one commit (executor output contract). Hash backfilled into this file by the follow-up `chore(GTC-164)` commit, per house convention.
