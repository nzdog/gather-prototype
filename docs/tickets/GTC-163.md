---
ticket: GTC-163
title: "File Moment 4 and Hinge product specs into docs/03_specs"
status: closed
branch: feat/moment-one-redesign
commit: 
moment: 4
type: chore
depends_on: []
blocks: []
tags: [moment-4, hinge, docs, spec, product-authority]
---

# [[GTC-163]] — File Moment 4 + Hinge product specs — CLOSED

**Status:** Complete | **Branch:** feat/moment-one-redesign | **Commit:** (backfilled below)

**Severity:** Medium (documentation of record — product-authority documents entering the repo for the first time)
**Type:** Chore (docs). No code, schema, or skill changes.
**Surfaced by:** Nigel, 2026-08-03. Two specs authored outside the repo and handed over for filing.

---

## What this ticket does

Files two product-authority specifications into `docs/03_specs/`, alongside the existing spec set (`builder-spec-v1.3.3.md`, `csv-import-spec.md`, `current-data-model.md`, `implementation-plan-v1.3.3.md`).

| File | Lines | Bytes | SHA-256 |
|---|---|---|---|
| `docs/03_specs/gather-moment-4-spec-v1.md` | 128 | 15,398 | `1ce023802e468eef1bb45d9518e02bde757189b78e948f0f17df998ae1c8ec94` |
| `docs/03_specs/gather-hinge-spec-v1.md` | 163 | 19,675 | `9c690254d9e3014349f7c8795ed2d305e7447493dd754ce15ee597beaeab3dac` |

**What they are:**

- **`gather-moment-4-spec-v1.md`** — Moment 4 ("Is everyone sorted?"), record of the 24 July 2026 protocol walk. All eight formerly-open questions ruled 3 August 2026; **no open questions remain**. Establishes: sorted-is-the-host's-decision, the four-second contract, the colour encoding as *ownership* not status, criticality inherited from Moment 2, the runbook as a printed ownership artifact, and freeze dissolving into the send.
- **`gather-hinge-spec-v1.md`** — The Hinge (the send), record of the 3 August 2026 protocol walk plus the same-day maybe-state ruling. **No open questions remain.** Governs the threshold, the pre-flight, the outgoing messages, the minutes after the press, and send failure.

**Authority relationship (from the documents themselves):**

- The Moment 4 spec wins over any earlier document it disagrees with — explicitly naming the four-moment synthesis, the reconciliation campaign skill, and prior flow documents.
- The Hinge spec wins over the Moment 4 spec on the Hinge's territory (threshold, outgoing messages, the minutes after the press, send failure).
- Where both are silent, the walk transcripts are the tiebreaker before any inference.

**Filename note:** the specs cross-reference each other by the hyphenated names used above (Moment 4 §Status → `gather-hinge-spec-v1.md`; Hinge §Source of authority → `gather-moment-4-spec-v1.md`). The filenames on disk match those cross-references exactly.

---

## Integrity verification

Product-authority documents: a truncated copy is worse than no copy. Verified after transfer.

**1. Byte-level identity — PASS.** SHA-256 of each file on disk matches the source byte-for-byte (hashes in the table above; computed at source before transfer and re-computed on disk after).

**2. Line and byte counts — PASS.**

```
  163  3294 19675 gather-hinge-spec-v1.md
  128  2540 15398 gather-moment-4-spec-v1.md
```

**3. Section structure survived — PASS.**

`gather-moment-4-spec-v1.md` — 9 top-level sections, all present in order:

```
  1: # Moment 4 — Is Everyone Sorted?
  2: ## Specification v1
 10: ## 1. What the Moment is
 22: ## 2. Sorted
 31: ## 3. The screen
 54: ## 4. Silence and the nudge machinery
 64: ## 5. Criticality
 73: ## 6. The runbook
 83: ## 7. Freeze, the ending, and after
100: ## 8. FORMERLY OPEN QUESTIONS — ALL RESOLVED
122: ## 9. Feature tests this spec establishes
```

`gather-hinge-spec-v1.md` — 10 top-level sections, all present in order:

```
  1: # The Hinge — The Send
  2: ## Specification v1
 12: ## 1. The pre-flight (Kate's side of the threshold)
 32: ## 2. The press
 52: ## 3. What arrives (the guest's side)
 71: ## 4. Recipients — Kate's toggle
 81: ## 5. The voice at the door
 98: ## 6. The minutes after (the gap)
120: ## 7. Mechanics and failure
132: ## 8. The maybe state — RESOLVED
148: ## 9. Effects on the Moment 4 spec's open questions
158: ## 10. Feature tests (inherited and extended)
```

**4. §8 spot-check (the two headings that carry the resolution status) — PASS.**

```
gather-moment-4-spec-v1.md:100:## 8. FORMERLY OPEN QUESTIONS — ALL RESOLVED
gather-hinge-spec-v1.md:132:## 8. The maybe state — RESOLVED
```

All eight Moment 4 rulings present under §8 (8.1 red by time, 8.2 critical badge, 8.3 nudge cadence 4/7, 8.4 thank-you voice, 8.5 material changes, 8.6 green reverting, 8.7 minors and the unreachable, 8.8 the stale printout).

**5. Tail integrity (no truncation) — PASS.** Both files end on their final feature-test sentence: Moment 4 on the refusal test ("…must overturn the refusal's *reason*, not just re-request the feature."), the Hinge on the one-act test ("…*all fourteen are away* passes.").

**6. Prettier non-interference — PASS.** `.prettierignore` excludes `*.md`, so the lint-staged pre-commit hook cannot reformat these files. `prettier --check` on both files also reports clean. The committed bytes are the authored bytes.

---

## Known-superseded references (NOT corrected in this ticket)

Scan of `docs/` and `.claude/skills/` for (a) Moment 4 scope described as open / inferred / not-yet-built, and (b) the old freeze-as-ending model. **Listed only — no edits made.** The upcoming discovery pass owns the corrections.

### A. Living doctrine — skills (highest priority for the discovery pass)

| Location | What it says | Superseded by |
|---|---|---|
| `.claude/skills/gather-domain-reference/SKILL.md:60` | Moments table row 4: "**No dedicated V2 component exists.** Today: V1 invite-status/reachability views + freeze" | Moment 4 spec §3 (the screen), §4 (nudge machinery) — Moment 4 is now fully specified |
| `.claude/skills/gather-domain-reference/SKILL.md:66` | "Moments 3 & 4 as V2 experiences are **open roadmap**" | Moment 4 spec (all §§) + Hinge spec — no open questions remain in either |
| `.claude/skills/gather-domain-reference/SKILL.md:70–72` | "Frozen = the psychological finish line; participant responses are locked while frozen." | Moment 4 spec §7 — freeze dissolves into the send; the ending is Kate walking in holding nothing. §7 also: "Greens keep accumulating after the send — that's the Moment working" |
| `.claude/skills/gather-domain-reference/SKILL.md:40` | Gather "turns the plan into an external, confirmable state that can **freeze** into shared reality" | Same — the lock point is the send (Hinge §2, the lock-as-ledger), not a freeze ceremony |
| `.claude/skills/gather-domain-reference/SKILL.md:135` | Assignment response "blocked while FROZEN" | Moment 4 §7 — responses/claims are the plan being *answered*, not the plan changing |
| `.claude/skills/gather-domain-reference/SKILL.md:118–119, 177–186` | Freeze-readiness / `LOW_COMPLIANCE` <80% threshold framing | Moment 4 §2 — "no readiness score, no threshold, no completion nag" |
| `.claude/skills/gather-architecture-contract/SKILL.md:174–209` | `DRAFT → CONFIRMING → FROZEN → COMPLETE` as the lifecycle, with FROZEN semantics and the `frozen-edit` override route | Moment 4 §7 build implication: "the send becomes the lock point; what FROZEN currently models either collapses into the send or is removed." Flagged there as a state-machine change requiring Plan mode + max effort + review |
| `.claude/skills/gather-architecture-contract/SKILL.md:222–236` | Freeze-readiness warnings as what the host reads | Moment 4 §3 — the surface is the colour grid + one assistant message |
| `.claude/skills/gather-frontier-and-roadmap/SKILL.md:43, 55, 79` | "Freeze \| Event lifecycle transition CONFIRMING → FROZEN"; "Epic 4 — Freeze Enhancements"; "4.x Freeze enhancements \| open" | Moment 4 §7 — there is no freeze act and no freeze ceremony to enhance |
| `.claude/skills/gather-frontier-and-roadmap/SKILL.md:57, 81, 154, 159, 173` | Epic 6 metric instrumentation built on a "frozen-rate" metric | Same — "frozen rate" has no referent once freeze dissolves into the send |
| `.claude/skills/gather-docs-and-writing/SKILL.md:54` | Describes `docs/BUILD_STATUS.md` as the epic backlog incl. "freeze, threshold UX" | The epic framing for 3/4/5 is superseded by the two specs |

Note (not a conflict, supporting): `.claude/skills/gather-v1-v2-reconciliation-campaign/SKILL.md:33` already flags `docs/moment-1-and-2-build-report.md` and `docs/moment-2-flow-document.md` as STALE — on generation architecture. The Moment 4 references below are an additional reason.

### B. Living doctrine — docs

| Location | What it says | Superseded by |
|---|---|---|
| `docs/BUILD_STATUS.md:15, 20, 25` (Last Updated 2026-01-25, line 70) | "Epic 3: Nudge Infrastructure", "Epic 4: Freeze Enhancements", "Epic 5: Threshold UX" — all unchecked | Moment 4 §4 (nudge machinery, cadence ruled at 8.3) and §7 (no freeze act) |
| `docs/moment-2-flow-document.md:119` | "Still deciding" sections — "Gather knows not to assume and will flag this in Moment 4 for later attention" | Moment 4 §3 refuses everything not serving the four-second contract, and §5 puts criticality at the menu in Moment 2. Whether "still deciding" surfaces in Moment 4 at all is now a question the spec answers, not an open hand-off |
| `docs/moment-1-and-2-build-report.md:349` | Follow-on-moment tickets (GTC-FM1/FM2/FM3-FM5) for host nudges + post-event wrap-up land "closer to Moment 4/5 of the broader four-moment arc" | Moment 4 §4 (nudges) and §7 (thank-you, voice ruled at 8.4) now specify both |
| `docs/00_overview/project-overview.md:44, 47, 51, 85, 86, 139` | "DRAFT → CONFIRMING → FROZEN → COMPLETE"; "Freeze/unfreeze with audit logging"; "freeze controls"; "FreezeCheck component with coverage indicator"; "All items must be assigned before CONFIRMING→FROZEN" | Moment 4 §2 and §7. (Line 86 was already false before this spec — GTC-156 recorded freeze as warnings-only) |
| `docs/00_overview/onboarding-report.md:180, 552–579, 694, 719, 727` | State machine + `transitionToConfirming` flow + `structureMode = LOCKED` at CONFIRMING | Moment 4 §7 build implication — the lock point moves to the send |
| `docs/launch-readiness-section-6-instrumentation-VERIFIED.md:145` | Threshold instrumentation on `complianceRate, thresholdReached, criticalGaps, readyToFreeze` | Moment 4 §2 — no readiness score, no threshold |

### C. Archival records (mentioned for completeness; correction NOT expected)

These are closed-ticket and changelog records of what was true at the time. They should stay as written.

- `docs/tickets/GTC-133.md:283, 304, 314, 330` — "Moment 4, not yet built, OUT of scope"; "To be addressed during Moment 4 build" (`suggest-resolution` route AI call cap left at 10). Note for the discovery pass: the *route* is still uncapped-at-10 and unbuilt-for-V2 — that is a live build item even though the ticket text is archival.
- `docs/00_overview/changelog.md:71, 82` — CONFIRMING transition log entries.
- Closed freeze-related tickets (GTC-029, 048, 049, 062, 066, 067, 068, 069, 094, 148, 154, 156, 157, 158) — historical record of the freeze surface as built.

---

## Scope boundary

Explicitly **out of scope** for this ticket, by instruction:

- No edits to any file listed in the superseded-references table.
- No edits to the two specs themselves.
- No skill edits.
- No discovery work, no state-machine work, no code.

The corrections are owned by the upcoming discovery pass (its Task 4).

---

## Assertions met

1. ✅ Ticket filed first (ticket-first doctrine) — next free number confirmed by scanning `docs/tickets/`: highest existing is GTC-162, so GTC-163.
2. ✅ One chore ticket covers both files.
3. ✅ Both files placed in `docs/03_specs/`, alongside the existing spec documents.
4. ✅ Filenames match the specs' own cross-references exactly.
5. ✅ Integrity verified: SHA-256 byte-identical, line/byte counts match, full section structure intact, both §8 headings correct, no truncation at either tail.
6. ✅ Stale-doc conflicts scanned across `docs/` and `.claude/skills/`, listed above, **nothing edited**.
7. ✅ One commit containing the ticket + both specs, followed by the hash-backfill commit per house convention.
8. ✅ Branch verified: `feat/moment-one-redesign`.

## Preflight note

Preflight sanity sequence (install / migrate / boot / security suite) **not run** — this ticket touches only markdown under `docs/`. No source, schema, config, or dependency is affected; `.prettierignore` excludes `*.md` so even the local Husky gate is a no-op here. Recorded as a deliberate, scoped omission rather than a skipped gate.

## Evidence

- `shasum -a 256` before and after transfer — identical (table above).
- `wc -lwc` on both files on disk — counts above.
- `grep -nE '^#{1,2} '` heading dumps for both files — full section lists above.
- `prettier --check` on both files — "All matched files use Prettier code style"; `.prettierignore` contains `*.md`.
- Superseded-reference scan: `grep -rniE "moment ?-?4|moment four"` and `grep -rniE "freeze|frozen|confirming"` across `docs/` and `.claude/skills/`.
- Commits: (backfilled below)
